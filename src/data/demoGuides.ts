/**
 * Rich human-facing guides per demo: a substantial overview, the processing
 * pipeline, an explanation of every important parameter/argument, an annotated
 * code example, and concrete use cases. Rendered on the demo detail page to
 * supplement the terse catalog (opencvDemos.ts) spec data.
 *
 * Generated 2026-06 (reviewed). Keyed by demo id.
 */

export interface ParamDoc {
  arg: string;
  desc: string;
}

export interface DemoGuide {
  overview: string;
  pipeline: string[];
  params: ParamDoc[];
  code: string[];
  useCases: string[];
}

export const demoGuides: Record<string, DemoGuide> = {
  "grayscale-conversion": {
    overview: `カラー画像（RGBA/RGB）を1チャンネルの濃淡画像に変換する処理です。各画素のR・G・Bを輝度の式 Y = 0.299R + 0.587G + 0.114B（人間の目が緑に最も敏感な特性を反映）で重み付き平均し、1つの明るさ値にまとめます。結果は色情報が失われ、明暗だけの白黒画像になり、データ量が3分の1以下になります。エッジ検出や2値化など多くの画像処理の最初の前処理として使われます。`,
    pipeline: [`cv.imread でキャンバスから画像を読み込む（ブラウザ経由なので4チャンネルRGBA）`, `出力用の空の cv.Mat を用意する`, `cv.cvtColor に COLOR_RGBA2GRAY を渡し1チャンネルへ変換する`, `cv.imshow で結果を表示し、使い終わった Mat を delete で解放する`],
    params: [{ arg: `cv.COLOR_RGBA2GRAY`, desc: `変換コード。ブラウザのcanvasから来る画像は4チャンネル(RGBA)なのでRGBA2GRAYを使う。3チャンネルのときはCOLOR_RGB2GRAY、BGR順ならCOLOR_BGR2GRAYと使い分ける。間違ったコードを渡すとチャンネル数不一致でエラーになる。` }, { arg: `src`, desc: `入力画像Mat。カラーである必要がある。すでにグレースケールのものを渡すとエラーになる。` }, { arg: `dst`, desc: `出力先Mat。空のcv.Mat()を渡せば変換後の1チャンネル画像が自動で確保される。` }],
    code: [
      `let src = cv.imread('canvasInput'); // canvas経由なので4チャンネル(RGBA)で読み込まれる`,
      `let dst = new cv.Mat(); // 出力先。サイズ・型は自動で確保される`,
      `cv.cvtColor(src, dst, cv.COLOR_RGBA2GRAY); // 変換コード:RGBA→1chグレー。RGBならRGB2GRAYに変える`,
      `cv.imshow('canvasOutput', dst); // 結果を表示`,
      `src.delete(); dst.delete(); // メモリ解放(必須。漏れるとリークする)`,
    ],
    useCases: [`Cannyエッジ検出やHough変換などグレースケール入力が必須な処理の前段`, `QRコード・バーコード・文字(OCR)の読み取り前処理`, `顔検出や特徴点マッチングで計算量を1/3以下に減らす高速化`, `古い写真をモノクロ表現にするレタッチ`, `監視カメラ映像の動体検知で色の影響を排除して差分を取る`],
  },
  "image-resize": {
    overview: `画像を任意の倍率や指定サイズに拡大・縮小する処理です。新しい画素の色は元画像の周辺画素から補間（interpolation）して計算され、補間方法によって品質と速度が変わります。縮小では複数画素を平均する面積補間(INTER_AREA)、拡大では滑らかなINTER_CUBIC/LINEARが定石です。結果は指定サイズの画像になり、縮小すると処理が高速化し、拡大しすぎるとぼやけやジャギーが出ます。`,
    pipeline: [`出力用の空の cv.Mat を用意する`, `目標サイズ cv.Size を計算する(元サイズ×倍率、または絶対値)`, `縮小か拡大かに応じて補間方法を選ぶ(縮小=AREA、拡大=CUBIC/LINEAR)`, `cv.resize に dsize と補間方法を渡して変換する`],
    params: [{ arg: `dsize (cv.Size)`, desc: `出力サイズ(幅, 高さ)。これを指定すると後ろのfx/fyは無視される。大きくすると拡大、小さくすると縮小。0を渡すとfx/fyから計算される。` }, { arg: `fx / fy`, desc: `横・縦方向の倍率。dsizeに0を渡したときに有効。0.5で半分、2.0で2倍。dsizeを指定した場合は0でよい。アスペクト比を保つにはfxとfyを同じ値にする。` }, { arg: `interpolation`, desc: `補間方法。INTER_NEAREST=最近傍(最速だがギザギザ)、INTER_LINEAR=線形(標準的でバランス良)、INTER_AREA=面積平均(縮小に最適でモアレが出にくい)、INTER_CUBIC=4x4三次補間(拡大で最も滑らかだが遅い)。縮小はAREA、拡大はCUBICが推奨。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `let dsize = new cv.Size(src.cols*0.5, src.rows*0.5); // 目標サイズ:幅・高さを半分に(縮小)`,
      `cv.resize(src, dst, dsize, 0, 0, cv.INTER_AREA); // dsize優先なのでfx/fy=0。縮小なのでINTER_AREAが最適`,
      `// 拡大する場合の例:`,
      `// let dsize2 = new cv.Size(src.cols*2, src.rows*2); // 2倍に拡大`,
      `// cv.resize(src, dst, dsize2, 0, 0, cv.INTER_CUBIC); // 拡大は滑らかなCUBIC推奨`,
    ],
    useCases: [`機械学習モデルの入力サイズ(例:224x224)へ画像を揃える前処理`, `一覧表示用サムネイル画像の生成`, `高解像度写真を縮小して画像処理を高速化する`, `Webページ表示用に画像の容量を削減する`, `印刷向けに低解像度画像をアップスケールする(限界はあるが)`],
  },
  "image-rotate": {
    overview: `画像を任意角度で回転させる処理です。まず getRotationMatrix2D で「回転中心・角度・拡大率」から2x3のアフィン変換行列を作り、それを warpAffine で各画素に適用して座標を変換します。回転中心を画像中央にすると中心まわりに回り、角度がプラスで反時計回りになります。出力サイズを元のままにすると、傾けたぶん四隅がはみ出して切れる点に注意が必要です。`,
    pipeline: [`回転中心 cv.Point を決める(通常は画像中央)`, `cv.getRotationMatrix2D で角度・拡大率から2x3変換行列Mを作る`, `出力用の空の cv.Mat と出力サイズを用意する`, `cv.warpAffine で行列Mを適用し回転後の画像を得る`],
    params: [{ arg: `center (cv.Point)`, desc: `回転の中心座標。画像中央(cols/2, rows/2)にすると中心まわりに回る。左上(0,0)などにずらすと回転軸が変わり、画像が大きく画面外へ移動する。` }, { arg: `angle`, desc: `回転角度(度)。プラスで反時計回り、マイナスで時計回り。30で30度反時計回りに傾く。90/180など90度単位だけならcv.rotateの方が無劣化・高速。` }, { arg: `scale`, desc: `回転と同時にかける拡大率。1.0で等倍、2.0で2倍拡大、0.5で半分縮小。回転で四隅が切れるのを避けたいときに少し小さくして全体を収める使い方もある。` }, { arg: `dsize (cv.Size)`, desc: `warpAffineの出力サイズ。元と同じだと傾けた角がはみ出して切れる。切れないようにするには回転後の外接矩形サイズを計算して大きめに指定し、Mの平行移動成分も補正する。` }],
    code: [
      `let center = new cv.Point(src.cols/2, src.rows/2); // 回転中心:画像の中央`,
      `let M = cv.getRotationMatrix2D(center, 30, 1); // angle=30度(反時計回り), scale=1(等倍)の2x3行列`,
      `let dst = new cv.Mat(); // 出力先`,
      `cv.warpAffine(src, dst, M, new cv.Size(src.cols, src.rows)); // dsize=元サイズ。角がはみ出ると切れる`,
    ],
    useCases: [`スキャン・撮影した書類の傾きを補正してまっすぐにする`, `顔検出後に両目を水平に揃えて認識精度を上げる`, `学習データを少しずつ回転させて水増し(データ拡張)する`, `ジャイロ情報をもとに撮影画像の傾きを自動で立て直す`, `地図やレシートの向きを正しい方向に回す`],
  },
  "image-flip": {
    overview: `画像を水平・垂直・両方向に反転（鏡像化）する処理です。cv.flip に渡す flipCode で軸が決まり、画素の並びを左右や上下で入れ替えるだけなので非常に高速かつ無劣化です。左右反転は鏡に映したような像、上下反転は天地が逆の像になります。フロントカメラの自撮りで左右が逆に見える問題の補正や、学習データの水増しによく使われます。`,
    pipeline: [`出力用の空の cv.Mat を用意する`, `反転方向を表す flipCode を決める(0=上下, 1=左右, -1=両方)`, `cv.flip で画素を入れ替えて反転画像を得る`],
    params: [{ arg: `flipCode`, desc: `反転の軸。0=垂直反転(上下が逆、x軸まわり)、1(正の値)=水平反転(左右が逆、y軸まわり=鏡像)、-1=両方向(上下左右とも反転、180度回転と同じ見た目)。値が0か正か負かで意味が変わる点が学習ポイント。` }, { arg: `src`, desc: `入力画像Mat。カラーでもグレースケールでも、どんなチャンネル数でもそのまま使える。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `cv.flip(src, dst, 1); // flipCode=1:左右反転(鏡像)。0なら上下反転、-1なら両方向`,
      `// cv.flip(src, dst, 0);  // 上下(垂直)反転`,
      `// cv.flip(src, dst, -1); // 両方向(180度回転に相当)`,
    ],
    useCases: [`スマホのインカメラ(自撮り)で左右逆に映るのを正す鏡像補正`, `学習データを左右反転して枚数を倍にするデータ拡張`, `鏡に映したようなプレビュー表示(ビデオ通話の自分の映像など)`, `左右対称な部品の検査で片側のテンプレートを使い回す`, `印刷物の版下を鏡像にする(転写印刷向け)`],
  },
  "image-crop": {
    overview: `画像から矩形の関心領域（ROI: Region of Interest）を切り出す処理です。cv.Rect で(x, y, 幅, 高さ)を指定し、Mat.roi() でその範囲を参照します。roi() が返すのは元画像の一部を指す「ビュー(参照)」で、新しいメモリを確保しないため、書き換えると元画像にも影響します。独立した画像として扱うには clone() でコピーします。結果は指定した矩形だけを抜き出した小さな画像になります。`,
    pipeline: [`切り出したい矩形を cv.Rect(x, y, 幅, 高さ) で定義する`, `矩形が画像範囲内に収まるよう座標をクランプする`, `src.roi(rect) で領域を参照(ビュー)として取得する`, `clone() して元画像から独立したMatにする`],
    params: [{ arg: `cv.Rect(x, y, w, h)`, desc: `切り出す矩形。x,yは左上の座標、w,hは幅と高さ。x+wやy+hが画像サイズを超えるとエラーになるため、必ず範囲内にクランプする。w/hを大きくすると広い領域、小さくすると狭い領域を切り出す。` }, { arg: `src.roi(rect)`, desc: `矩形領域を返す。これは元画像を指す参照(ビュー)であり、新しいメモリは確保されない。これを書き換えると元のsrcも変わる点に注意。` }, { arg: `.clone()`, desc: `roiのビューを実体のあるMatに複製する。これを付けないと元画像に依存したままで、元をdeleteすると無効になる。独立した切り抜き画像が欲しいときは必須。` }],
    code: [
      `let rect = new cv.Rect(50, 50, 200, 150); // x=50,y=50 から 幅200×高さ150 を切り出す`,
      `// 範囲外エラーを防ぐにはx+w<=src.cols, y+h<=src.rowsを確認すること`,
      `let dst = src.roi(rect).clone(); // roi=参照ビューを取得 → clone()で独立したMatにコピー`,
      `// clone()を省くと元画像に依存したままなので注意`,
    ],
    useCases: [`顔検出で得たバウンディングボックスの顔部分だけを切り出す`, `書類の特定欄(日付・金額)だけを抜き出してOCRにかける`, `ナンバープレート領域を切り出して文字認識する`, `画像の不要な余白や背景をトリミングする`, `監視映像の特定エリアだけを処理対象にして負荷を下げる`],
  },
  "brightness-adjustment": {
    overview: `各画素値に一定値βを足し引きして画像全体の明るさを変える処理です。新しい値は out = α×in + β の式で計算され、明るさ調整ではα=1に固定してβだけを動かします。βがプラスで全体が明るく、マイナスで暗くなります。計算結果が0〜255の範囲を超えると自動で飽和（クリップ）されるため、明るくしすぎると白飛び、暗くしすぎると黒つぶれが起きます。convertTo で一括処理します。`,
    pipeline: [`出力用の空の cv.Mat を用意する`, `加算する明るさβを決める(プラスで明るく、マイナスで暗く)`, `src.convertTo(dst, -1, 1, β) でα=1・β加算を全画素に適用する`, `0〜255を超えた値は自動でクリップされる`],
    params: [{ arg: `rtype (第2引数 -1)`, desc: `出力の画素型。-1を渡すと入力srcと同じ型(通常8bit/CV_8U)を維持する。型を変えたい特別な場合以外は-1でよい。` }, { arg: `alpha (第3引数 1)`, desc: `コントラスト倍率。明るさ調整では1.0に固定する。これを変えると明暗差も同時に変わってしまう(それはコントラスト調整)。` }, { arg: `beta (第4引数)`, desc: `加算する明るさ。プラスで明るく(+30で全画素+30)、マイナスで暗く。大きくしすぎると上限255に張り付き白飛び、小さくしすぎると0に張り付き黒つぶれする。±30程度が自然。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `src.convertTo(dst, -1, 1, 30); // rtype=-1(型維持), alpha=1(コントラスト変えず), beta=30(明るさ+30)`,
      `// out = 1*in + 30 を全画素に適用。255超は255、0未満は0に自動クリップ`,
      `// betaを-30にすれば全体が暗くなる`,
    ],
    useCases: [`逆光やアンダー露出で暗く写った写真を明るく補正する`, `撮影条件の違う複数画像の明るさを揃える前処理(正規化)`, `夜間・室内の暗い監視映像を見やすくする`, `スキャン画像の地色を飛ばして文字を読みやすくする`, `明るさを変えた画像を量産して学習データを水増しする`],
  },
  "contrast-adjustment": {
    overview: `各画素値を定数倍αして明暗の差（コントラスト）を強調または緩和する処理です。out = α×in + β の式でβ=0に固定し、αだけを動かします。α>1で明るい所はより明るく暗い所はより暗くなりメリハリが増し、0<α<1で全体の差が縮まって眠い(平坦な)画像になります。明るさ調整と同じconvertToを使い、結果が範囲外になると飽和でクリップされるため、強くかけすぎると階調が失われます。`,
    pipeline: [`出力用の空の cv.Mat を用意する`, `コントラスト倍率αを決める(>1で強調、<1で緩和)`, `src.convertTo(dst, -1, α, 0) でβ=0・α倍を全画素に適用する`, `0〜255を超えた値は自動でクリップされる`],
    params: [{ arg: `rtype (第2引数 -1)`, desc: `出力の画素型。-1で入力と同じ型を維持する。通常は-1でよい。` }, { arg: `alpha (第3引数)`, desc: `コントラスト倍率。1.0で変化なし、1.5で明暗差を1.5倍に強調、0.5で半分に緩和。大きくしすぎると明部の白飛び・暗部の黒つぶれが増え階調が失われる。1.2〜1.8程度が自然。` }, { arg: `beta (第4引数 0)`, desc: `加算する明るさ。コントラスト調整では0に固定する。値を入れると明るさも同時にずれる。α倍は0を基準にかかるので、必要なら128中心にずらす工夫(in-128)*α+128もある。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `src.convertTo(dst, -1, 1.5, 0); // rtype=-1, alpha=1.5(コントラスト1.5倍), beta=0(明るさ変えず)`,
      `// out = 1.5*in + 0。明るい画素はより明るく、暗い画素はより暗くなる`,
      `// alphaを0.5にすると明暗差が縮まり眠い画像になる`,
    ],
    useCases: [`霧・曇りで眠くなった低コントラスト写真のメリハリ回復`, `X線・顕微鏡など医療/科学画像で微妙な濃淡差を強調する`, `うすい鉛筆書きや退色した文書の文字をはっきりさせる`, `2値化やエッジ検出の前に対象を背景から際立たせる前処理`, `製品検査でわずかな傷や色ムラを目立たせる`],
  },
  "channel-split-merge": {
    overview: `カラー画像をR・G・B各チャンネルの3枚の濃淡画像に分離（split）し、必要に応じて再合成（merge）する処理です。カラー画像が実は3枚のグレースケールの重ね合わせであることを体感できます。cv.split は MatVector に各チャンネルを格納し、get(i) で取り出します。各チャンネルを単独で見ると、その色成分が強い部分が明るく写ります。特定チャンネルだけ加工して色被りを直すといった応用ができます。`,
    pipeline: [`cv.cvtColor で4ch(RGBA)を3ch(RGB)に変換する`, `cv.MatVector を用意し cv.split で3チャンネルに分離する`, `channels.get(0/1/2) でR/G/B各チャンネルを取り出す`, `必要なら cv.merge で再合成し、MatVectorと各Matを delete する`],
    params: [{ arg: `cv.COLOR_RGBA2RGB`, desc: `splitの前に4ch(RGBA)から3ch(RGB)へ落とす変換コード。canvas由来の画像はアルファ付き4chなので、3チャンネルだけ扱いたいときに先に変換する。` }, { arg: `channels (cv.MatVector)`, desc: `分離後の各チャンネルを格納する入れ物。splitに渡すと自動でチャンネル数ぶんのMatが詰められる。使用後はchannels.delete()で必ず解放する(漏れやすい)。` }, { arg: `channels.get(i)`, desc: `i番目のチャンネルを取り出す。RGB順ならget(0)=R, get(1)=G, get(2)=B。取り出したMatも個別にdeleteが必要。順序は元画像のチャンネル並びに依存する点に注意。` }],
    code: [
      `let rgb = new cv.Mat();`,
      `cv.cvtColor(src, rgb, cv.COLOR_RGBA2RGB); // 4ch(RGBA)→3ch(RGB)に変換`,
      `let channels = new cv.MatVector(); // チャンネルを入れる器`,
      `cv.split(rgb, channels); // R/G/Bの3枚に分離`,
      `let rCh = channels.get(0); // 0番目=Rチャンネル(1ch濃淡)。1=G, 2=B`,
      `// 再合成: cv.merge(channels, dst);`,
      `// 解放: rCh.delete(); channels.delete(); rgb.delete();`,
    ],
    useCases: [`色かぶり(全体が青っぽい等)を特定チャンネルだけ補正して直す`, `植生指数の計算など赤・近赤外チャンネルだけを使う解析の理解`, `Gチャンネルが最も鮮明な性質を使ったエッジ検出の前処理`, `青チャンネルにノイズが乗りやすい現象を可視化して学ぶ`, `特定の色成分だけを差し替えるカラーグレーディング`],
  },
  "image-arithmetic": {
    overview: `2枚の画像を重み付きで足し合わせて1枚に合成（アルファブレンド）する処理です。cv.addWeighted は out = α×img1 + (1-α)×img2 + γ を画素ごとに計算します。αが1に近いほど画像1が濃く、0に近いほど画像2が濃く出ます。αを徐々に変えればクロスフェード(滑らかな切り替え)になります。2枚は同じサイズ・同じ型である必要があり、違うと事前にresizeで揃える必要があります。`,
    pipeline: [`合成する2枚の画像を同じサイズ・同じ型に揃える`, `画像1の重みαを決める(画像2の重みは自動で1-α)`, `cv.addWeighted(img1, α, img2, 1-α, γ, dst) で合成する`, `結果dstを表示する`],
    params: [{ arg: `alpha (第2引数)`, desc: `画像1(img1)の重み。1.0で画像1だけ、0.0で画像2だけ、0.5で半々。大きくすると画像1が濃く、小さくすると画像2が濃くなる。0〜1の範囲で使うのが一般的。` }, { arg: `beta (第4引数 1-alpha)`, desc: `画像2(img2)の重み。通常はalphaと足して1になるよう1-alphaを渡すと、明るさを保ったまま自然に混ざる。1を超える値にすると全体が明るくなり白飛びしやすい。` }, { arg: `gamma (第5引数)`, desc: `合成後に全画素へ加算するバイアス値。0で素直な加重平均。プラスにすると全体が明るく、マイナスで暗くなる。通常は0でよい。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `// 前提:img1とimg2は同じサイズ・同じ型(違えば事前にcv.resizeで揃える)`,
      `cv.addWeighted(img1, 0.5, img2, 0.5, 0, dst); // alpha=0.5(img1の重み), beta=0.5(img2の重み), gamma=0`,
      `// out = 0.5*img1 + 0.5*img2 + 0。alphaを0.8にすればimg1が濃く出る`,
    ],
    useCases: [`スライドショーやスクロールでの画像クロスフェード切り替え`, `ロゴや著作権表示を半透明で重ねる透かし(ウォーターマーク)合成`, `2枚の露出を混ぜて二重露光のような芸術表現を作る`, `before/after画像をスライダーで滑らかに混ぜて比較表示する`, `基準画像と現在画像をブレンドして位置ずれを目視確認する`],
  },
  "bitwise-operations": {
    overview: `画像とマスク（または2枚の画像）をビット単位で論理演算し、領域の合成・反転・くり抜きを行う処理です。AND は両方が白(1)の部分だけ残し、OR はどちらかが白の部分を残し、XOR は異なる部分だけ白にし、NOT は白黒を反転します。bitwise_and に mask を渡すと、マスクの白い部分だけ画像を残す「くり抜き」ができます。マスクと画像の合成や背景除去の土台となる基本演算です。`,
    pipeline: [`演算したい画像とマスク(8UC1)を用意する`, `実行する論理演算(AND/OR/XOR/NOT)を選ぶ`, `cv.bitwise_and/or/xor/not で演算する`, `AND等でmaskを渡すとマスクの白い部分だけ結果に残る`],
    params: [{ arg: `src1, src2`, desc: `演算する2つの入力。bitwise_andは両方が1のビットだけ、orはどちらか1なら1、xorは異なるとき1にする。同じ画像を両方に渡し第4引数maskを使うとマスク適用になる。サイズ・型は一致が必要。` }, { arg: `mask`, desc: `8ビット1チャンネル(CV_8UC1)のマスク。白(255)の画素位置だけ演算結果が書き込まれ、黒(0)の位置は出力されない。型が8UC1でないとエラー。inRangeやthresholdの出力をそのまま使える。` }, { arg: `演算の種類`, desc: `AND=共通部分(くり抜き)、OR=和(重ね合わせ)、XOR=排他(差分の可視化)、NOT=反転(白黒入れ替え、マスクの内外を反転するのに便利)。NOTだけは入力が1つ。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `cv.bitwise_and(src, src, dst, mask); // 同じ画像を2回渡し+mask指定 → maskの白い部分だけ残す(くり抜き)`,
      `// cv.bitwise_or(src1, src2, dst);   // どちらかが明るい部分を残す`,
      `// cv.bitwise_xor(src1, src2, dst);  // 異なる部分だけ白にする(差分)`,
      `// cv.bitwise_not(src, dst);         // 白黒反転(マスクの内外反転にも使う)`,
    ],
    useCases: [`inRangeで作ったマスクで対象色の領域だけ切り出す`, `円や任意形状のマスクで画像を型抜き(角を丸くする等)する`, `透過ロゴを背景画像へ自然に合成する(ロゴ部分をくり抜いて貼る)`, `2フレームのXORで変化した部分(動いた領域)を抽出する`, `マスクをNOTして対象と背景を入れ替える`],
  },
  "rgb-hsv-conversion": {
    overview: `RGB色空間と HSV（色相Hue・彩度Saturation・明度Value）色空間を相互変換する処理です。HSVは「色味・鮮やかさ・明るさ」を別々の軸で表すため、人が色を直感的に扱えて、照明の変化に比較的強いのが特長です。特にOpenCVでは8bit画像のHが0〜179（一般的な0〜360を半分にした値）である点が初学者の混乱ポイントです。色抽出や色調整など、色を扱う処理の基礎になります。`,
    pipeline: [`cv.cvtColor で4ch(RGBA)を3ch(RGB)に変換する`, `出力用の空の cv.Mat を用意する`, `cv.cvtColor に COLOR_RGB2HSV を渡してHSVへ変換する`, `H/S/Vの各成分を取り出して観察する(Hは0〜179スケール)`],
    params: [{ arg: `cv.COLOR_RGBA2RGB`, desc: `HSV変換の前にcanvas由来の4ch(RGBA)を3ch(RGB)へ落とす変換コード。RGB2HSVは3チャンネル入力を前提とするため先に必要。` }, { arg: `cv.COLOR_RGB2HSV`, desc: `RGBからHSVへの変換コード。チャンネル並びがRGB順のときに使う。逆変換はCOLOR_HSV2RGB。BGR順の場合はCOLOR_BGR2HSVを使う。` }, { arg: `H (色相, 出力ch0)`, desc: `色味。OpenCVの8bit画像では0〜179(一般的な角度0〜360度の半分)。赤≒0と179の両端、緑≒60、青≒120付近。0〜255だと思い込むと色がずれるので注意。` }, { arg: `S (彩度, 出力ch1)`, desc: `鮮やかさ。0〜255で、0は灰色(無彩色)、255で最も鮮やか。低いと色判定が不安定になりやすい。` }, { arg: `V (明度, 出力ch2)`, desc: `明るさ。0〜255で、0は黒、255で最も明るい。照明の影響を主に受ける軸なので、色判定ではVの範囲を緩めにすると照明変化に強くなる。` }],
    code: [
      `let rgb = new cv.Mat();`,
      `cv.cvtColor(src, rgb, cv.COLOR_RGBA2RGB); // 4ch(RGBA)→3ch(RGB)`,
      `let hsv = new cv.Mat();`,
      `cv.cvtColor(rgb, hsv, cv.COLOR_RGB2HSV); // RGB→HSV。注意:Hは0〜179スケール`,
      `// HSVの各成分は cv.split(hsv, channels) で H=0, S=1, V=2 として取り出せる`,
    ],
    useCases: [`特定色のボールや物体を照明変化に強く追跡する前段`, `信号機・道路標識の色(赤/青/黄)を判定する`, `肌色領域を検出して顔・手のひらを見つける`, `彩度Sだけ上げて鮮やかにする/明度Vだけ調整する色補正`, `クロマキー(背景の緑/青)を色相で切り出す前処理`],
  },
  "color-extraction-inrange": {
    overview: `HSV色空間で色相・彩度・明度の下限と上限を指定し、その範囲に入る画素だけを白くしたマスク画像を作る処理です。cv.inRange は各画素が lowerb〜upperb の範囲内なら255(白)、範囲外なら0(黒)にします。HSVで指定するため照明変化に比較的強く、特定色の物体だけを抜き出せます。注意点として赤はHの0付近と179付近の両端にまたがるため、2つの範囲を作ってORで合成する必要があります。`,
    pipeline: [`画像をHSVに変換しておく(別デモ参照)`, `抽出したい色の下限low(H,S,V)と上限high(H,S,V)をMatで作る`, `cv.inRange(hsv, low, high, mask) で範囲内を白・外を黒にする`, `得たマスクをbitwise_and等で元画像に適用して色領域を取り出す`],
    params: [{ arg: `lowerb (下限 H,S,V)`, desc: `抽出範囲の下限。例[35,50,50]は緑系の色相35以上・彩度50以上・明度50以上。Hを狭めると色味を絞り込み、SやVの下限を上げるとくすんだ色や暗い色を除外できる。下限を下げすぎると無関係な色まで拾う。` }, { arg: `upperb (上限 H,S,V)`, desc: `抽出範囲の上限。例[85,255,255]はH85まで・S/Vは最大。lowerbとupperbのH幅を広げるほど多くの色相が混ざる。緑なら概ねH=35〜85が目安。` }, { arg: `mask (出力)`, desc: `8UC1の2値マスク。範囲内の画素が255(白)、範囲外が0(黒)。そのままbitwise_andのmask引数やcopyToのマスクに使える。` }, { arg: `赤色の扱い`, desc: `赤はHが0付近と179付近の両端にまたがるため、低H側と高H側で2回inRangeしてbitwise_orで合成する必要がある。1範囲だけだと赤の半分しか拾えない。` }],
    code: [
      `// hsvは事前にRGB→HSV変換済みとする`,
      `let low = new cv.Mat(hsv.rows, hsv.cols, hsv.type(), [35,50,50,0]); // 下限 H=35,S=50,V=50(緑系)`,
      `let high = new cv.Mat(hsv.rows, hsv.cols, hsv.type(), [85,255,255,255]); // 上限 H=85,S/V=255`,
      `let mask = new cv.Mat();`,
      `cv.inRange(hsv, low, high, mask); // 範囲内=255(白), 範囲外=0(黒) の2値マスク`,
      `// 赤を抽出するなら0付近と179付近の2範囲を作りcv.bitwise_orで合成する`,
    ],
    useCases: [`特定色のボール・マーカーを追跡するロボット/AR`, `信号機や標識の赤・青・黄を色で判定する`, `緑/青バックのクロマキー合成で背景を抜く`, `果物・野菜の熟度を色で選別する`, `蛍光ペンでマークした箇所だけを書類画像から抽出する`],
  },
  "mask-operation": {
    overview: `2値マスク（白黒画像）を使って、画像の対象領域だけを残したり、消したり、背景を置き換えたりする処理です。bitwise_and(src, src, dst, mask) でマスクの白い部分だけ画像を残せます。マスクをNOTで反転すれば対象を消す、別画像をcopyToで流し込めば背景置換になります。色抽出やセグメンテーションで得たマスクを実際の画像に適用する、合成処理の要となるステップです。`,
    pipeline: [`対象領域が白・それ以外が黒の2値マスク(8UC1)を用意する`, `モードを選ぶ(対象を残す/対象を消す/背景を黒に など)`, `「残す」はbitwise_and(src, src, dst, mask)を実行する`, `「消す」はマスクをbitwise_notで反転してから適用する`],
    params: [{ arg: `mask`, desc: `8ビット1チャンネル(CV_8UC1)の2値マスク。白(255)の画素位置だけが出力に残り、黒(0)は捨てられる。inRangeやthresholdの出力をそのまま使える。型が8UC1でないとエラーになる。` }, { arg: `bitwise_and(src, src, dst, mask)`, desc: `同じ画像を2回渡してmaskを指定する定番パターン。マスクの白い部分だけ元の色が残り、黒い部分は0(黒)になる。これが『対象を残す』モードの実装。` }, { arg: `モード(keep/remove/bg-black)`, desc: `keep=マスクの白い部分(対象)を残す。remove=マスクをbitwise_notで反転してから適用し対象を消す。bg-black=対象以外を黒く塗る(keepと同じ結果になることが多い)。背景を別画像にするにはcopyToを使う。` }],
    code: [
      `let dst = new cv.Mat(); // 出力先`,
      `cv.bitwise_and(src, src, dst, mask); // maskの白い部分だけ元画像が残る('対象を残す')`,
      `// 対象を消すには先にマスクを反転:`,
      `// let inv = new cv.Mat(); cv.bitwise_not(mask, inv);`,
      `// cv.bitwise_and(src, src, dst, inv); // 反転マスクで対象側を消す`,
      `// 別背景に差し替えるには src.copyTo(background, mask) で白い部分だけ流し込む`,
    ],
    useCases: [`色抽出で得た対象だけを残し背景を黒くする`, `人物セグメンテーション結果で背景を別画像に差し替える`, `対象部分だけカラー・背景はモノクロにするスポットカラー表現`, `検出した不要オブジェクトをマスクで消去(インペイントの前処理)`, `ロゴ・ステッカーを形どおりに切り抜いて別画像へ合成する`],
  },
  "color-space-gallery": {
    overview: `画像を Lab・YCrCb・HLS・XYZ など RGB 以外の色空間へ変換し、それぞれの成分がどう分布するかを並べて比較する処理です。色空間ごとに「明るさ」と「色味」を分離する軸の取り方が異なるため、目的に応じた最適な色空間を選べるようになります。変換結果をそのまま RGB として可視化すると、各色空間特有の不自然な色合いで表示され、どのチャンネルにどの情報が乗っているかを直感的に把握できます。`,
    pipeline: [`入力画像を RGB 形式の Mat として用意する`, `選択した色空間に対応する cv.COLOR_RGB2* フラグを決める`, `cv.cvtColor で目的の色空間へ変換する`, `変換結果を RGB とみなしてキャンバスに描画し特性を観察する`],
    params: [{ arg: `space (変換先色空間)`, desc: `Lab は知覚的に均一で色差(ΔE)計算に向く。YCrCb は輝度Yと色差Cr/Cbを分け、肌色検出や JPEG 圧縮の基礎になる。HLS は色相・明度・彩度を直感的に扱え色選択UIに便利。XYZ はデバイス非依存の基準色空間で色管理の中間表現。用途に合わせて選ぶ。` }, { arg: `cv.COLOR_RGB2Lab (変換フラグ)`, desc: `cvtColor の第3引数。COLOR_RGB2YCrCb / COLOR_RGB2HLS / COLOR_RGB2XYZ などに差し替えると変換先が変わる。入力のチャンネル順(RGB か BGR か)とフラグを一致させないと色が崩れる点に注意。` }],
    code: [
      `let lab = new cv.Mat();`,
      `// 第1引数 rgb: 入力画像(RGB 3チャンネル)`,
      `// 第3引数 cv.COLOR_RGB2Lab: 変換先の色空間。YCrCb/HLS/XYZ なら COLOR_RGB2YCrCb 等に差し替える`,
      `cv.cvtColor(rgb, lab, cv.COLOR_RGB2Lab);`,
    ],
    useCases: [`Lab に変換して2色間のΔE(色差)を数値で評価し、印刷物の色再現精度をチェックする`, `YCrCb の Cr/Cb 成分だけで肌色領域を抽出し、顔検出の前処理にする`, `HLS の H(色相)成分で「赤い物体だけ」を照明変化に強く抽出する`, `色空間ごとのチャンネル分布を見比べて、画像処理の特徴量設計の勘所を学ぶ`],
  },
  "sepia-tone": {
    overview: `3x3 の色変換行列を各画素の RGB ベクトルに掛けることで、画像全体を古い写真のようなセピア調(茶系の単色トーン)に変える処理です。新しい R/G/B 値を元の3チャンネルの重み付き和として計算する線形色変換の典型例で、行列を差し替えれば白黒化や色相回転など任意の色効果を作れます。係数の合計が1を超える行では明るい部分が飽和(255でクリップ)し、独特の温かみのある退色した雰囲気が生まれます。`,
    pipeline: [`セピア変換用の 3x3 係数行列を cv.matFromArray で作成する`, `cv.transform で各画素 (R,G,B) に行列を適用し新しい色を計算する`, `intensity に応じて元画像と変換結果をブレンドして効果の強さを調整する`, `結果を描画する(飽和したハイライトが茶系に転ぶ)`],
    params: [{ arg: `変換行列の係数 [0.272,0.534,0.131, ...]`, desc: `1行目が新しいR、2行目が新しいG、3行目が新しいBを、元のR/G/Bからどう合成するかの重み。各行の係数を大きくすると該当チャンネルが明るくなり、行ごとの重みを揃えるほどグレースケールに近づく。標準的なセピア係数をそのまま使うのが無難。` }, { arg: `cv.CV_32F (行列のデータ型)`, desc: `matFromArray で作る係数行列の型。小数の重みを扱うため 32bit 浮動小数点(CV_32F)を指定する。整数型にすると係数が丸められ正しい色変換にならない。` }, { arg: `intensity (効果の強さ)`, desc: `0〜1。0で元画像のまま、1で完全セピア。元画像と変換結果を線形補間する係数として使うと、効果を弱めて自然な退色感に調整できる。0.5前後が「ほんのりレトロ」で扱いやすい。` }],
    code: [
      `// matFromArray(行数, 列数, 型, 値): 3x3 のセピア係数行列を作る`,
      `// 各行 = 出力R/G/B を入力R,G,B からどう混ぜるかの重み`,
      `let m = cv.matFromArray(3,3,cv.CV_32F,[0.272,0.534,0.131, 0.349,0.686,0.168, 0.393,0.769,0.189]);`,
      `let dst = new cv.Mat();`,
      `// transform(入力, 出力, 変換行列): 各画素の色ベクトルに行列を掛ける`,
      `cv.transform(rgb, dst, m);`,
    ],
    useCases: [`写真アプリのレトロ/ヴィンテージフィルタとしてセピア調を提供する`, `行列の係数を変えて白黒化・色温度補正など他の色効果を自作し、線形色変換の原理を学ぶ`, `プレゼン資料で「昔の写真」風の雰囲気を出す加工を行う`, `色変換行列(カラーマトリクス)の仕組みを理解し、ホワイトバランス補正の基礎を掴む`],
  },
  "average-blur": {
    overview: `注目画素を中心とした正方形の窓内にある全画素の単純平均で各画素を置き換える、最も基本的な平滑化(ぼかし)処理です。すべての近傍画素を等しい重みで足し合わせるボックスフィルタなので計算が高速ですが、エッジも一様にぼけます。窓(カーネル)を大きくするほど強くぼけて細部が失われ、軽いノイズが目立たなくなります。`,
    pipeline: [`ぼかしの強さを決めるカーネルサイズ(窓の一辺の画素数)を指定する`, `各画素について窓内の全画素値を合計し画素数で割って平均を求める`, `求めた平均値で中心画素を置き換える(これを全画素で行う)`, `平滑化された画像を出力する`],
    params: [{ arg: `ksize (cv.Size(5,5) / カーネルサイズ)`, desc: `平均を取る窓の幅と高さ。大きくするほど強くぼけてノイズは消えるがエッジや文字も潰れる。小さくすると効果が弱い。3〜7程度が軽い平滑化の目安。縦横を別の値にすると方向性のあるぼかしになる。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// blur(入力, 出力, カーネルサイズ)`,
      `// new cv.Size(5,5): 平均を取る窓の大きさ。値を大きくするほど強くぼける`,
      `cv.blur(src, dst, new cv.Size(5,5));`,
    ],
    useCases: [`撮影画像の細かなノイズを軽く抑えて見栄えを整える`, `エッジ検出や2値化の前処理として高周波の細かいゆらぎを減らす`, `プライバシー保護のため背景や顔を大まかにぼかす`, `ガウシアンぼかしと並べて、重みの違いによる仕上がりの差を学習する`],
  },
  "gaussian-blur": {
    overview: `中心に近い画素ほど大きな重みを与えるガウス分布の重み付き平均で平滑化する、ノイズ除去の定番処理です。単純平均より自然でなめらかなぼかしになり、リンギング(輪状のアーティファクト)が出にくいのが特徴です。カーネルサイズとσ(標準偏差)で効き具合が決まり、大きくするほど広範囲がなだらかにぼけて細かなノイズや高周波成分が抑えられます。`,
    pipeline: [`カーネルサイズ(奇数)とσ(ぼかしの広がり)を指定する`, `ガウス分布に基づく重みカーネルを内部生成する`, `各画素を窓内の重み付き平均で置き換える`, `なめらかにぼけた画像を出力する`],
    params: [{ arg: `ksize (cv.Size(5,5) / カーネルサイズ)`, desc: `ぼかしの窓サイズで必ず奇数(5,7,9...)を指定する。偶数や0は不可。大きいほど広範囲が強くぼける。Canny 前処理なら 5x5 程度が定番。0,0 を渡すと σ から自動でサイズが決まる。` }, { arg: `sigmaX (σ / 標準偏差)`, desc: `ガウス分布の広がり。大きいほど中心から離れた画素の重みも残り、強くぼける。0を指定するとカーネルサイズから自動計算される(初心者はまず0でよい)。明示すると ksize と独立にぼかしの柔らかさを微調整できる。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// GaussianBlur(入力, 出力, カーネルサイズ, sigmaX)`,
      `// new cv.Size(5,5): 奇数のカーネルサイズ。大きいほど強くぼける`,
      `// 第4引数 0: σ(標準偏差)。0でカーネルサイズから自動計算`,
      `cv.GaussianBlur(src, dst, new cv.Size(5,5), 0);`,
    ],
    useCases: [`Canny エッジ検出や輪郭抽出の前処理として高周波ノイズを除去する`, `写真の細かなセンサーノイズを自然な仕上がりで軽減する`, `ポートレートの背景ぼかし(被写界深度風)を作る`, `アンシャープマスクで元画像との差分を取り、シャープ化の基礎にする`],
  },
  "median-blur": {
    overview: `注目画素を中心とした窓内の画素値を並べ替え、その中央値(メディアン)で置き換える非線形フィルタです。平均と違って外れ値の影響を受けないため、画像にポツポツと現れる白黒の点(ごま塩ノイズ/インパルスノイズ)を、エッジを比較的保ったまま効果的に除去できます。窓を大きくするほどノイズ除去力は上がりますが、細部やテクスチャが消えて塗り絵のような質感になります。`,
    pipeline: [`窓サイズ(奇数)を指定する`, `各画素について窓内の全画素値を集める`, `それらを並べ替えて中央値を求める`, `中央値で中心画素を置き換えてノイズを除去する`],
    params: [{ arg: `ksize (窓サイズ)`, desc: `中央値を取る正方形窓の一辺で、3以上の奇数を指定する(整数1個で幅=高さ)。3でごま塩ノイズに効き、大きくするほど強力だが細部やテクスチャも消えて絵が平坦になる。ノイズ粒が大きいときだけ5〜7に上げる。平均ぼかしと違いエッジは比較的残る。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// medianBlur(入力, 出力, ksize)`,
      `// 第3引数 5: 窓サイズ。3以上の奇数。大きいほどノイズに強いが細部が消える`,
      `cv.medianBlur(src, dst, 5);`,
    ],
    useCases: [`古いスキャン画像や劣化写真のごま塩ノイズをクリーニングする`, `センサー欠陥によるホットピクセル(点状ノイズ)を除去する`, `2値化前にインパルスノイズを消して輪郭抽出を安定させる`, `イラスト風加工の前処理として細かなテクスチャを平坦化する`],
  },
  "bilateral-filter": {
    overview: `「空間的な近さ」に加えて「色(明るさ)の近さ」も重みに使うことで、エッジ(色が大きく変わる境界)をぼかさずに、平坦な領域だけを滑らかにする高度な平滑化フィルタです。色が大きく異なる画素は平均に混ざりにくいため、輪郭がくっきり残ったままノイズや肌のムラだけが消え、美肌処理やイラスト風加工に向きます。計算コストが高く、直径やσを大きくすると処理が重くなる点に注意が必要です。`,
    pipeline: [`入力を RGBA から RGB(8UC3)へ変換する`, `近傍直径 d と色σ・距離σを指定する`, `各画素で「距離の近さ」×「色の近さ」を重みにした加重平均を計算する`, `エッジを保ったまま平滑化された画像を出力する`],
    params: [{ arg: `d (近傍直径)`, desc: `フィルタが参照する近傍の直径(画素)。大きいほど広範囲を平滑化して効果が強まるが計算が急激に重くなる。5〜9 が実用範囲。負値やゼロにすると sigmaSpace から自動決定される。ブラウザでは大きすぎる値は避ける。` }, { arg: `sigmaColor (色σ / 色の混ざりやすさ)`, desc: `色の差をどこまで「似ている」とみなして混ぜるかの許容度。大きいほど色が違う画素も平均に混ざりエッジ保持が弱まる(=普通のぼかしに近づく)。小さいとエッジは強く残るがノイズが消えにくい。50〜100 が目安。` }, { arg: `sigmaSpace (距離σ / 空間的な広がり)`, desc: `どれだけ遠い画素まで影響させるかの空間的広がり。大きいほど広い範囲が平滑化される。d が正のときは d が優先され、d が0以下のときこの値から近傍サイズが決まる。sigmaColor と同程度の 50〜100 を合わせるのが定番。` }, { arg: `cv.BORDER_DEFAULT (境界処理)`, desc: `画像の端で窓がはみ出す部分の補完方法。BORDER_DEFAULT は鏡像反射でフチを埋め、端のにじみを抑える。通常は既定のままでよい。` }],
    code: [
      `let dst = new cv.Mat();`,
      `let rgb = new cv.Mat();`,
      `// bilateralFilter は RGBA 非対応のことがあるため RGB(3ch)へ変換しておく`,
      `cv.cvtColor(src, rgb, cv.COLOR_RGBA2RGB);`,
      `// bilateralFilter(入力, 出力, d, sigmaColor, sigmaSpace, 境界処理)`,
      `// 9: 近傍直径。大きいほど強く重くなる`,
      `// 75: 色σ。大きいほど色違いも混ざりエッジ保持が弱まる`,
      `// 75: 距離σ。空間的な平滑化の広がり`,
      `cv.bilateralFilter(rgb, dst, 9, 75, 75, cv.BORDER_DEFAULT);`,
    ],
    useCases: [`ポートレート写真の肌のムラやノイズを消し、輪郭は残す美肌処理`, `アニメ/イラスト風変換の前処理として色面を平坦化しつつ線を残す`, `エッジを保持したい医療・衛星画像のノイズ除去`, `ガウシアンぼかしと比較し、エッジ保持平滑化の効果を体感する`],
  },
  "sharpen-filter": {
    overview: `中心を強め周囲を引くシャープ化カーネルを画像に畳み込み、画素値の急変(エッジ)を強調してくっきり見せる処理です。中心の重みを大きく、上下左右を負にしたカーネルは、エッジ部分のコントラストを持ち上げて輪郭を際立たせます。強さを上げるほどシャープになりますが、過度に効かせるとノイズが目立ったり輪郭の周りに白い縁(ハロ)が出たりします。`,
    pipeline: [`シャープ化カーネル(中心が大きく周囲が負)を作成する`, `amount に応じてカーネルの強さを調整する`, `cv.filter2D でカーネルを画像に畳み込む`, `輪郭が強調された画像を出力する`],
    params: [{ arg: `kernel [0,-1,0,-1,5,-1,0,-1,0]`, desc: `シャープ化の3x3カーネル。中心5・上下左右-1で総和が1のため明るさを保ちつつエッジを強調する。中心を大きく(例:9)し周囲の負を増やすほど効果が強くなる。総和が1から外れると全体が明るく/暗くなる。` }, { arg: `amount (シャープ化の強さ)`, desc: `0〜3。0で無効、1で標準、値を上げるほど輪郭が強く立つ。大きすぎるとノイズ増幅やハロ(輪郭の白縁)が目立つ。カーネルの中心・周囲の重みをこの値でスケールして適用する。1前後が自然。` }, { arg: `cv.CV_8U (出力ビット深度)`, desc: `filter2D の出力 Mat の型。CV_8U は 0〜255 の通常画像。畳み込みで負値や255超が出ても自動で 0〜255 にクリップされるため、表示用にはこれが扱いやすい。-1 を渡すと入力と同じ型になる。` }],
    code: [
      `// matFromArray で 3x3 シャープ化カーネルを作る(中心5・周囲-1で総和1)`,
      `let kernel = cv.matFromArray(3,3,cv.CV_32F,[0,-1,0,-1,5,-1,0,-1,0]);`,
      `let dst = new cv.Mat();`,
      `// filter2D(入力, 出力, 出力深度, カーネル)`,
      `// cv.CV_8U: 出力は0〜255の8bit画像。範囲外は自動クリップ`,
      `cv.filter2D(src, dst, cv.CV_8U, kernel);`,
    ],
    useCases: [`ピントが甘い・わずかにぼけた写真の輪郭を補正する`, `縮小画像の細部(文字や模様)を強調して視認性を上げる`, `印刷前にディテールを際立たせる仕上げ処理`, `カーネルの数値を変えてアンシャープマスクの原理を学ぶ`],
  },
  "custom-kernel-filter2d": {
    overview: `ユーザーが 3x3 のカーネル(9つの数値)を自由に編集し、その重みで画像を畳み込んで効果がどう変わるかを体験する教材デモです。中心を1にすれば変化なし、周囲を負にすればエッジ強調、特定方向だけ重みを付けるとエンボス(浮き彫り)になるなど、CNN の畳み込みフィルタの基礎を手を動かして理解できます。カーネルの値の合計が1でないと画像全体が明るく/暗くなる点が、フィルタ設計の重要な勘所です。`,
    pipeline: [`9つの数値からなる 3x3 カーネルを入力する`, `cv.matFromArray でカーネル Mat を作成する`, `cv.filter2D でカーネルを画像に畳み込む`, `カーネルに応じたエッジ強調・ぼかし・エンボスなどの結果を観察する`],
    params: [{ arg: `values (3x3カーネルの9値)`, desc: `畳み込みの重み。中心だけ1で他0なら無変化、周囲を負にすればエッジ強調、全要素を1/9にすれば平均ぼかし。総和が1なら明るさ維持、1より大きいと全体が明るく、小さいと暗くなる。左上を負・右下を正など非対称にするとエンボス効果。` }, { arg: `cv.CV_32F (カーネルのデータ型)`, desc: `matFromArray で作るカーネルの型。小数や負の重みを扱うため浮動小数点(CV_32F)で作る。` }, { arg: `cv.CV_8U (出力ビット深度)`, desc: `filter2D の出力型。CV_8U は 0〜255 の通常画像で、計算で範囲外になった値は自動でクリップされる。負の畳み込み結果を符号付きで見たいときは CV_16S などを使う。` }],
    code: [
      `// values: UIで編集した9個の数値(3x3カーネル)。matFromArray でMat化`,
      `let k = cv.matFromArray(3,3,cv.CV_32F, values);`,
      `let dst = new cv.Mat();`,
      `// filter2D(入力, 出力, 出力深度, カーネル)`,
      `// cv.CV_8U: 出力は0〜255。範囲外は自動クリップ`,
      `cv.filter2D(src, dst, cv.CV_8U, k);`,
    ],
    useCases: [`CNN(畳み込みニューラルネット)のフィルタが何をするかを直感的に学ぶ教材`, `エンボス・エッジ強調・ぼかしなど各種カーネルを自作して効果を比較する`, `総和が1か否かで明るさが変わることを実験し、正規化の重要性を理解する`, `画像処理の課題で、目的に合うカスタムフィルタを試行錯誤で設計する`],
  },
  "nlmeans-denoising": {
    overview: `画像内の似たパッチ(小領域)を画像全体から探し出して平均することで、エッジやテクスチャを保ちながら高品質にノイズを除去する Non-local Means 法です。近傍画素だけでなく「形が似ている離れた領域」も活用するため、平均ぼかしより細部が残ります。ただし計算コストが非常に高く、OpenCV.js(techstark 4.10 ビルド)では photo モジュールが inpaint のみで本関数が未バインドのため、現状ブラウザでは利用できない場合がある点に注意が必要です。`,
    pipeline: [`photo モジュールで cv.fastNlMeansDenoisingColored が利用可能か確認する`, `ノイズ除去強度 h(輝度)と hColor(色)を指定する`, `各画素について画像内の類似パッチを探索し重み付き平均する`, `ノイズを抑えた高品質な画像を出力する(処理は重い)`],
    params: [{ arg: `h (輝度のノイズ除去強度)`, desc: `輝度成分のフィルタ強度。大きいほどノイズはよく消えるが細部やテクスチャも一緒に失われのっぺりする。小さいとノイズが残る。10前後が標準で、ノイズが強い写真ほど上げる。` }, { arg: `hColor (色のノイズ除去強度)`, desc: `色(クロマ)成分のフィルタ強度。コード例の2つ目の10。大きいほど色ノイズ(カラフルな斑点)が消えるが彩度の細かい変化も失われる。カラー画像では輝度hと同程度に設定するのが一般的。` }, { arg: `templateWindowSize (テンプレート窓)`, desc: `コード例の7。類似度を比較するパッチの一辺(奇数)。大きいほど比較が安定するが処理が重い。7が推奨の標準値。` }, { arg: `searchWindowSize (探索窓)`, desc: `コード例の21。類似パッチを探す範囲の一辺(奇数)。大きいほど遠くの似た領域も使えて品質が上がるが計算量が急増する。21が推奨値で、ブラウザでは大きくしすぎない。` }],
    code: [
      `// 注意: photo モジュール提供時のみ動作(OpenCV.js では未バインドの場合あり)`,
      `let dst = new cv.Mat();`,
      `// fastNlMeansDenoisingColored(入力, 出力, h, hColor, templateWindowSize, searchWindowSize)`,
      `// 10: 輝度のノイズ除去強度。大きいほど強いが細部も消える`,
      `// 10: 色のノイズ除去強度`,
      `// 7: 類似比較するパッチの一辺(奇数)`,
      `// 21: 類似パッチを探す範囲の一辺(奇数)。大きいほど高品質だが重い`,
      `cv.fastNlMeansDenoisingColored(rgb, dst, 10, 10, 7, 21);`,
    ],
    useCases: [`高 ISO で撮影した暗所写真のザラついたノイズを高品質に除去する`, `テクスチャや細部を残したい作品写真のレタッチ`, `天体・顕微鏡画像など細部が重要な科学画像のノイズ低減`, `平均/メディアン/バイラテラルとの仕上がり差を学ぶ(対応環境のみ)`],
  },
  "simple-threshold": {
    overview: `1つの固定しきい値を基準に、各画素を「しきい値より明るいか暗いか」で白か黒に分ける、最も基本的な2値化処理です。グレースケール画像の各画素値をしきい値と比較し、選んだ方式(BINARY なら超えたら白、INV なら逆)に従って変換します。仕組みが単純で高速ですが、画像全体で同じしきい値を使うため、照明にムラがあると一部が真っ黒/真っ白に潰れてしまいます。`,
    pipeline: [`入力をグレースケール化する`, `しきい値と2値化方式(type)を指定する`, `各画素値をしきい値と比較し方式に従って変換する`, `2値(または切り詰め)画像を出力する`],
    params: [{ arg: `thresh (しきい値)`, desc: `白黒を分ける境界値(0〜255)。上げると白くなる画素が減り(暗い側が増え)、下げると白が増える。被写体と背景の明るさの中間あたり(例 127)から調整する。照明ムラがあると最適値が場所ごとに変わり一律では破綻する。` }, { arg: `255 (maxval / 最大値)`, desc: `しきい値を超えた画素に割り当てる値。BINARY では超えた画素がこの値(白=255)になる。通常は 255 のままでよい。` }, { arg: `type (2値化方式)`, desc: `THRESH_BINARY=超えたら白/以下は黒、THRESH_BINARY_INV=その反転、THRESH_TRUNC=超えた分をしきい値で頭打ち(明部を抑える)、THRESH_TOZERO=以下を0にし明部はそのまま残す。マスク生成なら BINARY 系、明部抑制なら TRUNC を選ぶ。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// threshold(入力(グレー), 出力, thresh, maxval, type)`,
      `// 127: しきい値。これより明るい画素を白に。上げると白が減る`,
      `// 255: 超えた画素に割り当てる値(白)`,
      `// cv.THRESH_BINARY: 超えたら白/以下は黒。INV で反転、TRUNC で頭打ち等`,
      `cv.threshold(gray, dst, 127, 255, cv.THRESH_BINARY);`,
    ],
    useCases: [`明るさが一様なスキャン文書から文字を白黒に分離し OCR の前処理にする`, `背景と前景がはっきり分かれた画像でマスク(切り抜き範囲)を作る`, `明るさ一定の検査画像で欠陥や対象物を抽出する`, `TRUNC/TOZERO を試してハイライト抑制やトーン調整に応用する`],
  },
  "otsu-threshold": {
    overview: `画像の輝度ヒストグラムを解析し、前景と背景のクラス間分散が最大(=クラス内のばらつきが最小)になるしきい値を自動で計算する2値化です。人手でしきい値を決めなくても、明暗が2つの山(バイモーダル)に分かれた画像なら最適な境界を見つけてくれます。cv.threshold に THRESH_OTSU を併用し、戻り値として算出されたしきい値が得られるので、その値を表示すると学習に役立ちます。`,
    pipeline: [`入力をグレースケール化する`, `THRESH_BINARY と THRESH_OTSU を OR で指定する(thresh は無視され自動決定)`, `ヒストグラムからクラス間分散最大のしきい値を自動算出する`, `そのしきい値で2値化し、戻り値の最適しきい値も取得する`],
    params: [{ arg: `0 (thresh / しきい値)`, desc: `THRESH_OTSU 併用時はこの引数は無視され、最適値が自動計算される。慣例として 0 を渡す。手動値を入れても無効。` }, { arg: `255 (maxval / 最大値)`, desc: `しきい値を超えた画素に割り当てる白の値。通常 255。` }, { arg: `cv.THRESH_BINARY | cv.THRESH_OTSU`, desc: `BINARY(基本の2値化方式)に OTSU フラグを OR で足すと、しきい値を自動決定するモードになる。BINARY_INV と組み合わせれば自動決定＋反転も可能。前景/背景が明確な2峰でないと最適化が外れる。` }, { arg: `戻り値 t (算出しきい値)`, desc: `cv.threshold の返り値に、大津法が選んだ最適しきい値が入る。これを表示・記録すると、画像ごとにどんな境界が選ばれたかが分かり教育的。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// threshold(入力(グレー), 出力, thresh, maxval, type)`,
      `// 0: OTSU併用時このthreshは無視され自動計算される(慣例で0)`,
      `// 255: 超えた画素に割り当てる白`,
      `// THRESH_BINARY | THRESH_OTSU: 自動でしきい値を決める2値化`,
      `let t = cv.threshold(gray, dst, 0, 255, cv.THRESH_BINARY | cv.THRESH_OTSU);`,
      `// 戻り値 t = 大津法が選んだ最適しきい値`,
      `console.log('Otsu threshold =', t);`,
    ],
    useCases: [`明るさが変わる複数の文書を、画像ごとに最適なしきい値で自動2値化する`, `顕微鏡画像で細胞/背景のように明暗2峰の対象を自動分離する`, `しきい値を手調整できないバッチ処理で2値化を自動化する`, `算出されたしきい値を表示し、ヒストグラムからの最適化の仕組みを学ぶ`],
  },
  "adaptive-threshold": {
    overview: `画像全体で1つのしきい値を使う代わりに、各画素ごとにその周囲(ブロック)の局所的な明るさからしきい値を計算して2値化する処理です。場所ごとに基準が変わるため、片側だけ明るいといった照明ムラのある画像でも、文字や対象を均一にくっきり抜き出せます。ブロックサイズと定数 C で効き具合が決まり、書類スキャンの2値化で特に威力を発揮します。`,
    pipeline: [`入力をグレースケール化する`, `局所領域のブロックサイズ(奇数)・定数C・手法(平均/ガウシアン)を指定する`, `各画素ごとに周囲ブロックの(重み付き)平均から C を引いてしきい値を求める`, `そのローカルしきい値で各画素を2値化する`],
    params: [{ arg: `255 (maxval / 最大値)`, desc: `しきい値を超えた画素に割り当てる白の値。通常 255。` }, { arg: `adaptiveMethod (手法)`, desc: `ADAPTIVE_THRESH_MEAN_C は周囲ブロックの単純平均、ADAPTIVE_THRESH_GAUSSIAN_C は中心ほど重いガウス加重平均をしきい値の基準にする。GAUSSIAN の方がノイズに強く滑らかな結果になる。文字認識では GAUSSIAN が無難。` }, { arg: `cv.THRESH_BINARY (2値化方式)`, desc: `局所しきい値を超えたら白・以下は黒。THRESH_BINARY_INV にすると反転し、暗い文字を白く抜きたい場合に使う。` }, { arg: `blockSize (ブロックサイズ)`, desc: `しきい値を計算する近傍の一辺で、3以上の奇数を指定する。大きいほど広い範囲の平均を見て大きな構造に対応するが細部が鈍る。小さいとノイズを拾いやすい。文字サイズに合わせて 11〜31 程度で調整する。` }, { arg: `C (引く定数)`, desc: `局所平均から差し引く調整値。大きくすると白(前景)になりにくくなりノイズが減るが薄い文字が消える。小さく(負含む)すると白が増えノイズも拾う。2前後から微調整する。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// adaptiveThreshold(入力(グレー), 出力, maxval, 手法, 方式, blockSize, C)`,
      `// 255: 超えた画素に割り当てる白`,
      `// cv.ADAPTIVE_THRESH_GAUSSIAN_C: 周囲のガウス加重平均を基準に(MEAN_Cなら単純平均)`,
      `// cv.THRESH_BINARY: 超えたら白/以下は黒(INVで反転)`,
      `// 11: ブロックサイズ。奇数。大きいほど広範囲を平均`,
      `// 2: 平均から引く定数C。大きいほど白が減りノイズも減る`,
      `cv.adaptiveThreshold(gray, dst, 255, cv.ADAPTIVE_THRESH_GAUSSIAN_C, cv.THRESH_BINARY, 11, 2);`,
    ],
    useCases: [`影や照明ムラのある書類写真をムラなく2値化して OCR の精度を上げる`, `黒板やホワイトボードの撮影画像から文字をきれいに抜き出す`, `明るさが一定でないレシート/名刺のスキャン前処理`, `通常2値化と並べて、照明ムラへの強さの違いを学ぶ`],
  },
  "erosion": {
    overview: `構造要素(カーネル)を画像上で走らせ、その窓内が全て白のときだけ中心を白に残す(1つでも黒があれば黒にする)モルフォロジー演算で、白い領域を外側から削って縮める処理です。2値画像で効果が明確で、孤立した小さな白ノイズを消したり、くっついた物体を切り離したり、線を細くしたりできます。カーネルが大きいほど、また反復回数が多いほど強く縮みます。`,
    pipeline: [`cv.getStructuringElement で構造要素(矩形/楕円/十字)を作成する`, `カーネルサイズと反復回数を指定する`, `cv.erode で各画素の窓内が全て白のときだけ白を残す`, `白領域が縮んだ画像を出力する`],
    params: [{ arg: `cv.MORPH_RECT (構造要素の形)`, desc: `getStructuringElement で作る窓の形。MORPH_RECT=矩形、MORPH_ELLIPSE=楕円(角が丸く自然)、MORPH_CROSS=十字(上下左右だけ)。形によって削れ方が変わる。基本は矩形、円形物体には楕円が向く。` }, { arg: `new cv.Size(3,3) (カーネルサイズ)`, desc: `構造要素の大きさ。大きいほど一度に強く縮み、太いノイズや構造も削れるが対象本体もやせる。3x3 が最小で穏やか。奇数で指定するのが基本。` }, { arg: `iterations (反復回数)`, desc: `コードでは省略時1。収縮を繰り返す回数で、増やすほど累積して強く縮む。大きいカーネルを1回かけるか、小さいカーネルを複数回かけるかで効き方の細かさを調整できる。まず1で様子を見る。` }],
    code: [
      `// getStructuringElement(形, サイズ): 収縮に使う構造要素を作る`,
      `// cv.MORPH_RECT: 矩形(ELLIPSE=楕円, CROSS=十字 も選べる)`,
      `// new cv.Size(3,3): カーネルサイズ。大きいほど強く縮む`,
      `let M = cv.getStructuringElement(cv.MORPH_RECT, new cv.Size(3,3));`,
      `let dst = new cv.Mat();`,
      `// erode(入力(2値), 出力, 構造要素): 窓内が全て白のときだけ白を残す`,
      `// 反復回数を増やすには第4引数以降に anchor, iterations を渡す`,
      `cv.erode(bin, dst, M);`,
    ],
    useCases: [`2値マスクから孤立した小さな白い点ノイズを除去する`, `くっついて1つに見える物体を削って分離し、個数カウントを正確にする`, `文字や線画を細線化(スケルトン化の前処理)する`, `膨張(dilation)と組み合わせてオープニング/クロージングを構成し、形状を整える`],
  },
  "dilation": {
    overview: `膨張（Dilation）は2値画像の白領域（前景）を太らせる基本的なモルフォロジー演算です。構造要素（カーネル）を画像全体に走査し、カーネルが覆う近傍に1画素でも白があればその中心画素を白にします。結果として白い物体は外側に膨らみ、細い隙間や小さな黒い穴が埋まり、途切れた線が繋がって見えます。カーネルが大きいほど、また反復回数が多いほど太り方が強くなります。`,
    pipeline: [`入力をグレースケール化し、しきい値処理で2値画像（前景=白）を作る`, `getStructuringElement で形状とサイズを指定した構造要素（カーネル）を生成する`, `cv.dilate でカーネルを走査し、近傍に白があれば中心を白にする`, `必要なら iterations で繰り返し、膨張量を強める`],
    params: [{ arg: `ksize（カーネルサイズ）`, desc: `構造要素の一辺の画素数。大きいほど一度に太る量が増え、小さな穴や隙間も埋まりやすい。通常は奇数（3,5,7…）。3前後が穴埋めの定番、太らせ過ぎると物体同士が繋がるので注意。` }, { arg: `iterations（反復回数）`, desc: `膨張を連続適用する回数。回数を増やすほどカーネルサイズを大きくしたのと近い効果で前景が大きく成長する。1が基本。線の接続や大きな穴埋めには2〜3を試す。` }, { arg: `kernel shape（MORPH_RECT等）`, desc: `構造要素の形状。MORPH_RECT（矩形）は全方向に均等、MORPH_ELLIPSE（楕円）は角が丸く自然、MORPH_CROSS（十字）は縦横方向のみ太る。一般用途は RECT か ELLIPSE。` }],
    code: [
      `// 入力 bin は前景が白(255)の2値画像であること`,
      `let M = cv.getStructuringElement(`,
      `  cv.MORPH_RECT,        // 構造要素の形状(矩形)。ELLIPSE/CROSSも可`,
      `  new cv.Size(3, 3)     // カーネルサイズ。大きいほど強く太る(奇数推奨)`,
      `);`,
      `let dst = new cv.Mat();`,
      `cv.dilate(`,
      `  bin,                  // 入力(2値画像、前景=白)`,
      `  dst,                  // 出力先`,
      `  M,                    // 構造要素(カーネル)`,
      `  new cv.Point(-1, -1), // アンカー位置。(-1,-1)でカーネル中心`,
      `  1                     // iterations: 反復回数。増やすほど太る`,
      `);`,
      `M.delete(); // Matは明示的に解放`,
      `// dst を表示。bin/dst も使い終わったら delete する`,
    ],
    useCases: [`手書き文字やスキャン文書で、かすれて途切れた線を繋いで認識精度を上げる`, `二値マスク内に空いた小さな黒い穴（前景の欠け）を埋めて領域を一体化する`, `後段の輪郭検出やマスク合成の前に、検出対象の前景領域を確実に閉じた形に整える`, `細線化された血管や道路などの線状構造を強調し、視認性を高める`],
  },
  "opening": {
    overview: `オープニング（Opening）は「収縮→膨張」を同じカーネルで順に行う複合モルフォロジー演算です。先に収縮することでカーネルより小さい白い点ノイズや細い突起を消し去り、続く膨張で残った本体を元のサイズ近くまで戻します。結果として、大きな物体の形・サイズはほぼ保ったまま、孤立した白いゴミだけが取り除かれます。細く繋がった2つの領域を切り離す効果もあります。`,
    pipeline: [`ノイズを含む2値画像（前景=白）を用意する`, `getStructuringElement でカーネルを生成する`, `cv.morphologyEx に MORPH_OPEN を指定して収縮→膨張を一括実行する`, `小さな白ノイズが消え、本体の大きさは保たれた結果を得る`],
    params: [{ arg: `ksize（カーネルサイズ）`, desc: `除去できるノイズの大きさを決める。カーネルより小さい白領域が消える。大きくすると太い突起や細い接続も切れるが、本体のディテールも削れる。ノイズ除去は5前後が定番、消し過ぎなら小さくする。` }, { arg: `op = MORPH_OPEN`, desc: `morphologyEx の演算種別。MORPH_OPEN は収縮→膨張の順。白ノイズ除去・細い橋の切断に使う。黒い穴を埋めたい場合は逆の MORPH_CLOSE を選ぶ。` }, { arg: `kernel shape（MORPH_RECT等）`, desc: `構造要素の形状。ELLIPSE は丸い物体を自然に残し、RECT は角張った領域向き。ノイズ除去では ELLIPSE か RECT が一般的。` }],
    code: [
      `// 入力 bin は前景が白(255)の2値画像`,
      `let M = cv.getStructuringElement(`,
      `  cv.MORPH_RECT,        // 構造要素の形状`,
      `  new cv.Size(5, 5)     // この大きさ未満の白ノイズが除去される`,
      `);`,
      `let dst = new cv.Mat();`,
      `cv.morphologyEx(`,
      `  bin,                  // 入力(2値画像)`,
      `  dst,                  // 出力先`,
      `  cv.MORPH_OPEN,        // 演算種別: オープニング(収縮→膨張)`,
      `  M                     // 構造要素`,
      `);`,
      `M.delete();`,
      `// dst を表示。不要になったMatはdelete`,
    ],
    useCases: [`二値化後に残る塩ノイズ（白い点）を、文字や物体本体を保ったまま一掃する`, `細い橋で繋がってしまった2つの物体を切り離し、個別にカウントできるようにする`, `背景に散った微小な前景片を除去し、輪郭検出のノイズ輪郭を減らす`, `閾値処理で生じた前景のギザギザした突起を滑らかに整える`],
  },
  "closing": {
    overview: `クロージング（Closing）は「膨張→収縮」を同じカーネルで順に行う複合モルフォロジー演算で、オープニングの逆順です。先に膨張して前景内部の小さな黒い穴や細い隙間を埋め、続く収縮で外形を元のサイズへ戻します。結果として、物体の大きさは保ったまま内部の穴がふさがり、途切れた前景が繋がります。マスクの整形に多用されます。`,
    pipeline: [`穴や隙間を含む2値画像（前景=白）を用意する`, `getStructuringElement でカーネルを生成する`, `cv.morphologyEx に MORPH_CLOSE を指定して膨張→収縮を一括実行する`, `内部の黒い穴が埋まり、外形サイズはほぼ保たれた結果を得る`],
    params: [{ arg: `ksize（カーネルサイズ）`, desc: `埋められる穴・隙間の大きさを決める。カーネルより小さい黒領域（穴）がふさがる。大きくすると近接する別々の物体まで繋がってしまうので注意。穴埋めは5前後が定番。` }, { arg: `op = MORPH_CLOSE`, desc: `morphologyEx の演算種別。MORPH_CLOSE は膨張→収縮の順。前景内部の黒穴埋め・途切れの接続に使う。白ノイズ除去なら逆の MORPH_OPEN。` }, { arg: `kernel shape（MORPH_RECT等）`, desc: `構造要素の形状。ELLIPSE は穴埋めの境界が自然、RECT は計算が速く角張った領域向き。マスク整形では ELLIPSE が好まれることが多い。` }],
    code: [
      `// 入力 bin は前景が白(255)の2値画像`,
      `let M = cv.getStructuringElement(`,
      `  cv.MORPH_RECT,        // 構造要素の形状`,
      `  new cv.Size(5, 5)     // この大きさ未満の黒い穴が埋まる`,
      `);`,
      `let dst = new cv.Mat();`,
      `cv.morphologyEx(`,
      `  bin,                  // 入力(2値画像)`,
      `  dst,                  // 出力先`,
      `  cv.MORPH_CLOSE,       // 演算種別: クロージング(膨張→収縮)`,
      `  M                     // 構造要素`,
      `);`,
      `M.delete();`,
      `// dst を表示。不要になったMatはdelete`,
    ],
    useCases: [`色抽出（inRange）で得たマスクに開いた穴をふさぎ、物体を隙間のない一塊にする`, `途切れがちな文字のストロークやバーコードの線を繋いで読み取りを安定させる`, `前景セグメンテーション結果の内部欠けを埋めてから輪郭・面積を計測する`, `ノイズで分断された領域を統合し、後段のラベリングで1物体として扱えるようにする`],
  },
  "morphological-gradient": {
    overview: `morphologyEx の応用演算で、勾配・トップハット・ブラックハットの3種を切り替えます。モルフォロジー勾配（Gradient）は膨張と収縮の差で、物体の輪郭線だけが白く浮かび上がります。トップハット（Top-hat）は元画像とオープニングの差で、背景より明るい微小構造を抽出します。ブラックハット（Black-hat）はクロージングと元画像の差で、背景より暗い微小構造を取り出し、照明ムラの補正にも使えます。`,
    pipeline: [`入力画像（グレースケールまたは2値）を用意する`, `getStructuringElement でカーネルを生成する`, `morphologyEx に MORPH_GRADIENT / MORPH_TOPHAT / MORPH_BLACKHAT を指定する`, `選んだ演算に応じて輪郭・明部・暗部の微小構造が抽出された結果を得る`],
    params: [{ arg: `op（演算種別）`, desc: `GRADIENT＝膨張−収縮で輪郭抽出。TOPHAT＝元画像−オープニングで背景より明るい細部や背景ムラを抽出。BLACKHAT＝クロージング−元画像で背景より暗い細部を抽出。目的に応じて選ぶ。` }, { arg: `ksize（カーネルサイズ）`, desc: `抽出される構造のスケールを決める。Gradient では大きいほど輪郭線が太くなる。Top-hat/Black-hat では「カーネルより小さい」明暗構造が抽出されるため、対象のサイズに合わせて調整する。5前後から開始。` }, { arg: `kernel shape（MORPH_RECT等）`, desc: `構造要素の形状。輪郭抽出やムラ補正では ELLIPSE が滑らか、文字・線の強調では RECT が扱いやすい。` }],
    code: [
      `let M = cv.getStructuringElement(`,
      `  cv.MORPH_RECT,        // 構造要素の形状`,
      `  new cv.Size(5, 5)     // 抽出する構造のスケール(大きいほど太い輪郭/大きい構造)`,
      `);`,
      `let dst = new cv.Mat();`,
      `cv.morphologyEx(`,
      `  src,                  // 入力(グレースケール or 2値)`,
      `  dst,                  // 出力先`,
      `  cv.MORPH_GRADIENT,    // 演算: GRADIENT(輪郭) / TOPHAT(明部) / BLACKHAT(暗部)`,
      `  M                     // 構造要素`,
      `);`,
      `M.delete();`,
      `// dst を表示。不要になったMatはdelete`,
    ],
    useCases: [`MORPH_GRADIENT で物体の輪郭線だけを抽出し、エッジマップ代わりに使う`, `MORPH_TOPHAT で不均一な背景照明を取り除き、明るい文字や粒子を浮かび上がらせる`, `MORPH_BLACKHAT で背景より暗い傷・汚れ・文字を検出する（外観検査の前処理）`, `トップハット結果を二値化して、照明ムラに強い文字・欠陥のセグメンテーションを行う`],
  },
  "canny-edge-detection": {
    overview: `Canny法は、ノイズに強く線が細い高品質なエッジ検出アルゴリズムです。内部で Sobel により勾配の強さと方向を求め、勾配方向に沿って極大点のみ残す非極大抑制でエッジを1画素幅に細め、さらに2つのしきい値によるヒステリシス処理で強いエッジと、それに繋がる弱いエッジだけを残します。結果は黒背景に白い細線でエッジが描かれた2値画像になります。`,
    pipeline: [`入力をグレースケール化し、軽いガウシアンぼかしでノイズを抑える`, `内部 Sobel で各画素の勾配強度と方向を計算する`, `非極大抑制でエッジを1画素幅に細線化する`, `下限・上限の2しきい値でヒステリシス処理を行い、最終的なエッジを確定する`],
    params: [{ arg: `threshold1（下限しきい値）`, desc: `弱いエッジの下限。これ未満の勾配は完全に捨てる。上限を超える強エッジに連結していれば下限以上の弱エッジも採用される。下げると細かいエッジやノイズも拾い、上げると消える。` }, { arg: `threshold2（上限しきい値）`, desc: `強いエッジの基準。これを超えた画素は確実にエッジとして起点になる。上げると主要な輪郭だけ残り、下げるとエッジが増える。下限:上限＝1:2〜1:3が目安。` }, { arg: `apertureSize（Sobel窓）`, desc: `内部で勾配計算に使う Sobel カーネルのサイズ（3,5,7）。大きいほど太く滑らかな勾配を捉えノイズに鈍感になるが、位置精度が落ちる。通常は3で十分。` }, { arg: `L2gradient`, desc: `勾配の大きさ計算方法。falseは|gx|+|gy|の近似（高速）、trueは√(gx²+gy²)の正確な値。精度を要するなら true。デモでは false。` }],
    code: [
      `// 事前にグレースケール化し、軽くぼかしておくと安定する`,
      `let dst = new cv.Mat();`,
      `cv.Canny(`,
      `  gray,                 // 入力(8bitグレースケール)`,
      `  dst,                  // 出力(エッジ=白の2値画像)`,
      `  50,                   // threshold1: 下限しきい値。下げると細部/ノイズ増`,
      `  150,                  // threshold2: 上限しきい値。上げると主要輪郭のみ(目安1:2〜1:3)`,
      `  3,                    // apertureSize: 内部Sobelの窓(3/5/7)`,
      `  false                 // L2gradient: trueで√を使う正確な勾配計算`,
      `);`,
      `// dst を表示。不要になったMatはdelete`,
    ],
    useCases: [`物体の輪郭を抽出して、後段の findContours や Hough 変換による形状・直線検出につなげる`, `写真を線画（マンガ風・スケッチ風）に変換する画像加工`, `文書や名刺のエッジを検出し、四隅推定によるドキュメントスキャナの前処理にする`, `製品外観のエッジを抽出して、欠け・バリなどの形状異常を検査する`],
  },
  "sobel-derivative": {
    overview: `Sobel微分は、画像の輝度をx方向・y方向に1階微分し、輝度が急変する場所（＝エッジ）の強さと向きを求めます。各方向の勾配は3x3などの微分カーネルとの畳み込みで計算され、x勾配は縦エッジ、y勾配は横エッジに強く反応します。出力は符号付き（正負）になるため、そのまま表示すると暗くなり、convertScaleAbs で絶対値化してから可視化します。x・yの勾配からエッジ強度（magnitude）も合成できます。`,
    pipeline: [`入力をグレースケール化する（必要なら軽く平滑化）`, `cv.Sobel を CV_16S 等の符号付き型で呼び、x方向(dx=1)またはy方向(dy=1)の勾配を計算する`, `convertScaleAbs で絶対値を取り8bitに変換して可視化する`, `x・y両方を計算した場合は magnitude を合成してエッジ強度マップを得る`],
    params: [{ arg: `direction（方向）`, desc: `X はdx=1,dy=0で縦方向のエッジ（垂直線）に反応、Y はdx=0,dy=1で横方向のエッジに反応、合成(mag)は両者から√(gx²+gy²)でエッジ強度を作る。目的の向きに合わせて選ぶ。` }, { arg: `ksize（カーネルサイズ）`, desc: `Sobel微分カーネルの大きさ。1は単純差分、3が標準、5は広い範囲で平滑化しつつ微分するためノイズに強いが太いエッジになる。通常は3。` }, { arg: `ddepth（出力ビット深度）`, desc: `出力の型。勾配は負値を取るため CV_16S や CV_64F を使う。8bit(CV_8U)にすると負値が切り捨てられ片側のエッジが消えるので注意。` }, { arg: `convertScaleAbs`, desc: `符号付き勾配を絶対値化して0〜255の8bitへ変換し表示可能にする後処理。alpha/betaでスケール・オフセットも掛けられる。` }],
    code: [
      `let gx = new cv.Mat();`,
      `cv.Sobel(`,
      `  gray,                 // 入力(グレースケール)`,
      `  gx,                   // 出力(符号付き勾配)`,
      `  cv.CV_16S,            // ddepth: 負値を保持できる16bit符号付き`,
      `  1,                    // dx: x方向の微分次数(1=x勾配 → 縦エッジ検出)`,
      `  0,                    // dy: y方向の微分次数(0)`,
      `  3                     // ksize: Sobelカーネルサイズ(3が標準)`,
      `);`,
      `let abs = new cv.Mat();`,
      `cv.convertScaleAbs(gx, abs); // 絶対値化して8bitに変換し表示可能にする`,
      `// abs を表示。gx/abs は不要になったらdelete`,
    ],
    useCases: [`x勾配・y勾配からエッジの方向（角度）を求め、勾配方向ヒストグラム等の特徴量に使う`, `縦線・横線のどちらが優勢かを調べ、罫線やバーコードの向きを判定する`, `エッジ強度マップ（magnitude）を作り、Canny やしきい値処理の前段とする`, `勾配の急峻さから、画像のテクスチャの粗さ・コントラストを定量化する`],
  },
  "scharr-derivative": {
    overview: `Scharr微分は、3x3という小さなカーネルで Sobel より高精度に輝度勾配を求める演算です。Sobel の3x3カーネルは回転対称性が不完全で斜め方向の精度が落ちますが、Scharr は最適化された重み（中央±10、周辺±3）を使い、小カーネルでも正確な勾配方向を得られます。cv.Sobel で ksize=-1（CV_SCHARR）を指定するのと等価です。出力は符号付きなので convertScaleAbs で絶対値化して表示します。`,
    pipeline: [`入力をグレースケール化する`, `cv.Scharr を CV_16S 等で呼び、x方向(dx=1)またはy方向(dy=1)の勾配を計算する`, `convertScaleAbs で絶対値化して8bitに変換する`, `高精度な勾配マップとして表示・後段処理に利用する`],
    params: [{ arg: `direction（方向）`, desc: `X はdx=1,dy=0で縦方向エッジ、Y はdx=0,dy=1で横方向エッジに反応する。Scharr は1階微分専用で、x・yを同時に1にはできない（一方向ずつ計算する）。` }, { arg: `ddepth（出力ビット深度）`, desc: `出力の型。勾配は負値を含むため CV_16S や CV_64F を使う。CV_8U では負側が失われ片側エッジが消える。` }, { arg: `convertScaleAbs`, desc: `符号付き勾配を絶対値化し8bitへ変換して表示可能にする後処理。Sobel と同様に必須。` }],
    code: [
      `let gx = new cv.Mat();`,
      `cv.Scharr(`,
      `  gray,                 // 入力(グレースケール)`,
      `  gx,                   // 出力(符号付き勾配)`,
      `  cv.CV_16S,            // ddepth: 16bit符号付き(負値を保持)`,
      `  1,                    // dx: x方向の微分(1=縦エッジ)。Scharrは一方向ずつ`,
      `  0                     // dy: y方向の微分(0)`,
      `);`,
      `let abs = new cv.Mat();`,
      `cv.convertScaleAbs(gx, abs); // 絶対値化して8bitへ`,
      `// abs を表示。gx/abs は不要になったらdelete`,
    ],
    useCases: [`3x3の高速処理を保ちつつ、Sobelより正確な勾配方向が必要な特徴抽出に使う`, `微細なエッジやコントラストの低い境界を取りこぼさずに検出する`, `勾配方向の精度が重要な、エッジベースのテンプレートマッチングや形状記述の前段`, `Sobelと結果を並べて比較し、カーネル設計が勾配精度に与える影響を学習する`],
  },
  "laplacian": {
    overview: `Laplacianは輝度の2階微分（∂²/∂x² + ∂²/∂y²）を計算し、輝度変化の「変曲点」であるゼロ交差としてエッジを検出します。Sobel が方向ごとの1階微分なのに対し、Laplacian は方向に依存せず一度の演算で全方向のエッジに反応します。出力は符号付きで、エッジ周辺に正負のリンギングが出るため convertScaleAbs で可視化します。応答の分散（meanStdDev）は画像のシャープさの指標になり、ピントブレ判定に使われます。`,
    pipeline: [`入力をグレースケール化し、ノイズに敏感なので軽く平滑化する`, `cv.Laplacian を CV_16S 等で呼び、2階微分を計算する`, `convertScaleAbs で絶対値化して8bitに変換し、エッジを可視化する`, `応用として meanStdDev で分散を求め、ピントの鮮鋭度を数値化する`],
    params: [{ arg: `ksize（カーネルサイズ）`, desc: `2階微分カーネルの大きさ。1は標準の3x3ラプラシアン相当、3・5と大きくするほど広い範囲を見て応答が強くノイズの影響も増える。基本は1または3。` }, { arg: `ddepth（出力ビット深度）`, desc: `出力の型。ゼロ交差で正負を取るため CV_16S や CV_64F を使う。CV_8U だと負の応答が失われエッジが半分消える。` }, { arg: `convertScaleAbs`, desc: `符号付き出力を絶対値化して表示可能な8bitへ変換する後処理。ブレ判定では絶対値化せず CV_64F のまま meanStdDev に渡す。` }],
    code: [
      `// ノイズに敏感なので事前にGaussianBlurしておくと安定`,
      `let dst = new cv.Mat();`,
      `cv.Laplacian(`,
      `  gray,                 // 入力(グレースケール)`,
      `  dst,                  // 出力(符号付き2階微分)`,
      `  cv.CV_16S,            // ddepth: 負値を保持できる16bit符号付き`,
      `  3                     // ksize: カーネルサイズ。大きいほど応答強・ノイズ増`,
      `);`,
      `let abs = new cv.Mat();`,
      `cv.convertScaleAbs(dst, abs); // 絶対値化して8bitに変換し表示`,
      `// 応用: 分散が大きいほどシャープ(ピントが合っている)`,
      `// let mean = new cv.Mat(), std = new cv.Mat();`,
      `// cv.meanStdDev(dst, mean, std); // std^2 がブレ指標`,
      `// dst/abs は不要になったらdelete`,
    ],
    useCases: [`全方向のエッジを一度に検出して、線画化や輪郭強調を行う`, `Laplacian応答の分散を計算し、複数枚の写真からピントの合った1枚を自動選別する`, `オートフォーカスや手ブレ検知で、画像の鮮鋭度をリアルタイムに数値評価する`, `元画像にLaplacianを足し戻すアンシャープマスクで、画像をシャープ化する`],
  },
  "image-pyramids": {
    overview: `画像ピラミッドは、解像度の異なる画像を階層的に積み重ねた構造です。pyrDown はガウシアンぼかし後に偶数行・列を間引いて縦横を半分に縮小し、pyrUp は逆に2倍へ拡大します（足りない画素は補間で埋める）。pyrDown と pyrUp を往復すると高周波の情報が失われ、ぼけて戻ることが確認できます。この縮小列がガウシアンピラミッド、各層と1つ上の層を pyrUp した差がラプラシアンピラミッドで、マルチスケール処理やブレンドに使われます。`,
    pipeline: [`入力画像を用意する（カラー/グレースケールどちらも可）`, `縮小の場合は cv.pyrDown を levels 回繰り返し、各回で縦横を半分にする`, `拡大の場合は cv.pyrUp を levels 回繰り返し、各回で縦横を2倍にする`, `pyrDown→pyrUp の往復で情報が失われ画像がぼける様子を観察する`],
    params: [{ arg: `levels（階層）`, desc: `pyrDown/pyrUp を繰り返す回数。縮小では1回ごとに面積が1/4になり、回数が多いほど小さく粗くなる。拡大では逆に大きくぼやける。1〜5程度。` }, { arg: `direction（方向）`, desc: `down は pyrDown で縮小（ガウシアンぼかし＋間引き）、up は pyrUp で拡大（アップサンプル＋ぼかし補間）。down→up の順で適用すると元解像度に戻るが情報は失われている。` }, { arg: `dstsize`, desc: `出力サイズ。省略時は pyrDown が約半分、pyrUp が約2倍に自動設定される。奇数サイズだと丸め誤差が出るため、明示指定すると往復時のサイズずれを防げる。` }],
    code: [
      `let down = new cv.Mat();`,
      `cv.pyrDown(`,
      `  src,                  // 入力画像`,
      `  down                  // 出力(縦横が約半分に縮小される)`,
      `  // 第3引数 dstsize を省略すると約1/2サイズに自動設定`,
      `);`,
      `// さらに縮小するなら down を入力にもう一度 pyrDown を呼ぶ`,
      `let up = new cv.Mat();`,
      `cv.pyrUp(down, up);     // 縮小画像を2倍に拡大(高周波は失われ、ぼける)`,
      `// down/up は不要になったらdelete`,
    ],
    useCases: [`テンプレートマッチングや物体検出を粗い層から探索し、計算量を抑えて高速化する`, `ラプラシアンピラミッドを使い、2枚の画像を境界が目立たないように滑らかにブレンドする`, `オプティカルフローやステレオマッチングを多重解像度で実行し、大きな変位に対応する`, `サムネイル生成や段階的なズーム表示など、複数解像度を一括で用意する`],
  },
  "find-contours": {
    overview: `findContours は2値画像から物体の境界を、連続した点の列（輪郭）として抽出します。前景（白）と背景（黒）の境目をたどり、各物体を1本の閉じた点列で表現するため、形状解析・面積計測・物体カウントの起点になります。検出モードで輪郭の階層（外側のみ／全て／入れ子のツリー）を、近似方法で点列の間引き方を制御します。出力 contours は MatVector、hierarchy は Mat で、どちらも使用後に delete が必須です。`,
    pipeline: [`入力をグレースケール化し、threshold やモルフォロジーで質の良い2値画像を作る`, `輪郭の格納先 MatVector と階層情報の Mat を用意する`, `cv.findContours に mode と method を指定して輪郭点列を抽出する`, `contours.size() で物体数を取得し、各輪郭を後段の解析・描画に渡す`],
    params: [{ arg: `mode（検出モード）`, desc: `RETR_EXTERNAL は最も外側の輪郭だけ取得（物体カウント向き）。RETR_LIST は階層を無視して全輪郭をフラットに取得。RETR_TREE は親子（外形と内側の穴）の入れ子関係を完全な階層で取得する。` }, { arg: `method（近似方法）`, desc: `CHAIN_APPROX_SIMPLE は直線部分の中間点を省き端点だけ残すため点数が激減しメモリ効率が良い。CHAIN_APPROX_NONE は全境界画素を残すので描画は滑らかだが点数が多い。通常はSIMPLE。` }, { arg: `入力(2値画像)`, desc: `前景=白の8bit2値画像が前提。結果は2値化の質に強く依存するため、事前のしきい値処理・ノイズ除去（opening等）が重要。` }],
    code: [
      `// 入力 bin は2値画像(前景=白)`,
      `let contours = new cv.MatVector(); // 輪郭点列の格納先`,
      `let hierarchy = new cv.Mat();      // 階層情報(親子関係)`,
      `cv.findContours(`,
      `  bin,                  // 入力(2値画像)`,
      `  contours,             // 出力: 各輪郭の点列`,
      `  hierarchy,            // 出力: 輪郭の階層情報`,
      `  cv.RETR_EXTERNAL,     // mode: 外側のみ(LIST=全て/TREE=階層)`,
      `  cv.CHAIN_APPROX_SIMPLE// method: 点列を間引いて軽量化`,
      `);`,
      `console.log('count =', contours.size()); // 検出した輪郭(物体)数`,
      `// contours と hierarchy は必ず delete すること`,
      `contours.delete(); hierarchy.delete();`,
    ],
    useCases: [`二値化した顕微鏡画像や粒子画像から、細胞・粒の個数を自動カウントする`, `製品のシルエットを抽出し、面積・周囲長・形状特徴を計測する外観検査`, `色抽出マスクから対象物の領域を切り出し、トリミングや追跡につなげる`, `RETR_TREE で外形と内部の穴の関係を取得し、ドーナツ状や入れ子の形状を解析する`],
  },
  "draw-contours": {
    overview: `drawContours は findContours で得た輪郭を、元画像（またはコピー）の上に指定した色・太さで描画します。インデックスに -1 を渡すと全輪郭をまとめて描き、特定の番号を渡すとその輪郭だけを描けます。線の太さに -1（cv.FILLED）を指定すると輪郭内部を塗りつぶし、マスク生成にも使えます。検出結果の可視化やデバッグ、特定領域の塗り出しに用いる定番手段です。`,
    pipeline: [`findContours で輪郭（MatVector）を取得しておく`, `描画先の画像を用意する（元画像のコピーやカラー化したもの）`, `cv.drawContours に輪郭・インデックス・色・太さを渡して描画する`, `色付きの輪郭が重なった結果を表示・保存する`],
    params: [{ arg: `contourIdx（インデックス）`, desc: `描く輪郭の番号。-1 で全輪郭を一括描画、0以上でその番号の輪郭だけを描く。特定物体を強調したいときに番号を指定する。` }, { arg: `color（cv.Scalar）`, desc: `線の色をRGBA等で指定する。Scalar(255,0,0,255) は赤。RGBAキャンバスへ描く場合は4要素目（アルファ）を255にする。` }, { arg: `thickness（線の太さ）`, desc: `輪郭線の太さ（画素）。大きいほど太い線。-1（cv.FILLED）にすると輪郭内部を塗りつぶし、領域マスクや塗り分けに使える。` }, { arg: `lineType / hierarchy / maxLevel`, desc: `追加引数。lineType は cv.LINE_AA でアンチエイリアス。hierarchy と maxLevel を渡すと、階層の何階層下までネストして描くかを制御できる。` }],
    code: [
      `// dst は描画先(カラー画像)。contours は findContours の結果`,
      `cv.drawContours(`,
      `  dst,                  // 描画先の画像(上書きされる)`,
      `  contours,             // 描く輪郭の集合`,
      `  -1,                   // contourIdx: -1で全輪郭。番号指定で個別描画`,
      `  new cv.Scalar(255, 0, 0, 255), // color: 線の色(RGBA、ここでは赤)`,
      `  2                     // thickness: 線の太さ。-1で内部を塗りつぶし`,
      `);`,
      `// dst を表示。不要になったMat/MatVectorはdelete`,
    ],
    useCases: [`検出した輪郭を元画像に重ねて、セグメンテーション結果を目視確認する`, `thickness=-1 で特定輪郭を塗りつぶし、その領域だけのバイナリマスクを生成する`, `面積で選別した輪郭だけを色分け描画し、合格／不良などの分類結果を可視化する`, `輪郭番号付きで描画し、物体カウントや追跡のデバッグ表示を作る`],
  },
  "contour-area": {
    overview: `contourArea は1つの輪郭が囲む領域の面積（画素数換算）を計算します。内部ではグリーンの定理に基づき、輪郭の頂点座標から囲まれた多角形の面積を求めます。各輪郭の面積を一覧化すれば、サイズで物体を選別したり、小さすぎる輪郭をノイズとして除外したりできます。面積はあくまで画素単位なので、実寸法が必要な場合は別途スケール校正が要ります。`,
    pipeline: [`findContours で全輪郭を取得する`, `各輪郭に cv.contourArea を適用して面積（画素）を求める`, `minArea としきい値比較し、小さい輪郭をノイズとして除外する`, `残った輪郭の面積を一覧・ランキング表示する`],
    params: [{ arg: `minArea（最小面積）`, desc: `これ未満の面積の輪郭を除外するUI側のフィルタ閾値。上げるほど小さな物体やノイズ輪郭が消え大きい物体だけ残る。下げると微小領域も拾う。画像とノイズ量に応じて調整。` }, { arg: `contour（入力輪郭）`, desc: `contourArea に渡す1本の輪郭（MatVector から get(i) で取り出す）。点列が3点未満だと面積0になる。` }, { arg: `oriented`, desc: `省略可（既定false）。trueにすると頂点の並び順（時計回り/反時計回り）に応じて符号付き面積を返す。輪郭の向き判定に使えるが、面積の大小比較では絶対値（false）を使う。` }],
    code: [
      `// contours は findContours の結果(MatVector)`,
      `for (let i = 0; i < contours.size(); i++) {`,
      `  let a = cv.contourArea(`,
      `    contours.get(i)     // i番目の輪郭。面積(画素)を返す`,
      `    // 第2引数 oriented を true にすると符号付き面積`,
      `  );`,
      `  if (a < 100) continue; // minArea未満はノイズとして除外`,
      `  // a を物体サイズとして記録・表示する`,
      `}`,
      `// contours は不要になったらdelete`,
    ],
    useCases: [`二値化で生じた微小な輪郭ノイズを面積閾値で一括除外し、本物の物体だけ残す`, `粒子や細胞のサイズ分布を計測し、大小でグループ分け・統計を取る`, `面積でランキングし、画像中で最大の物体（主被写体）を自動選択する`, `面積と周囲長の比（円形度）を求めて、丸い物体と細長い物体を区別する`],
  },
  "contour-perimeter": {
    overview: `arcLength は輪郭の周囲長（弧長）を計算します。輪郭を構成する隣り合う点間のユークリッド距離をすべて足し合わせた値で、第2引数 closed が true なら最後の点と最初の点も結んで閉曲線として一周分を測り、false なら開いた折れ線として両端を結ばずに測ります。周囲長は形状の複雑さの指標になり、面積との比（円形度）や、多角形近似 approxPolyDP の許容誤差 epsilon を自動決定する基準としても使われます。`,
    pipeline: [`findContours で輪郭を取得する`, `対象の輪郭に cv.arcLength を適用し、closed の指定で開閉を選ぶ`, `得た周囲長を形状評価や面積との比較に用いる`, `周囲長×係数を epsilon として approxPolyDP に渡し、多角形近似につなげる`],
    params: [{ arg: `closed（閉曲線）`, desc: `trueなら輪郭を閉じた一周として周囲長を測る（始点と終点も連結）。falseなら開いた折れ線として両端を結ばずに測る。findContours の出力は閉輪郭なので通常 true。` }, { arg: `curve（入力輪郭）`, desc: `周囲長を測る点列（MatVector から get(i) で取得）。点が密なほど（CHAIN_APPROX_NONE）正確、間引かれていると（SIMPLE）わずかに短くなる傾向。` }],
    code: [
      `// contours は findContours の結果`,
      `let p = cv.arcLength(`,
      `  contours.get(0),      // 対象の輪郭(0番目)`,
      `  true                  // closed: trueで閉曲線として一周分を測る`,
      `);`,
      `// 応用: 周囲長を基準に多角形近似のepsilonを決める`,
      `// let epsilon = 0.02 * p; // 周囲長の2%を許容誤差に`,
      `// cv.approxPolyDP(contours.get(0), approx, epsilon, true);`,
      `// contours は不要になったらdelete`,
    ],
    useCases: [`周囲長²／面積（複雑さ指標）を計算し、輪郭の凹凸の多さ・形状の複雑さを評価する`, `円形度 4π×面積／周囲長² を求め、円・正方形・不定形を自動分類する`, `周囲長の数%を epsilon にして approxPolyDP を呼び、図形を頂点数の少ない多角形に近似する`, `周囲長の変化を追って、形状の崩れや欠けなどの品質異常を検出する`],
  },
  "polygon-approximation": {
    overview: `輪郭（点の連なり）を、見た目をほぼ保ったまま少数の頂点からなる多角形に置き換える処理です。Douglas-Peucker アルゴリズムにより、元の輪郭から大きく外れない範囲で冗長な点を間引き、許容誤差 epsilon を超えない最も単純な折れ線を求めます。結果として得られた頂点数を数えれば、三角形（3）・四角形（4）・五角形（5）といった形状分類ができ、四角形なら書類やカードの四隅座標として活用できます。`,
    pipeline: [`グレースケール化と二値化（threshold/Canny）で前処理する`, `findContours で対象の輪郭点列を取得する`, `arcLength で輪郭の周囲長を計算する`, `epsilon = 周囲長 × 比率 を求め approxPolyDP で近似する`, `得られた頂点数で形状を判定し overlay 描画する`],
    params: [{ arg: `epsilonRatio (epsilon)`, desc: `周囲長に対する許容誤差の割合。実際の epsilon は arcLength の戻り値に掛けて算出する。小さく（0.001 寄り）すると元輪郭に忠実で頂点が多く残り、大きく（0.1 寄り）すると大胆に間引かれ頂点が減る。図形分類なら 0.01〜0.04 が推奨。値がシビアでノイズの多い輪郭では不安定。` }, { arg: `closed`, desc: `arcLength と approxPolyDP の第2/最終引数。輪郭が閉じている（始点と終点を結ぶ）かを指定する。findContours で得た物体輪郭は閉曲線なので true。開いた線分列を扱う場合のみ false。` }],
    code: [
      `let approx = new cv.Mat();`,
      `let peri = cv.arcLength(cnt, true); // true=閉輪郭として周囲長を計算`,
      `let epsilon = 0.02 * peri; // 周囲長の2%を許容誤差に（小=忠実/大=粗い）`,
      `cv.approxPolyDP(cnt, approx, epsilon, true); // epsilon内で頂点を間引く, true=閉じる`,
      `let vertices = approx.rows; // 頂点数: 3=三角形, 4=四角形 ...`,
      `approx.delete();`,
    ],
    useCases: [`スキャンした書類やレシートの四隅を検出し、透視変換で正面化する前処理`, `図形パズルや幾何プリントで三角形・四角形・多角形を頂点数で自動分類する`, `QRコードやマーカーの四角い外枠を頂点4点として安定的に取り出す`, `手書き図形認識で、ギザついた輪郭をシンプルな多角形に整えてから判定する`, `地図やロゴのベクター化で、ピクセル輪郭を少ない頂点のポリゴンに変換する`],
  },
  "bounding-rect": {
    overview: `輪郭全体を過不足なく囲む、画像の軸に平行な（傾かない）最小の長方形を求める処理です。輪郭点の x の最小・最大、y の最小・最大を取るだけのシンプルな計算で、戻り値は左上座標 (x, y) と幅 width・高さ height です。物体の位置と大まかな大きさを矩形枠として一目で示せるため、検出結果の可視化やクロップ範囲の決定に最もよく使われます。`,
    pipeline: [`二値化と findContours で物体の輪郭を取得する`, `boundingRect に輪郭を渡して x, y, width, height を得る`, `rectangle で左上と右下の2点を結び矩形を描画する`],
    params: [{ arg: `cnt (array / Mat)`, desc: `boundingRect の入力。findContours で得た1つの輪郭（点群）を渡す。複数物体ではループで各輪郭に適用し枠を並べる。` }, { arg: `Scalar / thickness`, desc: `rectangle 描画時の色と線の太さ。色は (R,G,B,A) で 0〜255。thickness を正の値にすると枠線、-1 にすると塗りつぶしになる。可視化では 2 程度が見やすい。` }],
    code: [
      `let r = cv.boundingRect(cnt); // r.x, r.y, r.width, r.height を取得`,
      `let p1 = new cv.Point(r.x, r.y); // 矩形の左上`,
      `let p2 = new cv.Point(r.x + r.width, r.y + r.height); // 右下`,
      `let color = new cv.Scalar(0, 255, 0, 255); // 緑(R,G,B,A)`,
      `cv.rectangle(dst, p1, p2, color, 2); // 線の太さ2pxで枠を描画`,
    ],
    useCases: [`物体検出結果のバウンディングボックスとして検出枠を画面に重ねる`, `検出した文字・人物・商品領域を矩形でクロップして次工程に渡す`, `複数物体の個数カウントと、それぞれの位置・サイズ一覧の作成`, `ベルトコンベア上の部品の占有範囲を測り、はみ出しを検査する`, `動画追跡で対象のおおよその位置を毎フレーム矩形で更新する`],
  },
  "rotated-rect": {
    overview: `物体の傾きを許して、面積が最小になるように回転させた外接矩形（RotatedRect）を求める処理です。minAreaRect が中心座標・サイズ (width, height)・回転角度 angle を返し、boxPoints でその4頂点座標に展開できます。軸並行の外接矩形と違い、斜めに置かれた物体にぴったり沿うため、傾き角の推定や姿勢の計測に有効です。OpenCV 4.x では angle は概ね 0〜-90 度（4.5 以降は定義が見直され 0〜90 寄り）で返る点に注意が必要です。`,
    pipeline: [`二値化と findContours で輪郭を取得する`, `minAreaRect で中心・サイズ・角度を持つ回転矩形を求める`, `boxPoints（RotatedRect.points）で4頂点座標へ変換する`, `4頂点を線で結んで overlay 描画し、角度を numeric 表示する`],
    params: [{ arg: `cnt (Mat)`, desc: `minAreaRect の入力輪郭。点が密にある安定した輪郭ほど角度が正確になる。` }, { arg: `angle (戻り値)`, desc: `回転矩形の傾き角（度）。OpenCV の定義は直感に反しやすく、width/height のどちらを長辺とみなすかで符号や範囲が変わる。傾き補正に使うときは長辺基準に正規化する処理を添えると安定する。` }],
    code: [
      `let rr = cv.minAreaRect(cnt); // 中心 rr.center, サイズ rr.size, 角度 rr.angle`,
      `let angle = rr.angle; // 傾き角(度)。定義に注意(0〜-90付近)`,
      `let pts = cv.RotatedRect.points(rr); // 4頂点の座標配列に展開`,
      `for (let i = 0; i < 4; i++) {`,
      `  cv.line(dst, pts[i], pts[(i + 1) % 4], new cv.Scalar(255, 0, 0, 255), 2); // 隣り合う頂点を結ぶ`,
      `}`,
    ],
    useCases: [`斜めに置かれた部品やラベルの傾き角を測り、まっすぐに回転補正する`, `OCR 前処理で、傾いた文字行の角度を求めて水平化する`, `コンベア上の細長い物体の向きと長さ・幅を同時に計測する`, `顕微鏡画像で細胞や粒子の長軸方向（向き）を推定する`, `斜め撮影されたカードや名刺の外枠に沿った最小矩形を切り出す`],
  },
  "min-enclosing-circle": {
    overview: `輪郭上の全ての点を内側に含む、半径が最小の円を求める処理です。minEnclosingCircle が中心座標 center と半径 radius を返します。丸い物体や、向きを問わず大きさだけを評価したい対象に向いており、矩形では表現しづらい円形オブジェクトの位置と寸法を簡潔に示せます。輪郭から幾何的に円を当てはめるため、画像全体から円を探す Hough 円検出とは仕組みが異なります。`,
    pipeline: [`二値化と findContours で輪郭を取得する`, `minEnclosingCircle で中心と半径を求める`, `circle で中心点と外接円を overlay 描画する`],
    params: [{ arg: `cnt (Mat)`, desc: `minEnclosingCircle の入力輪郭。二値化品質が悪く輪郭にノイズの突起があると半径が過大になるため、前処理でモルフォロジー整形しておくと安定する。` }, { arg: `center / radius (戻り値)`, desc: `戻り値オブジェクトの center は浮動小数の中心座標、radius は半径。circle 描画では radius を整数に丸めて渡す。` }],
    code: [
      `let circle = cv.minEnclosingCircle(cnt); // circle.center と circle.radius を取得`,
      `let center = new cv.Point(circle.center.x, circle.center.y); // 中心`,
      `let radius = Math.round(circle.radius); // 半径(整数に丸める)`,
      `cv.circle(dst, center, radius, new cv.Scalar(255, 0, 0, 255), 2); // 外接円を描画`,
      `cv.circle(dst, center, 3, new cv.Scalar(0, 255, 0, 255), -1); // 中心点を塗りつぶしで表示`,
    ],
    useCases: [`コインやボール、果実など丸い物体の中心位置と半径を計測する`, `細胞やコロニーのおおよそのサイズ（直径）を一括で見積もる`, `向きが一定しない物体に対し、回転に依らない大きさ指標として半径を使う`, `検出物体に円マーカーを重ねて位置を視覚的に強調する`, `丸い部品の欠け・はみ出しを、理想円との差で簡易検査する`],
  },
  "convex-hull": {
    overview: `輪郭を外側から囲む最小の凸多角形（凸包）を求める処理です。輪郭の点の中から、内側にへこんだ点を除いて外周だけを結ぶことで、ゴムを掛けたような凸形状が得られます。元の輪郭と凸包を重ねれば、両者の差として凹み（くぼみ）が可視化でき、convexityDefects と組み合わせれば指の間のような凹凸を定量化できます。形状の単純化やジェスチャ認識の土台として使われます。`,
    pipeline: [`二値化と findContours で輪郭を取得する`, `convexHull で凸包（頂点群またはインデックス）を求める`, `凸包を polylines/drawContours で描画し元輪郭と重ねる`, `必要なら convexityDefects で凹みを解析する`],
    params: [{ arg: `clockwise (第3引数)`, desc: `凸包頂点の並び順。false で反時計回り、true で時計回り。描画だけなら影響は小さいが、凹み解析の向きに関わる。` }, { arg: `returnPoints (第4引数)`, desc: `true なら凸包の頂点座標そのものを、false なら元輪郭中のインデックスを返す。座標だけ描画したいなら true、convexityDefects に渡すなら false（インデックスが必要）にする。` }],
    code: [
      `let hull = new cv.Mat();`,
      `cv.convexHull(cnt, hull, false, true); // false=反時計回り, true=頂点座標を返す`,
      `let hullVec = new cv.MatVector();`,
      `hullVec.push_back(hull);`,
      `cv.drawContours(dst, hullVec, 0, new cv.Scalar(0, 0, 255, 255), 2); // 凸包を青線で描画`,
      `hull.delete(); hullVec.delete();`,
    ],
    useCases: [`手の輪郭の凸包と凹み（指の谷）から、立てた指の本数を数えるジェスチャ認識`, `複雑な物体形状を凸多角形に単純化して当たり判定や粗い領域表現に使う`, `凸包と元輪郭の面積比（solidity）で、物体の凹凸度合いを定量評価する`, `散らばった点群を囲む最小の囲い込み範囲を求める`, `破損や欠けのある部品で、本来の外形を凸包で推定し欠損量を測る`],
  },
  "image-moments": {
    overview: `輪郭または2値領域の画素分布を数値（モーメント）で表し、そこから面積や重心を導く処理です。0次モーメント m00 は面積に相当し、1次モーメント m10・m01 を m00 で割ると重心座標 (cx, cy) が得られます。物体の中心位置を1点で代表でき、追跡やマーキングに便利です。さらに高次の中心モーメントからは物体の向き（主軸の角度）も推定できます。`,
    pipeline: [`二値化と findContours で輪郭を取得する`, `moments で各次モーメント（m00, m10, m01 等）を計算する`, `cx = m10/m00, cy = m01/m00 で重心を求める`, `重心位置に点を描いて overlay 表示する`],
    params: [{ arg: `cnt (Mat)`, desc: `moments の入力。輪郭（点群）を渡すと輪郭ベースのモーメントを計算する。2値画像そのものを渡すこともできるが、その場合は領域全体の画素から計算される。` }, { arg: `binaryImage`, desc: `第2引数。true にすると非ゼロ画素をすべて値1として扱う（2値画像用）。輪郭を渡す通常用途では既定（false）でよい。` }],
    code: [
      `let M = cv.moments(cnt); // モーメント群を計算 (m00, m10, m01 ...)`,
      `if (M.m00 !== 0) { // m00=0(面積0)のゼロ除算を回避`,
      `  let cx = M.m10 / M.m00; // 重心X = m10/m00`,
      `  let cy = M.m01 / M.m00; // 重心Y = m01/m00`,
      `  cv.circle(dst, new cv.Point(cx, cy), 4, new cv.Scalar(255, 0, 0, 255), -1); // 重心に点を打つ`,
      `}`,
    ],
    useCases: [`動画中の物体の重心を毎フレーム求めてトラッキングする`, `検出した各物体の中心にラベルや番号を打つマーキング`, `ライントレースロボットで黒線領域の重心を求め進行方向を決める`, `中心モーメントから物体の主軸角を計算し、向き（姿勢）を推定する`, `領域の面積（m00）を使って物体の大小をフィルタリングする`],
  },
  "shape-matching": {
    overview: `2つの輪郭の形がどれだけ似ているかを、Hu モーメント（7つの不変量）に基づいて1つの距離値で評価する処理です。Hu モーメントは平行移動・拡大縮小・回転に対して不変なので、向きや大きさが違っても同じ形なら小さい値（=高い類似度）になります。matchShapes の戻り値は 0 に近いほど似ており、テンプレート形状との照合や形状分類に使えます。`,
    pipeline: [`2枚の画像をそれぞれ二値化し findContours で代表輪郭を取り出す`, `matchShapes に2つの輪郭と比較方法を渡す`, `戻り値（小さいほど類似）を numeric 表示する`],
    params: [{ arg: `method`, desc: `Hu モーメントの比較式の種類。I1 (CONTOURS_MATCH_I1)・I2・I3 の3種があり、いずれも7つの不変量の差を別の重み付けで合算する。多くの場合 I1 で十分。I3 は外れ値に強い傾向。用途に応じて切り替えて挙動を比較する。` }, { arg: `parameter`, desc: `matchShapes の第4引数。現行 OpenCV では未使用のため常に 0 を渡す。` }],
    code: [
      `let sim = cv.matchShapes(cntA, cntB, cv.CONTOURS_MATCH_I1, 0); // I1で比較, 第4引数は未使用(0)`,
      `// sim が 0 に近いほど形が似ている`,
      `console.log('similarity =', sim); // 例: 0.02 ならほぼ同形`,
    ],
    useCases: [`登録済みテンプレート形状と照合し、入力図形のカテゴリを判定する`, `向きや大きさがばらばらな部品を、形だけで同一種別に分類する`, `手書き記号や図形の認識で、回転に強い類似度指標として使う`, `ロゴやマークの簡易照合（細部より全体シルエットで判断）`, `良品形状との距離を測り、変形・欠けのある不良品を検出する`],
  },
  "hough-line-detection": {
    overview: `エッジ画像から直線を検出する処理で、Hough 変換は各エッジ点が通りうる直線をパラメータ空間で投票し、多くの票が集まった直線を抽出します。確率的版の HoughLinesP は実用的で、線分の両端点 [x1,y1,x2,y2] を直接返すため描画が容易です。罫線・枠・道路レーンなど直線的な構造の検出や、傾き推定に使われます。曲線は検出できません。`,
    pipeline: [`グレースケール化し Canny でエッジ画像を作る`, `HoughLinesP に投票しきい値・最小長・最大ギャップを渡す`, `各行 [x1,y1,x2,y2] を line で結んで overlay 描画する`],
    params: [{ arg: `rho`, desc: `距離方向のアキュムレータ解像度（ピクセル）。通常 1。大きくすると粗くなり検出は速いが精度が落ちる。` }, { arg: `theta`, desc: `角度方向の解像度（ラジアン）。通常 Math.PI/180（1度刻み）。細かくすると検出は精密だが計算が増える。` }, { arg: `threshold`, desc: `直線とみなすのに必要な投票数。大きくすると確かな（長く濃い）線だけが残り検出数が減る。小さくすると短い・かすれた線も拾うが誤検出が増える。50 前後が出発点。` }, { arg: `minLineLength`, desc: `これより短い線分は捨てる（HoughLinesP 専用）。大きくすると短い線が消え、ノイズに強くなる。` }, { arg: `maxLineGap`, desc: `同一直線上の途切れをどこまで1本の線として繋ぐかの許容隙間（HoughLinesP 専用）。大きくすると破線や途切れた線を連結できるが、別の線まで繋げてしまうことがある。` }],
    code: [
      `let edges = new cv.Mat();`,
      `cv.Canny(gray, edges, 50, 150); // 先にエッジ抽出`,
      `let lines = new cv.Mat();`,
      `cv.HoughLinesP(edges, lines, 1, Math.PI / 180, 50, 50, 10); // rho=1, theta=1度, 票50, 最小長50, ギャップ10`,
      `for (let i = 0; i < lines.rows; i++) {`,
      `  let x1 = lines.data32S[i * 4], y1 = lines.data32S[i * 4 + 1]; // 線分の始点`,
      `  let x2 = lines.data32S[i * 4 + 2], y2 = lines.data32S[i * 4 + 3]; // 線分の終点`,
      `  cv.line(dst, new cv.Point(x1, y1), new cv.Point(x2, y2), new cv.Scalar(255, 0, 0, 255), 2);`,
      `}`,
      `edges.delete(); lines.delete();`,
    ],
    useCases: [`車載カメラ映像から道路の白線（レーン）を検出する`, `表やフォームの罫線・枠線を抽出してセルを切り分ける`, `建物や部品のエッジから水平・垂直線を取り、画像の傾きを推定する`, `スポーツコートやフィールドのライン検出`, `バーコードや方眼など直線パターンの位置合わせ`],
  },
  "hough-circle-detection": {
    overview: `Hough 変換を円に拡張し、画像中の円形を検出する処理です。HOUGH_GRADIENT 法は内部でエッジ（Canny）と勾配方向を使って円の中心候補に投票し、票が集まった点を中心、半径を推定します。出力は各円の (x, y, 半径) です。コイン計数や瞳・部品検査など、丸い対象の位置と大きさを一括で得たい場面に向きます。前処理に medianBlur をかけると安定します。`,
    pipeline: [`グレースケール化し medianBlur でノイズを抑える`, `HoughCircles に HOUGH_GRADIENT と各パラメータを渡す`, `各列 [x, y, r] を取り出し circle で中心と円を描画する`, `検出した円の個数を numeric 表示する`],
    params: [{ arg: `dp`, desc: `アキュムレータ（投票空間）の解像度比。1 で入力画像と同解像度、2 で半分。大きくすると計算は速いが中心精度が落ちる。通常 1〜1.5。` }, { arg: `minDist`, desc: `検出する円中心同士の最小距離。小さすぎると1つの円が複数検出される。物体の想定間隔に合わせて設定する。コインなら半径程度が目安。` }, { arg: `param1`, desc: `内部 Canny の上側しきい値（下側は半分）。大きくするとエッジが厳しくなり弱い円を逃す。小さくすると拾いやすいがノイズも増える。` }, { arg: `param2`, desc: `中心検出のアキュムレータしきい値。小さいほど誤検出（偽の円）が増え、大きいほど確かな円だけ残るが見逃しも増える。30 前後から調整する。` }, { arg: `minRadius / maxRadius`, desc: `検出する半径の下限・上限（ピクセル）。0 を渡すと制限なし。対象の大きさが分かっていれば範囲を絞ると誤検出が大幅に減る。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `cv.medianBlur(gray, gray, 5); // 円検出前にノイズ低減`,
      `let circles = new cv.Mat();`,
      `cv.HoughCircles(gray, circles, cv.HOUGH_GRADIENT, 1, 30, 100, 30, 0, 0); // dp=1, minDist=30, p1=100, p2=30, 半径制限なし`,
      `for (let i = 0; i < circles.cols; i++) {`,
      `  let x = circles.data32F[i * 3], y = circles.data32F[i * 3 + 1]; // 円の中心`,
      `  let r = circles.data32F[i * 3 + 2]; // 半径`,
      `  cv.circle(dst, new cv.Point(x, y), r, new cv.Scalar(0, 255, 0, 255), 2);`,
      `}`,
      `gray.delete(); circles.delete();`,
    ],
    useCases: [`テーブル上のコインを検出して枚数を数え、半径から金種を推定する`, `顔画像から瞳・虹彩を検出して視線推定の起点にする`, `工業検査でボルト穴や円形部品の位置と径を測り欠品をチェックする`, `細胞やビーズなど円形の対象を計数する`, `ダイヤルやメーターの円形目盛り位置を特定する`],
  },
  "template-matching": {
    overview: `小さなテンプレート画像を、大きな入力画像の上で1ピクセルずつスライドさせ、各位置で似ている度合い（相関スコア）を計算する処理です。matchTemplate が結果マップを返し、minMaxLoc でスコアが最良の位置を取れば、テンプレートが写っている場所が分かります。正規化相関を使えばスコアは 0〜1 になり、しきい値処理で同一パターンを複数検出できます。回転や拡大縮小には弱く、向き・大きさが一致している必要があります。`,
    pipeline: [`探索対象画像とテンプレート画像（ROI 切り出しまたは2枚目入力）を用意する`, `matchTemplate で類似度マップを計算する`, `minMaxLoc で最良スコアの位置を求める`, `その位置にテンプレートサイズの矩形を描画する（しきい値で複数検出）`],
    params: [{ arg: `method`, desc: `類似度の計算法。TM_CCOEFF_NORMED は明るさ変化に強く汎用的（最大が最良、~1）。TM_CCORR_NORMED は相関ベース。TM_SQDIFF_NORMED は差の二乗和で、こちらは最小が最良（0 が完全一致）になる点に注意。NORMED 版はスコアが 0〜1 に正規化され、しきい値を決めやすい。` }, { arg: `threshold`, desc: `検出採用の下限スコア。0.8 程度から始め、高くすると確実な一致だけ、低くすると緩く拾うが誤検出が増える。SQDIFF 系では逆に上限として扱う。` }, { arg: `templ (テンプレート画像)`, desc: `探す対象のパターン画像。入力画像と同じチャンネル・型である必要がある。回転・拡大縮小には不変でないため、見た目が一致したものを用意する。` }],
    code: [
      `let res = new cv.Mat();`,
      `cv.matchTemplate(img, templ, res, cv.TM_CCOEFF_NORMED); // 正規化相関でスコアマップを計算`,
      `let mm = cv.minMaxLoc(res); // 最大/最小スコアとその位置を取得`,
      `let maxLoc = mm.maxLoc; // CCOEFF系では最大が最良位置`,
      `let p2 = new cv.Point(maxLoc.x + templ.cols, maxLoc.y + templ.rows); // テンプレートサイズ分の右下`,
      `cv.rectangle(img, maxLoc, p2, new cv.Scalar(0, 0, 255, 255), 2); // 検出枠を描画`,
      `res.delete();`,
    ],
    useCases: [`UI 自動操作で画面上のアイコンやボタンの位置を見つけてクリックする`, `プリント基板で既知の部品マークの有無と位置を検査する`, `ゲーム画面から特定オブジェクトを探し座標を取得する`, `書類上の決まったロゴ・印影・記号の位置検出`, `同じ模様が複数並ぶ画像で、しきい値処理により全インスタンスを検出する`],
  },
  "histogram-calculation": {
    overview: `画像の輝度や各色チャンネルが、どの明るさ（0〜255）にどれだけ分布しているかを数え上げ、棒グラフ（ヒストグラム）として可視化する処理です。calcHist が各ビン（階級）の画素数を返し、それを描画することで露出オーバー・アンダーや色の偏りが一目で分かります。グラフ描画自体は OpenCV は行わないため、結果配列をアプリ側で Canvas や SVG に描く必要があります。`,
    pipeline: [`グレースケール化、または色チャンネルを分離する`, `対象 Mat を MatVector に詰めて calcHist に渡す`, `ビン数と値域を指定してヒストグラム（各階級の度数）を得る`, `結果配列を Canvas/SVG で棒グラフとして描画する`],
    params: [{ arg: `channels ([0])`, desc: `ヒストグラムを取る対象チャンネルの番号。グレースケールや単一チャンネルなら [0]。カラーで R/G/B を別々に取りたいときは split してそれぞれ [0] で計算する。` }, { arg: `histSize ([256])`, desc: `ビン（階級）数。256 なら 0〜255 を1刻みで集計する。減らす（例 8, 32, 64）と粗いが滑らかな分布になり、ノイズの影響を受けにくくなる。` }, { arg: `ranges ([0,255])`, desc: `集計する値の範囲。8bit 画像では [0, 256] 相当（上限は排他的）。範囲を絞ると特定輝度帯だけを詳しく見られる。` }, { arg: `mask`, desc: `集計対象を限定するマスク。空 Mat を渡すと画像全体。特定領域だけのヒストグラムが欲しいときに白マスクを指定する。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `let srcVec = new cv.MatVector();`,
      `srcVec.push_back(gray); // 入力はMatVectorで渡す`,
      `let hist = new cv.Mat();`,
      `let mask = new cv.Mat(); // 空=画像全体を対象`,
      `cv.calcHist(srcVec, [0], mask, hist, [256], [0, 255]); // ch0, ビン256, 値域0-255`,
      `// hist.data32F[i] が輝度値iの画素数 → アプリ側でグラフ描画`,
      `srcVec.delete(); hist.delete(); mask.delete();`,
    ],
    useCases: [`撮影画像の露出（白飛び・黒つぶれ）をヒストグラムの偏りで確認する`, `色被り（特定チャンネルの偏り）を検出してホワイトバランスの方針を決める`, `二値化のしきい値を、谷（バイモーダルの境）から手動で選ぶ`, `前処理として均一化や正規化が必要かを分布から判断する`, `画像間の明るさ傾向の比較・品質チェック`],
  },
  "histogram-equalization": {
    overview: `輝度ヒストグラムが特定の範囲に偏っている画像に対し、累積分布関数を使って明るさの分布を全域に引き伸ばし、コントラストを自動的に高める処理です。equalizeHist はグレースケール画像にそのまま適用でき、暗い部分や薄い部分の細部が浮かび上がります。画像全体に一律で効くグローバル手法のため、局所的な明暗ムラには弱く、その場合は CLAHE が適します。カラー画像では YCrCb の Y チャンネルだけに適用するのが定石です。`,
    pipeline: [`画像をグレースケール化する`, `equalizeHist で輝度分布を平坦化する`, `処理前後の画像とヒストグラムを並べて比較表示する`],
    params: [{ arg: `src (グレースケール)`, desc: `equalizeHist の入力は 8bit 単一チャンネル（グレースケール）である必要がある。カラーをそのまま渡せないため、色画像は YCrCb に変換して Y だけに適用し、戻して RGB 化する。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY); // 8bit単一chに変換`,
      `let dst = new cv.Mat();`,
      `cv.equalizeHist(gray, dst); // 輝度分布を全域に引き伸ばしコントラスト改善`,
      `cv.imshow('canvasOutput', dst);`,
      `gray.delete(); dst.delete();`,
    ],
    useCases: [`暗所・逆光で撮った低コントラスト写真の視認性を底上げする`, `X 線・CT など医用画像で淡い構造を見やすくする`, `監視カメラの夜間映像のコントラスト改善`, `特徴抽出やマッチングの前処理として明るさ条件を揃える`, `古い退色した写真やスキャン画像の階調回復`],
  },
  "clahe": {
    overview: `画像を小さなタイルに分割し、各タイルごとにヒストグラム均一化を行う適応的手法です（Contrast Limited Adaptive Histogram Equalization）。局所的に均一化するため、明暗ムラのある画像でも場所ごとに最適なコントラストが得られます。さらに clipLimit でヒストグラムの突出を制限し、ノイズの過度な増幅（過強調）を防ぎます。霧・逆光・医用画像など、通常の均一化では破綻する場面で威力を発揮します。`,
    pipeline: [`画像をグレースケール化する`, `createCLAHE（CLAHE オブジェクト）に clipLimit と tileGridSize を設定する`, `apply で適応的に均一化する`, `equalizeHist の結果と並べて違いを比較する`],
    params: [{ arg: `clipLimit`, desc: `コントラスト制限のしきい値。タイル内でヒストグラムが突出した分を切り取り再分配する。大きくすると効果が強まるがノイズも増幅され、小さくすると穏やかになる。2.0 前後が標準。` }, { arg: `tileGridSize`, desc: `画像を分割するタイル数（一辺の数）。8×8 が標準。小さくする（タイルを大きくする）とグローバル均一化に近づき、大きくする（細かく割る）と局所適応が強まるが境界ノイズやブロックノイズが出やすくなる。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `let clahe = new cv.CLAHE(2.0, new cv.Size(8, 8)); // clipLimit=2.0, タイル8x8`,
      `let dst = new cv.Mat();`,
      `clahe.apply(gray, dst); // タイルごとに適応的均一化`,
      `cv.imshow('canvasOutput', dst);`,
      `gray.delete(); dst.delete(); clahe.delete();`,
    ],
    useCases: [`霧や逆光で部分的に白飛び・黒つぶれした写真の局所コントラスト回復`, `医用画像（眼底・MRI・X 線）で淡い病変や構造を強調する`, `水中・夜間など照明ムラの大きい映像の視認性改善`, `顕微鏡画像で背景ムラを抑えつつ細部のコントラストを上げる`, `OCR 前処理で、照明ムラのある書類の文字を均一に際立たせる`],
  },
  "histogram-comparison": {
    overview: `2枚の画像それぞれの色ヒストグラム（色や明るさの分布）を求め、その分布同士の似ている度合いを1つの数値スコアに変換する処理です。仕組みとしては、各画像をHSVなどに変換してcalcHistでビンごとの度数分布を作り、両者を正規化して同じスケールに揃えたうえでcompareHistが選んだメトリックで距離・類似度を計算します。結果は「相関なら1に近いほど似ている」「Bhattacharyyaやカイ二乗なら0に近いほど似ている」といった一つの数値として得られ、構図を無視して色味・明暗の傾向だけを比べられます。`,
    pipeline: [`両画像をHSVなどへ色変換する（色味で比べる場合）`, `calcHistで各画像のヒストグラムを計算する`, `normalizeで両ヒストグラムを同じスケールに正規化する`, `compareHistに比較メトリックを指定して類似度スコアを算出する`, `スコアをメトリックの意味に従って解釈・しきい値判定する`],
    params: [{ arg: `method`, desc: `比較メトリック。CORREL(相関)は-1〜1で1が完全一致・大きいほど類似。CHISQR(カイ二乗)とBHATTACHARYYAは0が完全一致・小さいほど類似。INTERSECT(交差)は正規化時に大きいほど類似。色味の近さ判定にはCORRELかBHATTACHARYYAが扱いやすく推奨。値の符号と向きがメトリックごとに逆になる点に注意。` }],
    code: [
      `let hist1 = new cv.Mat(), hist2 = new cv.Mat();`,
      `// 各画像のヒストグラムをcalcHistで作成（HSVのHチャンネルなどを使う）`,
      `cv.calcHist(srcVec1, [0], maskNone, hist1, [256], [0, 256]); // [0]=対象チャンネル, [256]=ビン数, [0,256]=値域`,
      `cv.calcHist(srcVec2, [0], maskNone, hist2, [256], [0, 256]);`,
      `// 両ヒストグラムを0〜1へ正規化し比較スケールを揃える`,
      `cv.normalize(hist1, hist1, 0, 1, cv.NORM_MINMAX); // 0=下限, 1=上限, NORM_MINMAX=最小最大正規化`,
      `cv.normalize(hist2, hist2, 0, 1, cv.NORM_MINMAX);`,
      `// 相関メトリックで類似度を算出（1に近いほど似ている）`,
      `let score = cv.compareHist(hist1, hist2, cv.HISTCMP_CORREL); // 第3引数=比較法。CHISQR/INTERSECT/BHATTACHARYYAに変更可`,
      `console.log('類似度スコア:', score);`,
    ],
    useCases: [`ECサイトで「色味が近い商品画像」を探す簡易な画像検索のスコアリング`, `動画でフレーム間のヒストグラム差が大きい箇所をシーンチェンジとして検出する`, `同一シーンを撮った2枚の写真の色被り・ホワイトバランスのズレを数値で確認する`, `重複・類似画像の自動グルーピング前段として色分布の近さでふるい分けする`],
  },
  "back-projection": {
    overview: `あらかじめ作っておいた「対象色のヒストグラム」を使い、入力画像の各画素が『どれくらいその色らしいか』を確率（0〜255の濃淡）として表すマップを生成する処理です。仕組みは、まずROI（対象領域）からHやH-Sのヒストグラムを作り、calcBackProjectが入力画像の各画素の色を見て、その色がヒストグラム上でどれだけ頻出かを引き当てて画素値に置き換えます。結果は対象色の部分が白く明るく、それ以外が暗いグレースケールマップになり、しきい値処理すれば特定色領域のマスクとして使え、CamShiftの入力にもなります。`,
    pipeline: [`対象物のROIを切り出しHSVに変換する`, `ROIからcalcHistで色ヒストグラムを作りnormalizeする`, `入力画像全体もHSVに変換する`, `calcBackProjectでヒストグラムを画像へ逆投影し確率マップを得る`, `しきい値や膨張で整えマスク・追跡入力として使う`],
    params: [],
    code: [
      `let hsv = new cv.Mat();`,
      `cv.cvtColor(src, hsv, cv.COLOR_RGB2HSV); // 入力をHSVへ。色相Hで色を判定するため`,
      `let hsvVec = new cv.MatVector();`,
      `hsvVec.push_back(hsv);`,
      `let dst = new cv.Mat();`,
      `// roiHistは対象色ROIから作り正規化済みのヒストグラム`,
      `cv.calcBackProject(hsvVec, [0], roiHist, dst, [0, 180], 1); // [0]=Hチャンネル, [0,180]=Hの値域, 1=スケール`,
      `// dstは対象色ほど明るい確率マップ。しきい値でマスク化できる`,
      `cv.threshold(dst, dst, 50, 255, cv.THRESH_BINARY); // 50=採用しきい値`,
    ],
    useCases: [`肌色のヒストグラムを作り、画像中の手や顔の領域だけを抽出する`, `オレンジ色のボールなど特定色物体の位置を確率マップ化してトラッキングする`, `CamShift/MeanShiftで物体を追跡する際の入力確率画像を作る前処理`, `特定の緑（グリーンバック）領域を検出して背景合成のマスクを作る`],
  },
  "affine-transform": {
    overview: `平行移動・回転・拡大縮小・せん断（斜めのゆがみ）を1枚の2x3行列でまとめて表現する幾何変換です。仕組みは、変換前の3点と変換後の3点の対応からgetAffineTransformが2x3のアフィン行列を逆算し、warpAffineがその行列に従って全画素を移動・補間して新しい画像を描き直します。アフィン変換は『直線は直線のまま、平行線は平行のまま』が保たれるのが特徴で、紙を斜め上から見たような遠近のゆがみ（透視）は表現できません。3点をドラッグすると画像が傾いたり伸びたりする様子が見えます。`,
    pipeline: [`変換元の3点と変換先の3点を決める`, `getAffineTransformで3点対応から2x3行列を求める`, `出力サイズを指定する`, `warpAffineで行列に従い全画素を変換・補間する`],
    params: [{ arg: `srcPoints (src)`, desc: `変換前の基準となる3点(x,y を3組)。元画像のどの三角形を動かすかを定義する。通常は左上・右上・左下など離れた3点を選ぶと安定。3点が一直線上にあると行列が求まらない（退化する）ので避ける。` }, { arg: `dstPoints (dst)`, desc: `変換後にsrcの3点を移したい先の座標(3組)。src3点との差が回転・拡大・せん断の量を決める。dstをsrcに近づければ変化が小さく、大きく動かすほど強く変形する。` }],
    code: [
      `// 変換元の三角形3点 (x0,y0, x1,y1, x2,y2)`,
      `let srcTri = cv.matFromArray(3, 1, cv.CV_32FC2, [0, 0, 100, 0, 0, 100]); // 3行1列・2チャンネル(xy)`,
      `// 変換先の三角形3点。srcとの差が変形量になる`,
      `let dstTri = cv.matFromArray(3, 1, cv.CV_32FC2, [10, 20, 100, 10, 20, 100]);`,
      `let M = cv.getAffineTransform(srcTri, dstTri); // 3点対応から2x3アフィン行列を算出`,
      `let dst = new cv.Mat();`,
      `let dsize = new cv.Size(src.cols, src.rows); // 出力画像サイズ`,
      `cv.warpAffine(src, dst, M, dsize, cv.INTER_LINEAR, cv.BORDER_CONSTANT, new cv.Scalar()); // INTER_LINEAR=線形補間, BORDER_CONSTANT=外側を定数で埋める`,
    ],
    useCases: [`スキャンした書類のわずかな傾きを回転＋移動で水平に補正する`, `学習データ拡張（data augmentation）で画像をランダムに回転・せん断して水増しする`, `2枚の画像の対応点を合わせて重ね合わせる（位置合わせ・レジストレーション）`, `ロゴやスタンプを指定の3点に合わせて貼り込む（簡易な貼り付け変形）`],
  },
  "perspective-transform": {
    overview: `平面を斜めから撮ったときに生じる遠近のゆがみを、4点対応で正面像に補正する射影変換です。仕組みは、ゆがんだ四角形の四隅(src)と補正後の長方形の四隅(dst)の対応からgetPerspectiveTransformが3x3の射影行列を求め、warpPerspectiveがその行列で全画素を再配置します。アフィン変換と違い平行性は保たれず『手前は大きく奥は小さい』遠近を表現・除去できるため、台形にゆがんだ書類やホワイトボードを真上から見たような長方形に直せます。四隅は補正対象の平面上にある前提で、立体物には不向きです。`,
    pipeline: [`ゆがんだ平面の四隅(src)を時計回りなどの順で指定する`, `補正後に置きたい長方形の四隅(dst)を決める`, `getPerspectiveTransformで3x3射影行列を求める`, `warpPerspectiveで画像を正面化して描き直す`],
    params: [{ arg: `srcQuad (src)`, desc: `補正対象の四隅(x,yを4組)。元画像でゆがんでいる平面の角を指定する。dstと点の並び順（左上→右上→右下→左下など）を必ず一致させること。順序がずれると画像が裏返ったり破綻する。` }, { arg: `dstQuad (dst)`, desc: `補正後に四隅を移す先の座標(4組)。通常は (0,0)(w,0)(w,h)(0,h) のように出力長方形の角を指定する。w,hで出力の縦横比が決まるため、元の書類の実寸比に合わせると自然な仕上がりになる。` }],
    code: [
      `// ゆがんだ平面の四隅。dstと同じ並び順(例: 左上,右上,右下,左下)で並べる`,
      `let srcQuad = cv.matFromArray(4, 1, cv.CV_32FC2, [0, 0, 200, 0, 200, 200, 0, 200]);`,
      `let w = 300, h = 400; // 出力する長方形のサイズ(縦横比を実寸に合わせる)`,
      `// 補正後の四隅。出力長方形の角に対応させる`,
      `let dstQuad = cv.matFromArray(4, 1, cv.CV_32FC2, [0, 0, w, 0, w, h, 0, h]);`,
      `let M = cv.getPerspectiveTransform(srcQuad, dstQuad); // 4点対応から3x3射影行列を算出`,
      `let dst = new cv.Mat();`,
      `cv.warpPerspective(src, dst, M, new cv.Size(w, h), cv.INTER_LINEAR, cv.BORDER_CONSTANT, new cv.Scalar()); // INTER_LINEAR=補間, 出力サイズ=(w,h)`,
    ],
    useCases: [`斜めから撮った契約書やレシートを真正面のスキャン画像に補正する`, `会議で撮ったホワイトボードのゆがみを正してテキストを読みやすくする`, `駐車場や交差点を斜め撮りした映像を真上から見た俯瞰（鳥瞰）図に変換する`, `看板や本のページなど平面被写体を正面化してOCRの精度を上げる前処理`],
  },
  "document-scanner": {
    overview: `スマホの書類スキャンアプリのように、写真の中の書類を自動で見つけて正面化し白黒のスキャン画像にする実用レシピです。仕組みは、Cannyでエッジを抽出→findContoursで輪郭を列挙→最大の四角形輪郭をapproxPolyDPで四隅に近似→その四隅を使ったgetPerspectiveTransform/warpPerspectiveで正面化→最後にadaptiveThresholdで局所的に二値化、という複数機能の組み合わせです。結果として、斜めに撮った台形の書類が真正面の長方形になり、紙の白と文字の黒がくっきりしたコピー機風の画像になります。背景と書類のコントラストが低いと四隅検出に失敗するため、その場合は手動で四隅を指定します。`,
    pipeline: [`グレースケール化しぼかしてからCannyでエッジを検出する`, `findContoursで輪郭を抽出し面積最大の輪郭を選ぶ`, `approxPolyDPで4頂点に近似し書類の四隅を得る`, `getPerspectiveTransform→warpPerspectiveで正面化する`, `adaptiveThresholdで局所二値化してスキャン風に仕上げる`],
    params: [{ arg: `autoDetect`, desc: `四隅を輪郭から自動検出するか。ONで最大の四角形輪郭を書類とみなして自動補正する。背景と書類の境界が不鮮明だと誤検出するため、失敗時はOFFにして手動で4点を指定するフォールバックが必要。` }, { arg: `binarize`, desc: `補正後に適応的二値化を掛けるか。ONでコピー機のような白黒くっきり画像になり文字が読みやすくOCR向き。OFFなら元の色味を残したカラー補正画像のまま。影や照明ムラがある書類ほどONが有効。` }],
    code: [
      `let gray = new cv.Mat(), edges = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `cv.GaussianBlur(gray, gray, new cv.Size(5, 5), 0); // ノイズを抑えエッジ検出を安定させる`,
      `cv.Canny(gray, edges, 50, 150); // 50=下限しきい値, 150=上限しきい値`,
      `let contours = new cv.MatVector(), hierarchy = new cv.Mat();`,
      `cv.findContours(edges, contours, hierarchy, cv.RETR_EXTERNAL, cv.CHAIN_APPROX_SIMPLE); // 外側輪郭のみ抽出`,
      `// 面積最大の輪郭を選び4頂点に近似する`,
      `let peri = cv.arcLength(maxContour, true); // true=閉曲線として周囲長を計算`,
      `let approx = new cv.Mat();`,
      `cv.approxPolyDP(maxContour, approx, 0.02 * peri, true); // 0.02*peri=許容誤差。小さいほど頂点が増える`,
      `// approxの4頂点をsrcQuadとして射影変換し正面化`,
      `let M = cv.getPerspectiveTransform(srcQuad, dstQuad);`,
      `cv.warpPerspective(src, warped, M, new cv.Size(w, h));`,
      `// 仕上げに局所二値化でスキャン風に`,
      `cv.cvtColor(warped, warped, cv.COLOR_RGBA2GRAY);`,
      `cv.adaptiveThreshold(warped, scan, 255, cv.ADAPTIVE_THRESH_GAUSSIAN_C, cv.THRESH_BINARY, 11, 2); // 11=ブロックサイズ, 2=定数C`,
    ],
    useCases: [`スマホで撮った請求書やレシートを真正面の白黒画像にして経費精算アプリに取り込む`, `ホワイトボードや配布資料を撮影してPDF化・保存する自作スキャナアプリ`, `古い書類や名刺を一括で正面化・二値化してOCRに渡し文字をデータ化する`, `斜め撮りした本のページを補正して読みやすい電子書籍ページに変換する`],
  },
  "image-translation": {
    overview: `画像をx方向・y方向にそのままスライドさせる平行移動で、アフィン変換の最も単純な特例です。仕組みは、平行移動だけを表す2x3行列 [[1,0,tx],[0,1,ty]] を作り、warpAffineがこの行列で全画素を (x+tx, y+ty) の位置へ移すだけです。回転や拡大は含まれないため形は一切変わらず、ただ位置がずれます。画面からはみ出した部分は切り落とされ、移動して空いた領域は境界補間（既定では黒や端の色）で埋められます。スライダでtx/tyを動かすと画像が左右・上下に動く様子が直感的に確認できます。`,
    pipeline: [`平行移動量 tx, ty を決める`, `2x3移動行列 [[1,0,tx],[0,1,ty]] を作成する`, `warpAffineで全画素をtx,tyだけずらす`, `空いた領域は境界補間で埋まり、はみ出しは切れる`],
    params: [{ arg: `tx`, desc: `横方向の移動量(ピクセル)。正で右、負で左へ移動する。絶対値が大きいほど大きくずれ、画像幅を超えると画面外に出て見えなくなる。` }, { arg: `ty`, desc: `縦方向の移動量(ピクセル)。正で下、負で上へ移動する。txと組み合わせて斜め移動も表現できる。空いた領域の埋め方はwarpAffineのborderModeで変えられる。` }],
    code: [
      `// 平行移動だけを表す2x3行列。3列目のtx,tyが移動量`,
      `let M = cv.matFromArray(2, 3, cv.CV_64F, [1, 0, 50, 0, 1, 30]); // 50=tx(右へ50px), 30=ty(下へ30px)`,
      `let dst = new cv.Mat();`,
      `let dsize = new cv.Size(src.cols, src.rows); // 出力サイズは元と同じ`,
      `cv.warpAffine(src, dst, M, dsize, cv.INTER_LINEAR, cv.BORDER_CONSTANT, new cv.Scalar(0, 0, 0, 255)); // BORDER_CONSTANT+黒で空き領域を埋める`,
    ],
    useCases: [`ずれて重なった2枚の画像の位置を合わせる（位置合わせの基本操作）`, `ゲームやアニメでスプライト（キャラ画像）を指定座標へ動かす`, `画像の周囲に余白（パディング）を作るために中身をずらす`, `手ブレ補正で各フレームを基準位置にシフトして揃える前処理`],
  },
  "watershed": {
    overview: `地形の分水嶺（水が流れ落ちる境界）に例えて、マーカー（種）を起点に領域を水で満たすように広げ、別々の領域がぶつかった線で物体を分離するセグメンテーション手法です。定番パイプラインは、二値画像にdistanceTransformを掛けて物体中心ほど高い『標高マップ』を作り、しきい値で確実な前景を取り、connectedComponentsでマーカー番号を振り、未知領域を0にしてwatershedを実行します。watershedは画像の勾配を谷、マーカーを水源として領域を成長させ、領域同士の境界に-1（仕切り線）を引きます。結果として、くっついて1つに見えたコインや細胞が個々に色分け・分離されます。マーカー設計が肝で、雑だと過分割しやすいのが難点です。`,
    pipeline: [`二値化しopeningでノイズ除去、dilateで確実な背景を得る`, `distanceTransform＋しきい値で確実な前景を抽出する`, `前景をconnectedComponentsでラベル付けしマーカーを作る`, `未知領域を0に設定しwatershedを実行する`, `境界(-1)を着色し物体ごとに分離表示する`],
    params: [],
    code: [
      `// 1. 二値化しモルフォロジでノイズ除去`,
      `cv.threshold(gray, bin, 0, 255, cv.THRESH_BINARY_INV + cv.THRESH_OTSU); // 大津法で自動しきい値`,
      `cv.morphologyEx(bin, opening, cv.MORPH_OPEN, kernel, anchor, 2); // 2=反復回数。小ノイズ除去`,
      `// 2. 距離変換で物体中心ほど高い値にし、しきい値で確実な前景を得る`,
      `cv.distanceTransform(opening, dist, cv.DIST_L2, 5); // DIST_L2=ユークリッド距離, 5=マスクサイズ`,
      `cv.threshold(dist, fg, 0.7 * maxVal, 255, cv.THRESH_BINARY); // 0.7*maxVal=前景採用の比率しきい値`,
      `// 3. 確実な前景をラベル付けしマーカーを作成`,
      `let markers = new cv.Mat();`,
      `cv.connectedComponents(fg8u, markers); // 各前景に1,2,3...の番号を付与`,
      `cv.add(markers, ones, markers); // 背景を1にずらし、未知領域用に0を空ける`,
      `// markersの未知領域(背景でも前景でもない部分)を0に設定しておく`,
      `// 4. watershed実行。境界画素は-1になる`,
      `cv.cvtColor(src, rgb, cv.COLOR_RGBA2RGB); // watershedは3chRGBを要求`,
      `cv.watershed(rgb, markers); // markersが更新され、-1が物体間の仕切り線になる`,
    ],
    useCases: [`顕微鏡画像で接触して固まった細胞を1つずつに分離してカウントする`, `重なって置かれたコインや錠剤を個別に分けて個数を数える`, `工業検査で隣接した部品の塊を分離して各部品の寸法を測る`, `前景物体が密集した画像で、領域分割して個体ごとにラベルを付ける`],
  },
  "grabcut": {
    overview: `前景を囲む矩形を1つ指定するだけで、背景と前景を自動で切り分ける対話的セグメンテーション手法です。仕組みは、矩形の外を『確実な背景』、内を『たぶん前景』として初期化し、前景/背景それぞれの色をガウス混合モデル(GMM)で学習しながら、隣接画素のつながりを考えるグラフカット最適化を反復的に解きます。反復のたびにモデルが更新され、前景マスクが洗練されていきます。結果は、各画素が前景/背景に分類されたマスクで、これを使えば人物や商品を背景から滑らかに切り抜けます。計算コストが高くブラウザでは大画像が遅いため、矩形指定から始めブラシで微修正する流れが一般的です。`,
    pipeline: [`前景を囲む矩形rectを指定する`, `mask・bgdModel・fgdModelの空Matを用意する`, `GC_INIT_WITH_RECTモードでgrabCutを反復実行する`, `出力maskから前景画素(GC_FGD/GC_PR_FGD)を抜き出す`, `マスクを元画像に適用して背景を除去する`],
    params: [{ arg: `rect`, desc: `前景を囲む矩形(x,y,幅,高さ)。この矩形の外は確実な背景として扱われ、内側から前景を探す。対象が必ず矩形内に収まり、かつ余白を含みすぎないよう密着気味に囲むほど精度が上がる。` }, { arg: `iterations (iterCount)`, desc: `最適化の反復回数。多いほどGMMとグラフカットが収束し前景境界が安定するが、計算時間が比例して増える。既定5前後で十分なことが多く、ブラウザでは増やしすぎると重くなる。1〜10が目安。` }],
    code: [
      `let rgb = new cv.Mat();`,
      `cv.cvtColor(src, rgb, cv.COLOR_RGBA2RGB); // grabCutは3chRGBを要求`,
      `let mask = new cv.Mat(); // 出力マスク(各画素に前景/背景ラベルが入る)`,
      `let bgdModel = new cv.Mat(); // 背景GMM用の内部モデル(空でよい)`,
      `let fgdModel = new cv.Mat(); // 前景GMM用の内部モデル(空でよい)`,
      `let rect = new cv.Rect(20, 20, 200, 200); // 前景を囲む矩形(x,y,w,h)`,
      `cv.grabCut(rgb, mask, rect, bgdModel, fgdModel, 5, cv.GC_INIT_WITH_RECT); // 5=反復回数, GC_INIT_WITH_RECT=矩形で初期化`,
      `// maskの値が GC_FGD(1) か GC_PR_FGD(3) の画素を前景として取り出す`,
      `for (let i = 0; i < mask.rows * mask.cols; i++) {`,
      `  mask.data[i] = (mask.data[i] === cv.GC_FGD || mask.data[i] === cv.GC_PR_FGD) ? 255 : 0;`,
      `}`,
    ],
    useCases: [`ポートレート写真から人物だけを切り抜いて背景を差し替える`, `ECの商品写真を白抜き（背景を真っ白）にして商品画像を統一する`, `プロフィール画像の背景をぼかす・透過する加工の前処理`, `アノテーション作業で対象物のマスクを半自動生成して手間を減らす`],
  },
  "connected-components": {
    overview: `二値画像（白黒）の中で、つながった白い画素のかたまり（連結成分）ごとに固有のラベル番号を振り、いくつのかたまりがあるかや各かたまりの統計を求める処理です。仕組みは、connectedComponentsが画像を走査し、4近傍または8近傍でつながっている白画素に同じ番号を割り当て、孤立したかたまりごとに連番のラベルを付けます。WithStatsを使えば各成分の面積・外接矩形・重心まで一度に得られます。結果はラベル画像（同じかたまりは同じ値）で、これを色分け表示すれば物体ごとに色が変わり、個数カウントやブロブ解析に使えます。二値化の品質に結果が左右されます。`,
    pipeline: [`対象を白、背景を黒に二値化する`, `connectivity(4 or 8)を指定してconnectedComponentsを実行する`, `返り値の成分数からラベル数を把握する`, `WithStatsなら各成分の面積・外接矩形・重心を取得する`, `ラベルごとに色を割り当てて可視化する`],
    params: [{ arg: `connectivity`, desc: `画素をつながりとみなす近傍の数。4は上下左右のみ、8は斜めも含めてつながりと判定する。8の方が斜めに接触した物体を1つにまとめやすく、4は厳しめで物体が分かれやすい。細い斜め線をつなげたいなら8、隣接物を分けたいなら4が向く。` }],
    code: [
      `let bin = new cv.Mat();`,
      `cv.threshold(gray, bin, 127, 255, cv.THRESH_BINARY); // 対象を白(255)・背景を黒(0)に二値化`,
      `let labels = new cv.Mat();`,
      `// 連結成分にラベル付け。返り値nはラベル数(背景0を含む)`,
      `let n = cv.connectedComponents(bin, labels, 8, cv.CV_32S); // 8=8近傍, CV_32S=ラベルの型`,
      `console.log('物体数:', n - 1); // 背景(0)を除いた数が物体数`,
      `// 統計付きが必要なら以下。stats/centroidsに面積・外接矩形・重心が入る`,
      `let stats = new cv.Mat(), centroids = new cv.Mat();`,
      `let n2 = cv.connectedComponentsWithStats(bin, labels, stats, centroids, 8, cv.CV_32S);`,
      `// stats.intAt(i, cv.CC_STAT_AREA) で i番ラベルの面積を取得できる`,
    ],
    useCases: [`二値化した画像で米粒や細菌のコロニーなど物体の個数を自動カウントする`, `各かたまりの面積を測り、小さすぎるノイズ成分を除去する（面積フィルタ）`, `ナンバープレートや文字領域を成分ごとに切り出してOCRに渡す`, `ブロブ解析で各領域の重心・外接矩形を求めて位置や大きさを統計化する`],
  },
  "flood-fill": {
    overview: `ペイントソフトのバケツツールと同じく、クリックした開始点（種）から、色が似ている範囲を連結している限りたどって一色に塗りつぶす処理です。仕組みは、floodFillが種画素の色を基準に、隣接画素の色差がloDiff（下限許容差）〜upDiff（上限許容差）の範囲内なら同じ領域とみなして再帰的に広げ、その範囲を新しい色で塗ります。マスクを渡せば塗った領域だけを記録でき、元画像を変えずに領域選択にも使えます。結果は、許容差が小さいとほぼ同色の狭い範囲だけ、大きいと広く塗り広がります。グラデーション領域では途中で色差が許容を超えて漏れることがあります。`,
    pipeline: [`塗りつぶしの開始点(seedPoint)をクリックで決める`, `色の許容差loDiff/upDiffを設定する`, `新しい塗り色と連結数(4/8)を指定する`, `floodFillで類似色の連結領域を塗る／マスクに記録する`],
    params: [{ arg: `seed (seedPoint)`, desc: `塗りつぶしを開始する画素座標(x,y)。この点の色が基準色になる。背景の真ん中など、塗りたい領域の代表的な場所を選ぶと均一に広がる。` }, { arg: `tolerance (loDiff/upDiff)`, desc: `種の色からどれだけ色がずれても同領域とみなすかの許容差。loDiff(基準より暗い側)・upDiff(明るい側)に同じ値を入れることが多い。小さいとほぼ同色の狭い範囲だけ、大きいと広く塗れるが別の領域へ漏れやすい。グラデ背景は値を上げると一気に塗れる反面、はみ出しに注意。0〜100程度で調整。` }],
    code: [
      `let img = src.clone(); // floodFillは入力画像を直接書き換えるためコピーして使う`,
      `// マスクは画像より縦横+2px必要。塗った領域がここに記録される`,
      `let mask = new cv.Mat.zeros(img.rows + 2, img.cols + 2, cv.CV_8UC1);`,
      `let rect = new cv.Rect(); // 塗られた領域の外接矩形が返る`,
      `let seed = new cv.Point(50, 50); // 塗り開始点(クリック座標)`,
      `let newColor = new cv.Scalar(0, 255, 0, 255); // 塗りつぶす色(緑)`,
      `let lo = new cv.Scalar(20, 20, 20, 20); // loDiff=基準より暗い側の許容差`,
      `let up = new cv.Scalar(20, 20, 20, 20); // upDiff=明るい側の許容差`,
      `cv.floodFill(img, mask, seed, newColor, rect, lo, up, 8); // 8=8近傍で連結判定`,
    ],
    useCases: [`画像エディタのバケツツールで背景をワンクリックで別色に塗り替える`, `ほぼ単色の背景を選択してマスク化し、被写体だけを残す簡易切り抜き`, `図形の内部（穴）を塗って穴埋め・穴検出をする`, `塗り絵アプリで線で囲まれた領域をクリックで着色する`],
  },
  "kmeans-color-quantization": {
    overview: `画像中の全画素をRGB色空間上の点とみなし、k-meansクラスタリングで似た色をK個のグループにまとめ、各画素をそのグループの代表色に置き換えて減色する処理です。仕組みは、画像を「画素数×3チャンネル」の浮動小数点データに整形し、kmeansがランダム初期化した中心をもとに『各点を最近接の中心に割り当て→中心を割り当て点の平均に更新』を収束まで反復します。最終的な各クラスタ中心がK色のパレットになり、全画素がそのいずれかの色に揃います。結果はポスターのような少数色のフラットな画像になり、Kを小さくするほど色数が減って大胆に、大きくするほど元画像に近くなります。`,
    pipeline: [`画像を画素数×3のCV_32Fデータにreshape/convertする`, `終了条件criteriaとクラスタ数Kを設定する`, `kmeansで全画素をK色に分類しlabelsとcentersを得る`, `各画素を所属クラスタの代表色(centers)に置き換える`, `元の画像形状に戻して減色画像を表示する`],
    params: [{ arg: `k (K)`, desc: `減色後の色数（クラスタ数）。小さい(例:2〜4)ほど色が大胆に減りポスター風になるが情報は失われる。大きい(例:16〜32)ほど元画像に近い自然な減色になる。代表色パレットを抽出する用途ならK=5〜8程度が見やすい。` }],
    code: [
      `// 画像を「全画素数×3チャンネル」の1列データに整形`,
      `let samples = src.reshape(1, src.rows * src.cols); // 1=チャンネルを展開, 行数=総画素数`,
      `samples.convertTo(samples, cv.CV_32F); // kmeansは32bit浮動小数点を要求`,
      `let labels = new cv.Mat(); // 各画素の所属クラスタ番号が入る`,
      `let centers = new cv.Mat(); // K個の代表色(クラスタ中心)が入る`,
      `// 終了条件: 最大反復10回 または 中心の移動が1.0未満で停止`,
      `let criteria = new cv.TermCriteria(cv.TermCriteria_EPS + cv.TermCriteria_MAX_ITER, 10, 1.0);`,
      `let K = 8; // 減色後の色数`,
      `cv.kmeans(samples, K, labels, criteria, 3, cv.KMEANS_PP_CENTERS, centers); // 3=試行回数, KMEANS_PP_CENTERS=賢い初期化`,
      `// labelsとcentersを使い各画素を代表色に置換し、元の形状にreshapeで戻す`,
    ],
    useCases: [`写真をポスター風・イラスト風のフラットな少数色画像に加工する`, `画像から代表的な数色を抽出してカラーパレット（テーマカラー）を自動生成する`, `色数を減らしてGIFやインデックスカラー画像として圧縮する仕組みを学ぶ`, `色によるざっくりした領域分割（空・草・建物などの大まかな色分け）`],
  },
  "harris-corner": {
    overview: `画像のコーナー（縦横どちらの方向にずらしても輝度が大きく変化する角）を、輝度勾配の統計から検出する古典的手法です。仕組みは、各画素まわりの小窓で輝度の勾配(Ix,Iy)を集め、その構造行列の2つの固有値の大きさをk係数を含む応答関数 R = det(M) - k·trace(M)^2 で評価します。Rが大きい点は両方向の勾配が強い＝コーナー、片方向だけならエッジ、どちらも弱ければ平坦と判別できます。結果は各画素のコーナーらしさを表す応答画像で、正規化してしきい値を超えた点を描画するとチェッカーの交点や物体の角に点が打たれます。スケール不変ではないため、拡大縮小には弱いです。`,
    pipeline: [`入力をグレースケールに変換する`, `cornerHarrisでblockSize近傍の勾配からコーナー応答を計算する`, `応答をnormalizeして0〜255に整える`, `しきい値を超えた画素をコーナーとして点描画する`],
    params: [{ arg: `blockSize`, desc: `コーナー判定に使う近傍窓の一辺サイズ。大きいほど広い範囲の勾配を平均するため、細かい角を逃しやすいがノイズに強く太い角に反応する。小さいと細かいコーナーまで拾うがノイズも増える。2〜5程度が一般的。` }, { arg: `k (Harris係数)`, desc: `応答関数 R=det(M)-k*trace(M)^2 の感度係数。小さい(0.04付近)とコーナー判定が緩く点が多く出やすい。大きい(0.1付近)と判定が厳しくなり真の角だけが残る。一般に0.04〜0.06が推奨。` }, { arg: `ksize (Sobel窓)`, desc: `内部で勾配を計算するSobelの窓サイズ(コード内の第4引数3)。大きいほど勾配がなめらかになり太いエッジに反応。通常は3で十分。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `gray.convertTo(gray, cv.CV_32F); // cornerHarrisは浮動小数点入力を要求`,
      `let dst = new cv.Mat();`,
      `cv.cornerHarris(gray, dst, 2, 3, 0.04); // 2=blockSize(近傍), 3=Sobel窓, 0.04=Harris係数k`,
      `// 応答dstを0〜255に正規化してしきい値判定しやすくする`,
      `cv.normalize(dst, dst, 0, 255, cv.NORM_MINMAX);`,
      `// dstの値が一定しきい値を超えた画素を元画像にコーナー点として描画する`,
    ],
    useCases: [`チェスボードやキャリブレーション用パターンの格子交点を検出する`, `コーナーという特徴点の概念を学ぶための教材・可視化`, `2枚の画像で共通の角を見つけて位置合わせ（レジストレーション）の起点にする`, `建物や箱など角の多い人工物の輪郭の角を検出する前処理`],
  },
  "shi-tomasi-corner": {
    overview: `Harrisを改良し、『追跡（トラッキング）に向く良質なコーナー』を品質順に上位N個だけ抽出する手法で、goodFeaturesToTrackとして実装されています。仕組みは、各画素の構造行列の2つの固有値のうち小さい方の値をコーナーの強さ（品質）とし、最も強い点の品質にqualityLevelを掛けた値を足切りラインにして弱い点を捨て、さらにminDistanceより近い点同士は強い方だけ残すことで点が密集しないようにします。結果は、画像全体に程よく散らばった追跡しやすいコーナー点群で、Lucas-Kanade法のオプティカルフローの追跡開始点として広く使われます。Harris同様スケール不変ではありません。`,
    pipeline: [`入力をグレースケールに変換する`, `goodFeaturesToTrackにmaxCorners/qualityLevel/minDistanceを与える`, `固有値で品質評価し弱い点・近すぎる点を間引く`, `残った上位コーナーをcornersとして取得・描画する`],
    params: [{ arg: `maxCorners`, desc: `抽出するコーナーの最大個数。多いほど多くの追跡点が得られるが処理は重くなる。オプティカルフロー用なら100前後、密に取りたいなら数百。0や負で無制限。` }, { arg: `qualityLevel`, desc: `最良コーナーの品質を1.0としたときの足切り比率。小さい(0.001)と弱いコーナーまで拾い点が増える。大きい(0.1)と非常に強い角だけが残る。0.01付近が標準で、ノイズが多い画像では上げると安定する。` }, { arg: `minDistance`, desc: `検出された点同士の最小間隔(ピクセル)。大きいほど点が画像全体に均等にばらけ密集を防ぐが総数は減る。小さいと近接した点も許容される。10〜30程度が扱いやすい。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);`,
      `let corners = new cv.Mat(); // 検出されたコーナー座標(x,y)が入る`,
      `cv.goodFeaturesToTrack(gray, corners, 100, 0.01, 10); // 100=最大点数, 0.01=品質レベル, 10=最小距離`,
      `// cornersをループして各点を元画像に円で描画する`,
      `for (let i = 0; i < corners.rows; i++) {`,
      `  let x = corners.data32F[i * 2], y = corners.data32F[i * 2 + 1];`,
      `  cv.circle(src, new cv.Point(x, y), 4, new cv.Scalar(0, 255, 0, 255), -1); // 検出点を緑丸で表示`,
      `}`,
    ],
    useCases: [`Lucas-Kanadeオプティカルフローで動画の物体を追跡する開始点を選ぶ`, `手ブレ補正や動画スタビライズで各フレームの対応点を取る`, `パノラマ合成や画像つなぎ合わせで対応付けに使う良質な特徴点を集める`, `AR(拡張現実)で平面やマーカーの追跡点を確保する前処理`],
  },
  "orb-features": {
    overview: `ORB (Oriented FAST and Rotated BRIEF) は、FAST コーナー検出器で特徴点を見つけ、各点に向き(オリエンテーション)を割り当てたうえで、回転補正した BRIEF バイナリ記述子を計算する高速な特徴抽出器です。記述子が0/1のビット列なので、ハミング距離で高速にマッチングでき、回転やある程度のスケール変化に頑健です。結果は画像上に多数の小円(特徴点)として重畳表示され、各円は周囲の局所パターンを表す256bitの記述子を持ちます。SIFT/SURFと違い特許フリーで、リアルタイム用途に向きます。`,
    pipeline: [`入力画像をグレースケールに変換する`, `cv.ORB を生成し最大特徴点数(nfeatures)を設定する`, `detectAndCompute で特徴点(keypoints)と記述子(descriptors)を同時に計算する`, `drawKeypoints で特徴点を画像上に描画して可視化する`],
    params: [{ arg: `nfeatures`, desc: `保持する特徴点の最大数。スコアの高い順に上位 nfeatures 点だけを残す。増やすとマッチングの手がかりが増え精度が上がるが処理が重くなる。減らすと高速だが対応点不足で位置合わせが失敗しやすい。一般的なシーンで 500〜1000 が目安。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY); // ORBはグレースケール入力が前提`,
      `let orb = new cv.ORB(500); // nfeatures=500: 抽出する最大特徴点数`,
      `let kp = new cv.KeyPointVector(); // 検出された特徴点(座標/サイズ/角度)の格納先`,
      `let des = new cv.Mat(); // 各特徴点の32byte(256bit)バイナリ記述子`,
      `orb.detectAndCompute(gray, new cv.Mat(), kp, des); // 第2引数はマスク(空=画像全体を対象)`,
      `let out = src.clone();`,
      `cv.drawKeypoints(src, kp, out); // 特徴点を円で描画して可視化`,
      `kp.delete(); des.delete(); gray.delete(); orb.delete();`,
    ],
    useCases: [`スマホアプリでのリアルタイム物体認識(ロゴ/パッケージの検出)`, `連続撮影した写真をつなぐパノラマ合成の前段としての特徴抽出`, `ドローンやロボットの視覚オドメトリ(自己位置推定)で連続フレーム間の対応付け`, `AR マーカーレストラッキングで対象平面の特徴点を高速に取得`],
  },
  "feature-matching": {
    overview: `2枚の画像からそれぞれ抽出した特徴記述子を総当たり(Brute-Force)で比較し、最も似た記述子同士を対応点として結びつける処理です。ORB のバイナリ記述子にはハミング距離(NORM_HAMMING)を使い、距離が小さいほど似ています。crossCheck を有効にすると「互いに最近傍である」ペアだけを残し誤対応を減らせます。結果は2枚を並べ、対応点を線で結んだ図として表示され、同一物体・同一シーンが画像間でどう対応するかが一目で分かります。`,
    pipeline: [`2枚の画像それぞれで特徴点と記述子を抽出する(例: ORB)`, `cv.BFMatcher を生成しノルム種別と crossCheck を指定する`, `match で記述子集合同士を対応付け DMatch のベクトルを得る`, `距離(distance)でソートして上位 maxMatches 件を選ぶ`, `drawMatches で2枚をつなぐ対応線を描画する`],
    params: [{ arg: `crossCheck`, desc: `相互最近傍チェック。true にすると画像1→画像2 と画像2→画像1 の双方で最近傍になるペアだけを採用し、誤対応(outlier)を大幅に減らす。false にすると一方向の最近傍を全て返すため対応数は増えるがノイズも増える。安定したマッチが欲しいなら true 推奨。` }, { arg: `maxMatches`, desc: `描画する対応線の本数(表示上の制御で OpenCV API の引数ではない)。距離の小さい(=似ている)順に上位何件を結ぶか。多くすると全体像が分かるが線が密集して見づらく、少なくすると確度の高い対応だけを確認できる。30 前後が見やすい。` }],
    code: [
      `let bf = new cv.BFMatcher(cv.NORM_HAMMING, true); // ORBはハミング距離, crossCheck=true で相互最近傍のみ`,
      `let matches = new cv.DMatchVector(); // 対応ペア(queryIdx/trainIdx/distance)の格納先`,
      `bf.match(des1, des2, matches); // des1=画像1, des2=画像2 の記述子を総当たり比較`,
      `let arr = [];`,
      `for (let i = 0; i < matches.size(); i++) arr.push(matches.get(i));`,
      `arr.sort((a, b) => a.distance - b.distance); // 距離が小さい=似ている順に並べる`,
      `let good = new cv.DMatchVector();`,
      `for (let i = 0; i < Math.min(30, arr.length); i++) good.push_back(arr[i]); // 上位30件(maxMatches)だけ採用`,
      `let out = new cv.Mat();`,
      `cv.drawMatches(img1, kp1, img2, kp2, good, out); // 2枚を並べ対応点を線で結ぶ`,
    ],
    useCases: [`撮影アングルの違う2枚から同一商品を照合する画像検索・類似画像判定`, `パノラマ合成や画像スティッチングで重なり領域の対応点を求める前処理`, `印刷物やポスターをスキャンして登録画像と照合するマーカーレス認識`, `前後フレーム間の対応からカメラの動きを推定するビジュアル SLAM の入口`],
  },
  "homography-estimation": {
    overview: `マッチングで得た対応点群から、ある平面が別視点でどう写像されるかを表す3x3のホモグラフィ行列 H を推定します。対応点には誤対応が混じるため RANSAC を使い、多数派の整合する対応(inlier)だけで H を求め、外れ値(outlier)を自動排除します。得た H を warpPerspective に渡すと一方の画像をもう一方の視点に重ね合わせられ、平面物体の位置合わせや AR 的な貼り付け、パノラマ合成が実現できます。結果は変形された画像や重畳表示、検出枠として現れます。`,
    pipeline: [`特徴点マッチングで対応点の座標ペアを取得する`, `対応点を src/dst の2チャンネル Mat に整形する`, `cv.findHomography で RANSAC を使い行列 H と inlier マスクを推定する`, `cv.warpPerspective で H を使い画像を相手の視点へ射影変換する`],
    params: [{ arg: `ransacReprojThreshold`, desc: `対応点が H で再投影されたときの許容誤差(ピクセル)。これ以内なら inlier、超えると outlier とみなす。小さくすると厳密になり外れ値に強いが、ノイズで正しい対応も捨てて inlier 不足になりやすい。大きくすると緩くなり外れ値を取り込んで精度が落ちる。3〜5 が一般的な目安。` }],
    code: [
      `let srcPts = cv.matFromArray(n, 1, cv.CV_32FC2, srcCoords); // 画像1側の対応点座標(x,y)を2chで`,
      `let dstPts = cv.matFromArray(n, 1, cv.CV_32FC2, dstCoords); // 画像2側の対応点座標`,
      `let mask = new cv.Mat(); // inlier/outlier を示すマスク(出力)`,
      `let H = cv.findHomography(srcPts, dstPts, cv.RANSAC, 5, mask); // RANSAC, 再投影誤差5px以内をinlierと判定`,
      `let warped = new cv.Mat();`,
      `let dsize = new cv.Size(img2.cols, img2.rows);`,
      `cv.warpPerspective(img1, warped, H, dsize); // Hで画像1を画像2の視点へ射影変換`,
      `srcPts.delete(); dstPts.delete(); mask.delete(); H.delete();`,
    ],
    useCases: [`看板やポスターなど平面対象に別画像を貼り付ける AR コンテンツ表示`, `傾いて撮った書類を正面に補正する書類スキャン・台形補正`, `複数枚の風景写真を1枚に合成するパノラマスティッチング`, `工場の検査で基準画像と撮影画像を位置合わせしてからの差分検査`],
  },
  "sift-features": {
    overview: `SIFT (Scale-Invariant Feature Transform) は、画像を複数スケールでぼかした DoG (Difference of Gaussian) 空間から極値点を検出し、各点に向きと128次元の勾配ヒストグラム記述子を与える高精度な特徴抽出器です。スケール変化・回転・照明変化に強く、ORB より精度が高い反面低速です。本アプリで使う techstark の opencv.js ビルドには cv.SIFT のネイティブバインディングが含まれないため、実際には同等のスケール不変なバイナリ特徴 AKAZE で代替実行しています。以下のコード例は教育目的でネイティブ OpenCV の標準的な SIFT の使い方を示します。結果は画像上にスケールと向きを持つ特徴点として重畳表示されます。`,
    pipeline: [`入力画像をグレースケールに変換する`, `cv.SIFT を生成する(代替実装では cv.AKAZE)`, `detectAndCompute で特徴点と記述子(SIFTは128次元float)を計算する`, `drawKeypoints で向き・スケール付きの特徴点を描画する`],
    params: [{ arg: `nfeatures`, desc: `保持する特徴点の最大数。0 を指定すると上限なしで検出できる限り全て返す。値を大きくすると多くの特徴を残せるが計算とマッチングが重くなる。0(無制限)はテクスチャの豊富な画像では数千点になり得るので、用途に応じ数百〜千程度に絞ると扱いやすい。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY); // SIFTもグレースケール入力`,
      `let sift = new cv.SIFT(0); // nfeatures=0: 上限なしで検出(本アプリでは cv.AKAZE で代替)`,
      `let kp = new cv.KeyPointVector(); // スケールと向きを持つ特徴点`,
      `let des = new cv.Mat(); // 各点128次元のfloat記述子(AKAZEはバイナリ記述子)`,
      `sift.detectAndCompute(gray, new cv.Mat(), kp, des); // 第2引数のマスクは空=全体`,
      `let out = src.clone();`,
      `cv.drawKeypoints(src, kp, out, new cv.Scalar(0,255,0,255), cv.DRAW_MATCHES_FLAGS_DRAW_RICH_KEYPOINTS); // 向き/スケール付きで描画`,
      `kp.delete(); des.delete(); gray.delete(); sift.delete();`,
    ],
    useCases: [`拡大縮小や回転が混在する写真からの高精度な物体・ランドマーク認識`, `撮影距離が大きく異なる画像同士のロバストなマッチング(地図・航空写真)`, `美術品や紙幣など細部のテクスチャを照合する真贋判定・登録照合`, `ORB では対応点が不足する難しいシーンでの精度重視の位置合わせ`],
  },
  "face-detection-haar": {
    overview: `学習済みの Haar Cascade 分類器を使い、明暗パターン(Haar 特徴)とそれを段階的にふるい分ける AdaBoost カスケードによって正面顔を矩形検出します。detectMultiScale は画像を少しずつ縮小しながら各スケールで検出窓を走査し、顔らしい領域を高速に見つけます。OpenCV の定番デモで、結果は検出した顔を囲む矩形として表示されます。なお本アプリでは cv.CascadeClassifier 自体は利用可能ですが、XML カスケードの非同期ロードと実画像入力が前提となるため、このデモは未提供です。以下のコード例は教育目的でネイティブ OpenCV の標準的な使い方を示します。`,
    pipeline: [`カスケード XML(haarcascade_frontalface_default.xml)を仮想FSにロードする`, `入力画像をグレースケールに変換する`, `detectMultiScale で scaleFactor/minNeighbors を指定し顔矩形を検出する`, `検出された各矩形を rectangle で描画する`],
    params: [{ arg: `scaleFactor`, desc: `各走査段階で画像を縮小する比率。1.1 なら毎段10%ずつ縮小して様々な大きさの顔を探す。1.05 など1に近づけると刻みが細かく検出漏れが減るが遅くなる。1.3〜1.5 と大きくすると高速だが中間サイズの顔を取りこぼす。1.1 が定番。` }, { arg: `minNeighbors`, desc: `1つの顔として確定するのに必要な近接検出数。大きくすると重なって検出された候補が多い領域だけ採用するので誤検出が減るが、本物の顔も落ちやすい。小さくすると検出は増えるが誤検出も増える。3〜6 が実用的。` }],
    code: [
      `let classifier = new cv.CascadeClassifier();`,
      `classifier.load('haarcascade_frontalface_default.xml'); // 学習済みカスケードを読み込む(事前にFSへ配置)`,
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY); // 検出はグレースケールで行う`,
      `let faces = new cv.RectVector(); // 検出された顔矩形の格納先`,
      `classifier.detectMultiScale(gray, faces, 1.1, 3, 0); // scaleFactor=1.1, minNeighbors=3, flags=0`,
      `for (let i = 0; i < faces.size(); i++) {`,
      `  let r = faces.get(i);`,
      `  let p1 = new cv.Point(r.x, r.y), p2 = new cv.Point(r.x + r.width, r.y + r.height);`,
      `  cv.rectangle(src, p1, p2, new cv.Scalar(255, 0, 0, 255), 2); // 顔を矩形で囲む`,
      `}`,
    ],
    useCases: [`SNS 投稿写真で顔へ自動モザイク・ぼかしをかけるプライバシー保護`, `イベント会場のカメラ映像から来場者の顔数をカウントする人数集計`, `顔の位置に帽子やメガネを重ねる AR 的な装飾フィルタの土台`, `オートフォーカスや自動トリミングで顔を基準に構図を決める処理`],
  },
  "eye-detection-haar": {
    overview: `顔検出で得た各顔領域(ROI)の内側に対して、目用の Haar Cascade(haarcascade_eye.xml)を改めて適用する入れ子(ネスト)検出の例です。画像全体ではなく顔の範囲だけを走査することで、無関係な背景での誤検出を抑えつつ目の位置を絞り込みます。結果は各顔の中に検出された目を囲む小さな矩形として表示されます。本アプリでは cv.CascadeClassifier は利用可能ですが、XML の非同期ロードと実画像入力が必要なため未提供で、以下は教育目的のネイティブ OpenCV 標準コードです。`,
    pipeline: [`まず顔検出で顔矩形を得る`, `各顔矩形でグレースケール画像を ROI として切り出す`, `目カスケードを ROI 内で detectMultiScale 実行する`, `検出した目矩形を顔のオフセットを足して元画像に描画する`],
    params: [{ arg: `minNeighbors`, desc: `目として確定するのに必要な近接検出数。目はパターンが小さく誤検出しやすいため、顔検出より大きめ(5〜10)に設定して鼻孔や眉などの偽検出を抑える。大きくしすぎると本物の目も落ちる。小さくすると検出は増えるが誤検出が急増する。` }],
    code: [
      `let eyeClassifier = new cv.CascadeClassifier();`,
      `eyeClassifier.load('haarcascade_eye.xml'); // 目用カスケードを読み込む`,
      `for (let i = 0; i < faces.size(); i++) {`,
      `  let f = faces.get(i);`,
      `  let faceRoiGray = gray.roi(f); // 顔の範囲だけを切り出して走査対象にする`,
      `  let eyes = new cv.RectVector();`,
      `  eyeClassifier.detectMultiScale(faceRoiGray, eyes, 1.1, 5, 0); // scaleFactor=1.1, minNeighbors=5`,
      `  for (let j = 0; j < eyes.size(); j++) {`,
      `    let e = eyes.get(j);`,
      `    let p1 = new cv.Point(f.x + e.x, f.y + e.y); // 顔のオフセットを足して元座標に戻す`,
      `    let p2 = new cv.Point(f.x + e.x + e.width, f.y + e.y + e.height);`,
      `    cv.rectangle(src, p1, p2, new cv.Scalar(0, 255, 0, 255), 2);`,
      `  }`,
      `  faceRoiGray.delete(); eyes.delete();`,
      `}`,
    ],
    useCases: [`まばたきや目の開閉を検出する眠気・疲労モニタリングの基礎処理`, `視線のおおよその位置を捉えるアイトラッキングの前処理`, `目の位置に合わせてサングラスやデカ目フィルタを重ねる AR メイク`, `顔認証の前段として目の位置で顔を正規化(アライメント)する処理`],
  },
  "qr-code-detection": {
    overview: `画像中の QR コードのファインダパターン(三隅の四角)を手がかりに領域を検出し、内部のモジュール配列をデコードして埋め込まれた文字列を取り出します。cv.QRCodeDetector の detectAndDecode は検出とデコードを一度に行い、コードの四隅座標と文字列を返します。結果は検出枠の重畳とデコードされたテキスト表示です。本アプリでは cv.QRCodeDetector は利用可能ですが、実画像入力とアセットの非同期ロードが前提となるため未提供で、ブラウザ標準の BarcodeDetector API も代替候補です。以下は教育目的のネイティブ OpenCV 標準コードです。`,
    pipeline: [`cv.QRCodeDetector を生成する`, `入力画像(グレースケール)を渡し detectAndDecode を呼ぶ`, `返り値の文字列と四隅座標(points)を受け取る`, `四隅を線で結び検出枠を描き、デコード文字列を表示する`],
    params: [],
    code: [
      `let qr = new cv.QRCodeDetector(); // QRコードの検出・デコード器`,
      `let points = new cv.Mat(); // 検出されたQRの四隅座標(出力)`,
      `let text = qr.detectAndDecode(gray, points); // 検出とデコードを一括実行し文字列を返す`,
      `if (text) {`,
      `  for (let i = 0; i < 4; i++) { // 四隅を順に線で結んで枠を描く`,
      `    let p1 = new cv.Point(points.data32F[i*2], points.data32F[i*2+1]);`,
      `    let n = (i + 1) % 4;`,
      `    let p2 = new cv.Point(points.data32F[n*2], points.data32F[n*2+1]);`,
      `    cv.line(src, p1, p2, new cv.Scalar(0, 255, 0, 255), 3);`,
      `  }`,
      `}`,
      `points.delete(); qr.delete();`,
    ],
    useCases: [`店舗レジやイベント受付でのチケット・クーポン QR の読み取り`, `倉庫の在庫管理で商品ラベルの QR をスキャンして照合`, `ポスターや名刺の QR を読んで URL へ遷移させる導線`, `決済アプリでの QR コード読み取りによる送金・支払い`],
  },
  "dnn-face-detection": {
    overview: `学習済みのディープニューラルネット(ResNet-SSD や軽量な YuNet など)を OpenCV の dnn モジュールで推論し、Haar Cascade より高精度に顔を検出します。画像を blobFromImage で正規化・リサイズしてネットに入力し、forward で各候補の位置と信頼度(confidence)を得て、しきい値以上のものを採用します。横顔・小顔・傾いた顔にも強く、結果は信頼度付きの顔矩形として表示されます。本アプリでは ONNX モデル(数MB)の同梱が必要なため未提供で、以下は教育目的のネイティブ OpenCV 標準コードです。`,
    pipeline: [`ONNX/Caffe モデルを cv.readNet で読み込む`, `入力画像を blobFromImage で固定サイズ・正規化された blob に変換する`, `setInput でネットに入力し forward で推論する`, `出力の各候補の信頼度を confThreshold で選別し顔矩形を描画する`],
    params: [{ arg: `confThreshold`, desc: `検出を採用する最小信頼度(0〜1)。各候補の confidence がこの値以上なら顔とみなす。高く(0.7〜0.9)すると確実な顔だけを残し誤検出が減るが見逃しが増える。低く(0.1〜0.3)すると小さい/不鮮明な顔も拾うが誤検出が増える。0.5 前後が標準的なバランス。` }],
    code: [
      `let net = cv.readNet('face_detection_yunet.onnx'); // 学習済みモデルを読み込む(事前にFSへ配置)`,
      `let blob = cv.blobFromImage(rgb, 1.0, new cv.Size(320, 320), new cv.Scalar(0,0,0), true, false); // scale=1.0, 320x320にリサイズ, swapRB=true`,
      `net.setInput(blob); // 前処理済みblobをネットに入力`,
      `let out = net.forward(); // 推論し各候補の座標と信頼度を得る`,
      `// out を走査し confidence >= 0.5(confThreshold) の候補だけ矩形描画する`,
      `blob.delete(); out.delete(); net.delete();`,
    ],
    useCases: [`横顔やマスク・低照度など Haar が苦手な条件での高精度な顔検出`, `防犯カメラで遠方の小さな顔まで検出する入退場モニタリング`, `オンライン会議アプリでの顔追従ズーム・背景ぼかしの基盤`, `後段の顔認証や表情推定に渡す高品質な顔領域の切り出し`],
  },
  "background-subtraction": {
    overview: `固定カメラの映像で、過去フレームから背景の統計モデルを学習し、それと大きく異なる画素を「動く前景」として抽出する手法です。MOG2 は各画素の色を複数のガウス分布(混合ガウスモデル)で表し、分布から外れる画素を前景と判定するため、徐々の照明変化や木の揺れなど緩やかな背景変動に適応します。毎フレーム apply を呼ぶとモデルが更新され、結果は前景を白・背景を黒にした前景マスクとして得られます。カメラが動くと背景モデルが崩れるため固定カメラ前提です。`,
    pipeline: [`cv.BackgroundSubtractorMOG2 を history/varThreshold 指定で生成する`, `映像フレームを毎フレーム取得する`, `apply に各フレームを渡し前景マスクを更新・取得する`, `モルフォロジ(opening 等)でマスクのノイズを除去して利用する`],
    params: [{ arg: `history`, desc: `背景モデルが参照する過去フレーム数。大きくすると背景が安定し一時的な変化に強くなるが、ゆっくり動く物体を背景に取り込みやすく適応も遅い。小さくすると環境変化に素早く追従するが安定性が下がる。屋内固定カメラで 200〜500 が目安。` }, { arg: `varThreshold`, desc: `画素が前景か背景かを分ける分散(マハラノビス距離)のしきい値。小さくすると微小な変化も前景とみなし感度が上がるがノイズも増える。大きくすると鈍感になり影や微動を無視するが、背景に似た前景を見逃す。16 が既定で 8〜32 の範囲で調整する。` }],
    code: [
      `let fgbg = new cv.BackgroundSubtractorMOG2(500, 16, true); // history=500, varThreshold=16, detectShadows=true(影検出ON)`,
      `let fgmask = new cv.Mat(); // 前景マスク(白=前景)の出力先`,
      `// ---- 毎フレーム以下を実行 ----`,
      `fgbg.apply(frame, fgmask); // フレームを与え背景モデルを更新しつつ前景マスクを得る`,
      `let kernel = cv.getStructuringElement(cv.MORPH_ELLIPSE, new cv.Size(3, 3));`,
      `cv.morphologyEx(fgmask, fgmask, cv.MORPH_OPEN, kernel); // 細かいノイズ点を除去`,
      `kernel.delete();`,
    ],
    useCases: [`固定監視カメラで侵入者や不審な動きを検知する防犯システム`, `店舗の入口を通る人をカウントする入退場・通行量計測`, `駐車場やゲートで車両の出入りを検知するトリガ処理`, `工場ラインで定位置を流れる製品の有無を検出する動体センシング`],
  },
  "frame-differencing": {
    overview: `連続する2フレーム(前フレームと現フレーム)の画素差の絶対値を absdiff で求め、しきい値処理で動きのある領域だけを白く取り出す最も単純な動体検知です。背景モデルを持たないため計算が軽く、突然の動きに即座に反応します。一方で物体が止まると差分が消えて検出できず、わずかなノイズや手ぶれにも反応しやすい弱点があります。結果は動いた部分が白くなる差分マスクとして表示されます。`,
    pipeline: [`前フレームと現フレームをグレースケールで用意する`, `cv.absdiff で2フレームの差の絶対値を計算する`, `cv.threshold で差分を2値化し動き領域を白くする`, `モルフォロジでノイズ除去し、現フレームを次回の前フレームとして保持する`],
    params: [{ arg: `threshold`, desc: `差分を「動き」とみなす境界値(0〜255)。小さくすると僅かな変化も検出して感度が上がるがノイズや照明ゆらぎを拾いやすい。大きくすると明確な動きだけを残しノイズに強いが、ゆっくり/小さな動きを見逃す。25 前後が標準で、ノイズの多い映像では上げる。` }],
    code: [
      `let diff = new cv.Mat();`,
      `cv.absdiff(prevGray, gray, diff); // 前フレームと現フレームの画素差の絶対値`,
      `cv.threshold(diff, diff, 25, 255, cv.THRESH_BINARY); // 差が25(threshold)超の画素を255(白)に2値化`,
      `let kernel = cv.getStructuringElement(cv.MORPH_ELLIPSE, new cv.Size(3, 3));`,
      `cv.morphologyEx(diff, diff, cv.MORPH_OPEN, kernel); // 小さなノイズを除去`,
      `gray.copyTo(prevGray); // 現フレームを次回の前フレームとして保存`,
      `kernel.delete();`,
    ],
    useCases: [`省電力デバイスでの軽量な動き検知トリガ(動いたら録画開始など)`, `玄関やデスクに置いた簡易モーションセンサー的な見守り`, `ゲームや展示の前で人が動いたことを検出するインタラクティブ起動`, `タイムラプス映像から変化のあったフレームだけを抽出する前処理`],
  },
  "optical-flow-lk": {
    overview: `Lucas-Kanade 法による疎なオプティカルフローで、画像中の少数の特徴点がフレーム間でどこへ移動したかを追跡します。まず goodFeaturesToTrack で追跡しやすいコーナー点を選び、calcOpticalFlowPyrLK で各点の移動先を画像ピラミッドを使って推定します。輝度が局所的に一定という仮定に基づくため、小さな滑らかな動きに強く、結果は各点の軌跡を結んだ線として可視化されます。大きな動きやオクルージョン(隠れ)には弱く、追跡点の補充ロジックが必要です。`,
    pipeline: [`最初のフレームで goodFeaturesToTrack により追跡する特徴点を選ぶ`, `次フレームを取得しグレースケール化する`, `calcOpticalFlowPyrLK で各点の移動先(nextPts)と追跡成否(status)を計算する`, `status=1 の点だけ採用し、移動を線で描いて軌跡を更新する`],
    params: [{ arg: `maxCorners`, desc: `goodFeaturesToTrack で初期化する追跡点の最大数。多くすると動きの捕捉が密になり画面全体の動きを把握しやすいが、点が増えるほど計算が重くなる。少なくすると軽快だが追跡が疎で局所的な動きを取りこぼす。100 前後が見やすく実用的。` }],
    code: [
      `let prevPts = new cv.Mat();`,
      `cv.goodFeaturesToTrack(prevGray, prevPts, 100, 0.3, 7); // maxCorners=100の良質なコーナーを初期追跡点に`,
      `// ---- 毎フレーム以下を実行 ----`,
      `let st = new cv.Mat(), err = new cv.Mat(), nextPts = new cv.Mat();`,
      `cv.calcOpticalFlowPyrLK(prevGray, gray, prevPts, nextPts, st, err); // 各点の移動先nextPtsと追跡成否stを推定`,
      `for (let i = 0; i < st.rows; i++) {`,
      `  if (st.data[i] === 1) { // 追跡に成功した点のみ`,
      `    let p0 = new cv.Point(prevPts.data32F[i*2], prevPts.data32F[i*2+1]);`,
      `    let p1 = new cv.Point(nextPts.data32F[i*2], nextPts.data32F[i*2+1]);`,
      `    cv.line(src, p0, p1, new cv.Scalar(0, 255, 0, 255), 2); // 移動の軌跡を線で描く`,
      `  }`,
      `}`,
      `nextPts.copyTo(prevPts); gray.copyTo(prevGray); // 次フレーム用に更新`,
    ],
    useCases: [`手や指の動きを追跡してジェスチャー入力に変換する操作系`, `動画のブレ量を点の移動から推定する手ぶれ解析・補正の前処理`, `スポーツ映像で選手やボールの軌道を追う動き解析`, `車載カメラで路面特徴点の流れから自車の動きを推定する処理`],
  },
  "dense-optical-flow": {
    overview: `Farneback 法による密なオプティカルフローで、特定の点だけでなく全画素について移動ベクトル(dx, dy)を計算します。各画素近傍を多項式で近似し、画像ピラミッドを使って大小さまざまな動きを捉えます。得たベクトル場は通常 HSV 色空間で可視化し、動きの向きを色相、動きの大きさを明度に対応させて「どこがどちらへどれだけ動いたか」を一目で表します。全画素を計算するため負荷が高く、ブラウザでは入力を縮小して使うのが定石です。`,
    pipeline: [`前フレームと現フレームをグレースケールで用意する`, `calcOpticalFlowFarneback で全画素の動きベクトル場 flow を計算する`, `flow を大きさ(magnitude)と角度(angle)に分解する`, `角度を色相・大きさを明度にした HSV を作り BGR に変換して可視化する`],
    params: [{ arg: `levels`, desc: `画像ピラミッドの階層数。階層を増やすほど縮小画像でも解析するため大きな動きを捉えやすくなるが計算が重くなる。1 だと等倍のみで大きな変位を見逃す。3 前後が標準で、群衆や高速な動きを扱うなら 4〜5 に上げる。` }],
    code: [
      `let flow = new cv.Mat(); // 各画素の(dx, dy)を持つ2chの動きベクトル場`,
      `cv.calcOpticalFlowFarneback(prevGray, gray, flow, 0.5, 3, 15, 3, 5, 1.2, 0); // pyrScale=0.5, levels=3, winsize=15, iterations=3, polyN=5, polySigma=1.2`,
      `let parts = new cv.MatVector();`,
      `cv.split(flow, parts); // flowをdx成分とdy成分に分離`,
      `let mag = new cv.Mat(), ang = new cv.Mat();`,
      `cv.cartToPolar(parts.get(0), parts.get(1), mag, ang); // 大きさmagと角度angに変換`,
      `// ang→色相(動きの向き), mag→明度(動きの強さ) のHSVを作りBGRへ変換して可視化`,
      `flow.delete(); parts.delete(); mag.delete(); ang.delete();`,
    ],
    useCases: [`交差点や広場の人波・群衆の流れを色で可視化する動線分析`, `煙・水・雲などの流体的な動きの方向と速さを観察する解析`, `動画圧縮や中間フレーム生成のための動き場推定`, `監視映像で全体の動きパターンから異常な流れを検出する処理`],
  },
  "meanshift-camshift": {
    overview: `追跡対象の色ヒストグラムをもとに、各フレームで対象らしさを表す確率画像(バックプロジェクション)を作り、その密度が高い方へ探索窓を反復移動させて対象を追う色ベース追跡です。MeanShift は窓のサイズを固定したまま位置だけを追い、CamShift(Continuously Adaptive MeanShift)は窓の大きさと回転角も毎フレーム適応させるため、対象の遠近や傾きにも追従します。結果は対象を囲む矩形(CamShift では回転矩形)として表示されます。背景に対象と似た色があると引っ張られやすく、初期枠の指定が重要です。`,
    pipeline: [`初期追跡枠(initRect)内の色ヒストグラムを HSV で計算する`, `各フレームを HSV 化し calcBackProject で対象らしさの確率画像を作る`, `meanShift または CamShift で窓を確率の高い方へ収束させる`, `更新された窓を矩形(CamShift は回転矩形)として描画する`],
    params: [{ arg: `window`, desc: `追跡対象の初期矩形 [x, y, w, h]。対象をちょうど囲むように設定するのが重要で、背景を多く含めると背景色を学習して追跡が不安定になる。狭すぎると対象の色特徴が不足する。CamShift では追跡中にこの窓の大きさ・角度が自動更新される。` }],
    code: [
      `let tw = new cv.Rect(100, 100, 80, 80); // 初期追跡枠 [x=100,y=100,w=80,h=80]`,
      `let crit = new cv.TermCriteria(cv.TERM_CRITERIA_EPS | cv.TERM_CRITERIA_COUNT, 10, 1); // 反復終了条件: 最大10回 or 移動1px未満`,
      `// ---- 毎フレーム ----`,
      `let hsv = new cv.Mat(); cv.cvtColor(frame, hsv, cv.COLOR_RGB2HSV);`,
      `let backproj = new cv.Mat();`,
      `cv.calcBackProject(hsvVec, [0], roiHist, backproj, [0, 180], 1); // 対象色ヒストから確率画像を生成`,
      `let rr = cv.CamShift(backproj, tw, crit); // 窓twを確率の高い方へ収束(位置/サイズ/角度を更新)`,
      `// rr は回転矩形。rr.points() で4頂点を得て描画。MeanShiftなら cv.meanShift(backproj, tw, crit)`,
      `hsv.delete(); backproj.delete();`,
    ],
    useCases: [`特定の色の服を着た人やボールを追い続ける色ベースの物体追跡`, `肌色ヒストグラムを使った顔や手の追跡とジェスチャ入力`, `スポーツ中継で目立つ色のユニフォーム選手をフォローするカメラワーク`, `ロボットが指定色のマーカーや対象物を視覚的に追従する処理`],
  },
  "chessboard-corners": {
    overview: `キャリブレーション用チェスボード（市松模様）を撮影した画像から、黒白マスが交わる内部コーナーの格子点を1点ずつ検出する。原理は、画像を二値化して市松パターンの矩形を見つけ、それらの角を連結して規則正しい格子（patternSize で指定した列×行）を構成し、cornerSubPix でサブピクセル精度に追い込む。結果は元画像の上に検出コーナーが色付きの折れ線（行ごとに虹色グラデーション）で重ね描きされ、全コーナーが見つかったかどうかが found フラグで返る。本アプリでは cv.findChessboardCorners / drawChessboardCorners / cornerSubPix が techstark 4.10 ビルドに未バインドのため未提供で、以下はネイティブ OpenCV の標準的な使い方の説明である。`,
    pipeline: [`画像をグレースケール化する（cv.cvtColor）。`, `cv.findChessboardCorners で内部コーナーの格子（列×行）を探索し found を得る。`, `見つかったら cv.cornerSubPix でサブピクセル精度に補正する。`, `cv.drawChessboardCorners で検出結果を元画像に折れ線で重ね描きする。`],
    params: [{ arg: `patternSize (cv.Size)`, desc: `内部コーナーの数を「列×行」で指定する（例 9x6 は黒白マス10x7枚の盤）。実際の盤の内部コーナー数と完全一致が必須で、ズレると found=false になる。マス数ではなく交点数を数える点に注意。` }, { arg: `flags`, desc: `探索の前処理オプション。CALIB_CB_ADAPTIVE_THRESH（適応的二値化、照明ムラに強い）, CALIB_CB_NORMALIZE_IMAGE（事前ヒストグラム正規化）, CALIB_CB_FAST_CHECK（盤が無い画像を高速に棄却）。OR で組み合わせる。` }],
    code: [
      `let gray = new cv.Mat();`,
      `cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY); // 検出はグレースケールで行う`,
      `let corners = new cv.Mat();`,
      `let patternSize = new cv.Size(9, 6); // 内部コーナー数（列9 × 行6）= 盤の交点数`,
      `let found = cv.findChessboardCorners(`,
      `  gray,                              // 入力グレースケール画像`,
      `  patternSize,                       // 探す格子の大きさ（実盤と一致必須）`,
      `  corners,                           // 検出された格子点（出力, CV_32FC2）`,
      `  cv.CALIB_CB_ADAPTIVE_THRESH | cv.CALIB_CB_NORMALIZE_IMAGE // 照明ムラに強い前処理`,
      `);`,
      `if (found) {`,
      `  let criteria = new cv.TermCriteria(cv.TERM_CRITERIA_EPS + cv.TERM_CRITERIA_MAX_ITER, 30, 0.001); // 反復30回/誤差0.001で収束`,
      `  cv.cornerSubPix(gray, corners, new cv.Size(11, 11), new cv.Size(-1, -1), criteria); // サブピクセル精度に補正`,
      `  cv.drawChessboardCorners(src, patternSize, corners, found); // 元画像に格子を重ね描き`,
      `}`,
    ],
    useCases: [`カメラキャリブレーション（焦点距離・主点・歪み係数の推定）の前段として複数枚の格子点を集める。`, `プロジェクタ・カメラ系の幾何補正や、AR の平面基準マーカー代わりに使う。`, `ステレオカメラの左右画像から対応点を取り、外部パラメータ（相対姿勢）を求める。`, `ロボットのハンドアイキャリブレーションで、既知サイズの盤を基準平面として座標系を合わせる。`],
  },
  "camera-calibration": {
    overview: `複数アングルから撮ったチェスボード画像群を使い、カメラの内部パラメータ行列K（焦点距離 fx,fy と主点 cx,cy）とレンズ歪み係数（半径方向 k1,k2,k3 と接線方向 p1,p2）を一括推定し、その結果でレンズの樽型・糸巻き型歪みを取り除く。原理は、盤上の格子点の3D座標（objectPoints）と各画像での2D観測座標（imagePoints）の対応から、再投影誤差を最小化するように最小二乗で解く（Zhang の手法）。結果として直線が真っ直ぐに写る歪み補正画像と、再投影誤差（RMS、1px未満なら良好）が得られる。本アプリでは cv.calibrateCamera / cornerSubPix が techstark 4.10 ビルドに未バインドで前段の盤検出も無いため未提供であり、以下はネイティブ OpenCV の標準的な手順の説明である。`,
    pipeline: [`各画像でチェスボードのコーナーを検出し imagePoints に集める。`, `盤の実寸格子に対応する3D座標 objectPoints を画像枚数ぶん用意する。`, `cv.calibrateCamera で K・歪み係数・各画像の姿勢(rvecs,tvecs)を推定する。`, `cv.getOptimalNewCameraMatrix と cv.undistort で歪みを補正する。`],
    params: [{ arg: `objectPoints`, desc: `盤の各内部コーナーの3D座標（z=0平面）の配列。マスの実寸（mm）を掛けると物理スケールで校正でき、tvec が実距離になる。画像枚数ぶん同じ格子を並べる。` }, { arg: `imagePoints`, desc: `各画像で検出した2Dコーナー座標。objectPoints と1対1対応。枚数が多く（10〜20枚）、盤の傾き・位置にバリエーションがあるほど推定が安定する。` }, { arg: `imageSize (cv.Size)`, desc: `校正に使う画像の幅×高さ。主点と焦点距離の基準になるので、実際の撮影解像度と一致させる。` }, { arg: `K (cameraMatrix)`, desc: `推定される3x3内部行列（出力）。fx,fy が焦点距離(px)、cx,cy が主点。CALIB_USE_INTRINSIC_GUESS で初期値を与えることも可能。` }, { arg: `distCoeffs`, desc: `推定される歪み係数(出力, 通常 k1,k2,p1,p2,k3)。広角レンズほど k1 の絶対値が大きくなる。CALIB_RATIONAL_MODEL 等で項数を増やせる。` }],
    code: [
      `// objectPoints[i]: i枚目の盤の3D格子点(CV_32FC3), imagePoints[i]: 対応する2D検出点(CV_32FC2)`,
      `let K = new cv.Mat();          // 推定される内部パラメータ行列(出力)`,
      `let distCoeffs = new cv.Mat(); // 推定される歪み係数(出力)`,
      `let rvecs = new cv.MatVector(); // 各画像のカメラ回転ベクトル(出力)`,
      `let tvecs = new cv.MatVector(); // 各画像のカメラ並進ベクトル(出力)`,
      `let rms = cv.calibrateCamera(`,
      `  objectPoints,            // 3D格子点（画像枚数ぶん）`,
      `  imagePoints,             // 2D検出点（objectPointsと1対1）`,
      `  imageSize,               // 校正画像の幅×高さ`,
      `  K, distCoeffs,           // 推定結果（出力）`,
      `  rvecs, tvecs             // 各画像の姿勢（出力）`,
      `); // 戻り値 rms は再投影誤差。1.0px未満なら良好`,
      `let dst = new cv.Mat();`,
      `cv.undistort(src, dst, K, distCoeffs); // 推定した K と歪みで補正画像を生成`,
    ],
    useCases: [`魚眼・広角レンズの強い樽型歪みを補正し、直線が直線に写る画像へ整える。`, `計測用カメラの校正で、画素座標から実寸法（測量・寸法検査）へ変換する基準を得る。`, `AR/VR でカメラの内部パラメータを既知化し、仮想物体を正しい遠近感で重畳する。`, `ステレオ・複数台カメラの統合キャリブレーションで3D復元やデプス推定の精度を上げる。`, `ドローン空撮のオルソ画像作成で、レンズ歪みを除いてから正射投影を行う。`],
  },
  "inpainting": {
    overview: `マスク（白で塗った領域）で指定した不要部分を、その周囲のピクセル情報から自然に補完して消す画像修復。原理は、欠損境界から内側へ向かって、周辺の色とエッジ（等照度線）の向きを伝播させて穴を埋める手法で、TELEA は高速マーチング法ベース、NS は流体力学（ナビエ・ストークス）に着想を得た方式。結果は、傷・文字・小さな不要物が消え、周囲のテクスチャと馴染んだ画像になる。本アプリでは photo モジュールの cv.inpaint が opencv.js ビルドに含まれるか要確認の状態で、Canvas のブラシでマスクを描いて対話的に修復する構成を想定している。`,
    pipeline: [`入力画像を3チャンネル（RGB/BGR）に整える。`, `消したい領域をCanvasのブラシで白く塗り、8bit1チャンネルのマスクを作る。`, `cv.inpaint に画像・マスク・補完半径・手法を渡して修復する。`, `結果を Before/After で比較表示する。`],
    params: [{ arg: `inpaintRadius (radius)`, desc: `各欠損ピクセルを補完する際に参照する近傍円の半径(px)。小さいほどシャープだが大きな穴は埋まりにくく、大きいほど滑らかだがディテールがぼけて処理も重い。細い傷や文字は3前後、太い欠損は5〜10が目安。default 3, 範囲1〜15。` }, { arg: `flags (method)`, desc: `補完アルゴリズム。cv.INPAINT_TELEA（高速マーチング法、エッジに沿って速く埋める）と cv.INPAINT_NS（ナビエ・ストークス、滑らかな勾配伝播）。一般に TELEA がやや速くシャープ、NS は緩やかなグラデーション領域で自然になりやすい。両方試して良い方を選ぶ。` }],
    code: [
      `let dst = new cv.Mat();`,
      `// mask: 消したい画素を255(白)、残す画素を0(黒)にした CV_8UC1`,
      `cv.inpaint(`,
      `  src,                 // 入力画像（8bit, 1 or 3チャンネル）`,
      `  mask,                // 修復対象を白で示すマスク（CV_8UC1）`,
      `  dst,                 // 修復結果（出力）`,
      `  3,                   // inpaintRadius: 参照する近傍半径(px)。小さいほどシャープ`,
      `  cv.INPAINT_TELEA     // 手法: TELEA(高速マーチング) / NS(ナビエ・ストークス)`,
      `);`,
    ],
    useCases: [`古い写真の折れ目・スクラッチ・カビ跡を消して修復する。`, `画像に写り込んだ日付スタンプ・透かし・電線などの不要物を除去する。`, `顔写真のニキビ・ホクロや、肌の小さな傷をレタッチする。`, `動画フレームのロゴ・字幕を1枚ずつマスクして消去する。`, `医用・衛星画像でセンサ欠損やノイズ穴を周囲情報から穴埋めする。`],
  },
  "stylization-filters": {
    overview: `写真を油彩・鉛筆画・カートゥーン調などの芸術的な見た目へ変換するフィルタ群。ネイティブ OpenCV では photo モジュールの cv.stylization（エッジ保持平滑化で水彩風）・cv.pencilSketch（鉛筆スケッチ）・cv.edgePreservingFilter が用意され、いずれもエッジを保ちながら平坦領域を滑らかにする非線形フィルタ（ドメイン変換）が核となる。結果は輪郭を残したままディテールが整理され、絵画のような質感になる。本アプリでは photo モジュールが未搭載のため、imgproc だけで等価な効果を自作している（鉛筆画＝グレー反転とぼかしのカラードッジ合成、カートゥーン＝bilateralFilter で平滑化＋adaptiveThreshold のエッジを重畳）。`,
    pipeline: [`入力をRGBに整え、重い処理なので適宜縮小する。`, `効果を選択（スタイライズ/鉛筆画/エッジ保持）。`, `自作版はカートゥーンなら cv.bilateralFilter で平滑化し cv.adaptiveThreshold の輪郭を合成、鉛筆画ならグレー反転＋ぼかしをカラードッジ合成。`, `結果を表示し Before/After で比較する。`],
    params: [{ arg: `effect (select)`, desc: `加工の種類。stylization（水彩/油彩風）、pencilSketch（鉛筆スケッチ）、edgePreserving（エッジ保持の滑らか化）。本アプリの自作版では加えてカートゥーン調も提供する。` }, { arg: `sigma_s (空間シグマ, ネイティブ)`, desc: `cv.stylization 等の空間方向の平滑化強度（0〜200程度）。大きいほど広範囲を均し、ベタっとした絵画調になる。` }, { arg: `sigma_r (範囲シグマ, ネイティブ)`, desc: `色差方向の平滑化強度（0〜1）。小さいほどエッジを強く保ち、大きいほどエッジも溶けて滑らかになる。stylization の既定は概ね 0.45 前後。` }, { arg: `d / sigmaColor / sigmaSpace (自作カートゥーンの bilateral)`, desc: `本アプリのカートゥーン実装で使う双方向フィルタの近傍径と色・空間シグマ。sigmaColor を上げるほど色がフラットになり、繰り返し適用するとアニメ調が強まる。` }],
    code: [
      `// --- ネイティブ OpenCV(photo) を使える場合 ---`,
      `let dst = new cv.Mat();`,
      `cv.stylization(`,
      `  rgb,    // 入力RGB画像（8bit3ch）`,
      `  dst,    // 水彩/油彩風の出力`,
      `  60,     // sigma_s: 空間方向の平滑化強度(0〜200)。大きいほど広範囲を均す`,
      `  0.45    // sigma_r: 色差方向の平滑化(0〜1)。小さいほどエッジを保つ`,
      `);`,
      `// --- 本アプリの自作カートゥーン（imgproc のみ）---`,
      `let smooth = new cv.Mat();`,
      `cv.bilateralFilter(rgb, smooth, 9, 75, 75); // エッジを保ち色を平滑化`,
      `let gray = new cv.Mat(), edges = new cv.Mat();`,
      `cv.cvtColor(rgb, gray, cv.COLOR_RGB2GRAY);`,
      `cv.adaptiveThreshold(gray, edges, 255, cv.ADAPTIVE_THRESH_MEAN_C, cv.THRESH_BINARY, 9, 2); // 輪郭線を抽出`,
      `let cartoon = new cv.Mat();`,
      `cv.bitwise_and(smooth, smooth, cartoon, edges); // 平滑化画像に輪郭マスクを重畳`,
    ],
    useCases: [`SNS投稿用に写真を水彩・油彩・カートゥーン風へ加工する。`, `ポートレートを鉛筆スケッチ化してプロフィール画像やイラスト素材にする。`, `プレゼン資料やサムネイルで、写真を絵画調に統一してトーンを揃える。`, `ノイズの多い写真をエッジ保持フィルタで滑らかにしつつ輪郭を残す前処理に使う。`, `アニメ調変換の教材として、双方向フィルタとエッジ抽出の組み合わせを学ぶ。`],
  },
  "panorama-stitching": {
    overview: `視野が重なる複数枚の写真を、重複領域を手がかりに位置合わせして1枚の広いパノラマへ繋ぐ。原理は、各画像から特徴点を検出・記述し、画像間で対応点をマッチングして射影変換（ホモグラフィ）を求め、片方の画像をその変換で歪めて重ね合わせる。結果は継ぎ目が自然に繋がった横長（または広角）の合成画像になる。本アプリでは高レベルの cv.Stitcher クラスが opencv.js ビルドに含まれないため未搭載で、ORB 特徴量＋BFMatcher のマッチング＋cv.findHomography（RANSAC）＋cv.warpPerspective による自作パイプラインで実装している。`,
    pipeline: [`各画像から cv.ORB で特徴点と記述子を抽出する。`, `cv.BFMatcher で2枚間の対応点をマッチングし、距離で良い対応に絞る。`, `cv.findHomography（RANSAC）で外れ値を除きつつ射影変換行列を推定する。`, `cv.warpPerspective で片方を変換し、もう一方のキャンバスに重ねて合成する。`],
    params: [{ arg: `mode (select / ネイティブ Stitcher)`, desc: `ネイティブ Stitcher の合成モード。PANORAMA は回転中心の風景パノラマ向け（球面/円筒ワープ）、SCANS は平面スキャン（書類など）向けでアフィン的に繋ぐ。本アプリ自作版では常に平面ホモグラフィで繋ぐ。` }, { arg: `nfeatures (ORB)`, desc: `ORB が抽出する特徴点の最大数。多いほど対応が増え位置合わせが安定するが計算は重い。500〜2000が目安。` }, { arg: `ratio / 距離しきい値 (マッチング)`, desc: `良い対応だけ残すための比率（Loweのratio test, 0.7前後）や距離上限。厳しくすると誤対応が減るが対応数も減る。` }, { arg: `ransacReprojThreshold (findHomography)`, desc: `RANSAC で対応をインライアと見なす再投影誤差の上限(px)。default 3前後。小さいほど厳密だが対応点が少ないと推定が不安定になる。` }],
    code: [
      `// --- 本アプリの自作パイプライン（Stitcher 非搭載のため）---`,
      `let orb = new cv.ORB(1000); // 特徴点を最大1000個抽出`,
      `let kp1 = new cv.KeyPointVector(), des1 = new cv.Mat();`,
      `orb.detectAndCompute(img1, new cv.Mat(), kp1, des1); // 画像1の特徴点と記述子`,
      `let kp2 = new cv.KeyPointVector(), des2 = new cv.Mat();`,
      `orb.detectAndCompute(img2, new cv.Mat(), kp2, des2); // 画像2の特徴点と記述子`,
      `let bf = new cv.BFMatcher(cv.NORM_HAMMING, true); // ORBはバイナリ記述子→ハミング距離`,
      `let matches = new cv.DMatchVector();`,
      `bf.match(des1, des2, matches); // 対応点をマッチング`,
      `// 良い対応から srcPoints / dstPoints(CV_32FC2) を作り、`,
      `let H = cv.findHomography(srcPoints, dstPoints, cv.RANSAC, 3); // 射影変換行列(RANSAC, 誤差3px)`,
      `let pano = new cv.Mat();`,
      `cv.warpPerspective(img1, pano, H, new cv.Size(img1.cols + img2.cols, img1.rows)); // 画像1を変換して広いキャンバスへ`,
      `// img2 を pano の対応領域にコピーして合成`,
    ],
    useCases: [`複数枚の風景写真を繋いで横長の広角パノラマを作る。`, `大きな書類や絵画を分割撮影し、つなぎ合わせて1枚の高解像画像にする。`, `ホワイトボードや壁面の写真を複数撮って全体像に合成する。`, `ドローン・スマホの連続写真からモザイク（オルソマップ）を生成する。`, `特徴点マッチングとホモグラフィ推定の仕組みを学ぶ教材として使う。`],
  },
  "kmeans-clustering": {
    overview: `データ点をk個のクラスタに分割する古典的な教師なし学習。原理は、各点を最も近い重心に割り当て→各クラスタの重心を再計算、を収束するまで反復する（Lloyd 法）。OpenCV では各画素のRGBを特徴ベクトルとみなせば「減色（色クラスタリング）」になり、2D点群に適用すれば分割の様子を可視化できる。結果は、画像なら代表k色だけで塗り分けられたポスタライズ画像、点群ならk色に色分けされたクラスタとして見える。本アプリでは cv.kmeans が core モジュールに含まれ実際に動作する。`,
    pipeline: [`画像を1画素1行のサンプル行列（CV_32F）に整形する（減色なら3列＝RGB）。`, `TermCriteria で反復回数・収束誤差を設定する。`, `cv.kmeans に k・試行回数・初期化方法を渡してラベルと重心を得る。`, `各画素を所属クラスタの重心色に置き換えて復元・表示する。`],
    params: [{ arg: `K`, desc: `分割するクラスタ数。減色なら出力色数に相当し、小さいほど大胆にポスタライズ、大きいほど元画像に近づく。default 3, 範囲2〜10。` }, { arg: `criteria (TermCriteria)`, desc: `停止条件。最大反復回数（例10）と収束誤差 epsilon（例1.0）のいずれか到達で終了。回数を増やすと精度は上がるが遅くなる。` }, { arg: `attempts`, desc: `異なる初期重心で試行する回数（例5）。多いほど局所解に陥りにくく良い分割が得られるが計算は線形に増える。` }, { arg: `flags (初期化)`, desc: `重心の初期化方法。cv.KMEANS_PP_CENTERS（k-means++、賢い初期化で安定）か cv.KMEANS_RANDOM_CENTERS（ランダム）。通常 PP_CENTERS 推奨。` }],
    code: [
      `// src を1画素1行の float サンプルに整形（減色: RGBの3列）`,
      `let samples = new cv.Mat();`,
      `src.convertTo(samples, cv.CV_32F);`,
      `samples = samples.reshape(1, src.rows * src.cols); // 1行=1画素の特徴ベクトル`,
      `let labels = new cv.Mat();   // 各画素の所属クラスタ番号(出力)`,
      `let centers = new cv.Mat();  // 各クラスタの重心色(出力)`,
      `let criteria = new cv.TermCriteria(cv.TERM_CRITERIA_EPS + cv.TERM_CRITERIA_MAX_ITER, 10, 1.0); // 反復10/誤差1.0で停止`,
      `cv.kmeans(`,
      `  samples,                 // 入力サンプル(CV_32F)`,
      `  3,                       // K: クラスタ数（=出力色数）`,
      `  labels,                  // 所属ラベル(出力)`,
      `  criteria,                // 停止条件`,
      `  5,                       // attempts: 初期値を変えた試行回数`,
      `  cv.KMEANS_PP_CENTERS,    // 初期化: k-means++（安定）`,
      `  centers                  // 重心(出力)`,
      `);`,
    ],
    useCases: [`画像の減色・ポスタライズ（代表k色で塗り直すアート加工）。`, `主要色（ドミナントカラー）抽出によるカラーパレット生成やテーマ色判定。`, `2D点群データを色分けして、教師なしクラスタリングの挙動を可視化・学習する。`, `前処理として色を量子化し、領域分割やインデックスカラー化を簡略化する。`, `特徴ベクトルの代表点（コードブック）を作り、Bag-of-Visual-Words の語彙生成に使う。`],
  },
  "knn-digit-recognition": {
    overview: `ml モジュールの k近傍法（kNN）で手書き数字0〜9を分類する。原理は、学習サンプルを特徴ベクトルとして全て記憶しておき、入力の特徴に最も近いk個の学習サンプルの多数決でクラスを決める「怠惰学習」。Canvas に描いた数字を小さくリサイズ・正規化して特徴にし、kNN.findNearest で最も近い既知サンプルを引いて数字を当てる。結果は推定された数字ラベルと、近傍の内訳・距離が得られる。本アプリでは cv.ml.KNearest が techstark 4.10 ビルドに未バインドのため未提供で、以下はネイティブ OpenCV の標準的な使い方の説明である。`,
    pipeline: [`学習データ（MNIST 等の数字画像）を特徴ベクトル化し train する。`, `Canvasに描いた数字を取得し、20x20程度に正規化して同じ特徴に変換する。`, `cv.ml.KNearest.findNearest にサンプルと k を渡して近傍を探す。`, `多数決で得た数字ラベルと近傍内訳を表示する。`],
    params: [{ arg: `k`, desc: `推論時に参照する近傍サンプル数。小さい（k=1）と局所的でノイズに敏感、大きいと滑らかだが境界がぼける。奇数にすると多数決の同数を避けられる。default 3, 範囲1〜10。` }, { arg: `trainData / layout`, desc: `学習特徴行列(CV_32F)。cv.ml.ROW_SAMPLE は1行=1サンプルの並び。サンプル数と次元が分類精度に直結する。` }, { arg: `responses (labels)`, desc: `各学習サンプルの正解ラベル(数字0〜9)。trainData と行数一致が必須。` }],
    code: [
      `let knn = cv.ml.KNearest.create();`,
      `knn.train(`,
      `  trainData,            // 学習特徴(CV_32F, 1行=1サンプル)`,
      `  cv.ml.ROW_SAMPLE,     // データ並び: 行方向に1サンプル`,
      `  labels                // 各サンプルの正解ラベル(0〜9)`,
      `);`,
      `let results = new cv.Mat();    // 推定ラベル(出力)`,
      `let neighbours = new cv.Mat(); // 近傍のラベル内訳(出力)`,
      `let dists = new cv.Mat();      // 近傍までの距離(出力)`,
      `let r = knn.findNearest(`,
      `  sample,    // 入力の特徴ベクトル(描いた数字を正規化したもの)`,
      `  3,         // k: 参照する近傍数。奇数だと同数を回避しやすい`,
      `  results, neighbours, dists // 推定ラベル・近傍・距離(出力)`,
      `);`,
    ],
    useCases: [`Canvas に手書きした数字をリアルタイムで認識する入力デモ。`, `古典的な分類器（kNN）の仕組みと k の影響を体験的に学ぶ教材。`, `簡易OCRの前段として、セグメント化した数字を1文字ずつ判定する。`, `特徴量設計（リサイズ・HOG等）の違いが精度に与える影響を比較する実験台。`],
  },
  "dnn-image-classification": {
    overview: `学習済みCNN（MobileNet 等）で1枚の画像が何のクラス（犬・猫・車…）かを推論する。原理は、画像を固定サイズにリサイズしモデル指定の平均減算・スケールで正規化した4次元テンソル（blob）に変換し、ネットワークを前向き計算（forward）して各クラスの確率（softmax 出力）を得る。結果は確率上位 topK 個のクラス名とスコアが表示される。本アプリでは dnn モジュール自体は利用可能だが、推論には ONNX モデル（MobileNet 約10MB）とラベル一覧の同梱が必要なため未提供で、以下はネイティブ OpenCV.js の標準的な使い方の説明である。`,
    pipeline: [`cv.readNetFromONNX で学習済みモデルを読み込む。`, `cv.blobFromImage で画像をモデル指定サイズ・正規化の入力blobに変換する。`, `net.setInput → net.forward で確率ベクトルを得る。`, `確率を降順ソートし上位topKのクラス名とスコアを表示する。`],
    params: [{ arg: `topK`, desc: `確率の高い上位何クラスを表示するか。小さいと最有力候補だけ、大きいと紛らわしい候補も並ぶ。default 5, 範囲1〜10。` }, { arg: `scalefactor (1/255 等)`, desc: `ピクセル値に掛ける倍率。多くのモデルは0〜1へ正規化するため1/255。モデルの学習時前処理に合わせる（合わないと精度が崩れる）。` }, { arg: `size (cv.Size)`, desc: `モデルが要求する入力解像度（MobileNetは224x224）。実際の入力テンソル形状と一致必須。` }, { arg: `mean (cv.Scalar)`, desc: `各チャンネルから引く平均値。ImageNet系は(0.485,0.456,0.406)*255等。scalefactorとの順序・値はモデル依存。` }, { arg: `swapRB (true/false)`, desc: `RとBチャンネルを入れ替えるか。OpenCVはBGR、多くのモデルはRGB学習のため通常 true。` }, { arg: `crop (true/false)`, desc: `リサイズ時に中央クロップするか。false ならアスペクト無視で size に伸縮。` }],
    code: [
      `let net = cv.readNetFromONNX('mobilenet.onnx'); // 学習済みONNXモデルを読み込み`,
      `let blob = cv.blobFromImage(`,
      `  rgb,                       // 入力画像`,
      `  1 / 255,                   // scalefactor: 画素値を0〜1へ正規化`,
      `  new cv.Size(224, 224),     // size: モデルが要求する入力解像度`,
      `  new cv.Scalar(0, 0, 0),    // mean: 各チャンネルから引く平均値（モデル依存）`,
      `  true,                      // swapRB: BGR→RGB入れ替え（多くのモデルで必要）`,
      `  false                      // crop: 中央クロップせず size に伸縮`,
      `);`,
      `net.setInput(blob);          // 入力テンソルをセット`,
      `let prob = net.forward();    // 前向き計算→各クラスの確率`,
      `// prob を降順ソートし上位 topK のクラス名・スコアを取り出す`,
    ],
    useCases: [`アップロードまたはカメラ画像の物体カテゴリを推定して画像にタグ付けする。`, `商品写真の自動分類や、不適切画像のスクリーニングの前段に使う。`, `ブラウザだけで完結する軽量な画像認識（サーバ送信不要のプライバシ重視用途）。`, `深層学習推論のパイプライン（blob化→forward→後処理）を体験的に学ぶ教材。`],
  },
  "dnn-object-detection": {
    overview: `学習済み物体検出モデル（YOLO や SSD）で、1枚の画像から複数の物体をクラスごとにバウンディングボックスで囲んで検出する。原理は、画像を入力blobにして forward すると、多数の候補枠（位置・サイズ・各クラスの信頼度）が出力され、信頼度しきい値で弱い枠を捨て、Non-Maximum Suppression（NMS）で重複枠を1つに統合する。結果は、検出された各物体に色付き矩形とクラス名・スコアが重畳表示される。本アプリでは dnn は使えるものの ONNX 検出モデル（YOLOv5/8-nano でも数MB）の同梱と出力デコードが必要なため未提供で、以下はネイティブ OpenCV.js の標準的な使い方の説明である。`,
    pipeline: [`cv.readNet で検出モデルを読み込む。`, `cv.blobFromImage で画像を入力サイズ・正規化のblobに変換する。`, `net.forward で候補枠（位置・信頼度）を得てモデル仕様に従いデコードする。`, `信頼度しきい値で絞り、cv.NMSBoxes で重複枠を抑制して描画する。`],
    params: [{ arg: `confThreshold`, desc: `検出として採用する最小信頼度。上げると誤検出（false positive）が減るが見逃しが増え、下げると拾うが誤検出も増える。default 0.5, 範囲0.1〜0.9。` }, { arg: `nmsThreshold`, desc: `NMSで重なる枠を重複とみなすIoUしきい値。小さいほど重複に厳しく枠が減り、大きいほど近接物体の枠を残す。default 0.4, 範囲0.1〜0.9。` }, { arg: `size (cv.Size)`, desc: `モデルの入力解像度（YOLOは640x640等）。大きいほど小物体に強いが遅くなる。` }, { arg: `scalefactor (1/255)`, desc: `画素を0〜1へ正規化する倍率。YOLO系は通常1/255。学習時前処理に合わせる。` }, { arg: `swapRB (true)`, desc: `BGR→RGBの入れ替え。多くのモデルがRGB学習のため通常true。` }],
    code: [
      `let net = cv.readNet('yolov5n.onnx'); // 学習済み検出モデルを読み込み`,
      `let blob = cv.blobFromImage(`,
      `  rgb,                     // 入力画像`,
      `  1 / 255,                 // scalefactor: 0〜1へ正規化`,
      `  new cv.Size(640, 640),   // size: 検出モデルの入力解像度（大きいほど小物体に強い）`,
      `  new cv.Scalar(),         // mean: 平均減算なし(0)`,
      `  true,                    // swapRB: BGR→RGB`,
      `  false                    // crop: 伸縮（クロップしない）`,
      `);`,
      `net.setInput(blob);        // 入力をセット`,
      `let out = net.forward();   // 候補枠（位置・信頼度）を出力`,
      `// out をモデル仕様でデコードし boxes/scores を作成`,
      `let indices = new cv.IntVector();`,
      `cv.NMSBoxes(boxes, scores, 0.5, 0.4, indices); // 信頼度0.5以上を残しIoU0.4で重複枠を抑制`,
    ],
    useCases: [`画像・カメラ映像内の人・車・物体を検出して矩形で囲み、種類を表示する。`, `通行人や車両のカウント、入店者数の概算などの計数アプリ。`, `リアルタイム検出によるインタラクティブデモ（手や物の追跡）。`, `監視・安全用途で特定物体（ヘルメット未着用など）の検出をトリガーにする。`, `検出パイプライン（blob→forward→デコード→NMS）の流れを学ぶ教材。`],
  },
  "image-upload": {
    overview: `ローカルのファイルやドラッグ&ドロップで画像を読み込み、OpenCV が扱える cv.Mat（CV_8UC4 のRGBAバッファ）へ変換する、全デモ共通の入力基盤。原理は、file input で受け取った画像を <img> もしくは Canvas に描画し、cv.imread(要素) または ImageData から cv.matFromImageData で Mat 化する。結果として、以降の任意のCV処理に渡せる行列が得られる。これはOpenCVのアルゴリズムというよりアプリのUI／I/O機能で、本アプリでは大きすぎる画像の自動リサイズも行う。`,
    pipeline: [`file input かドラッグ&ドロップで画像ファイルを受け取る。`, `<img> または Canvas に描画する（大画像はここでリサイズ）。`, `cv.imread(要素) で cv.Mat(CV_8UC4) に変換する。`, `得た Mat を各デモの処理へ渡す。`],
    params: [{ arg: `imread の引数`, desc: `OpenCV.js では <img> または <canvas> 要素（または id）を渡す。読み込み結果は常に RGBA 4チャンネルの CV_8UC4 になる点が、ファイルパスを取るネイティブ版と異なる。` }, { arg: `matFromImageData の引数`, desc: `Canvas の ctx.getImageData が返す ImageData を渡すと CV_8UC4 の Mat を生成する。Canvas 経由ならEXIF回転やリサイズを前処理で挟める。` }],
    code: [
      `let imgEl = document.getElementById('uploaded'); // 読み込んだ <img> 要素`,
      `let src = cv.imread(imgEl); // <img> から直接 cv.Mat(CV_8UC4=RGBA) を生成`,
      `// あるいは Canvas 経由（リサイズ・回転補正を挟みたい場合）`,
      `let ctx = canvas.getContext('2d');`,
      `ctx.drawImage(imgEl, 0, 0, w, h);          // 必要ならリサイズして描画`,
      `let imgData = ctx.getImageData(0, 0, w, h); // RGBAバイト列を取得`,
      `let src2 = cv.matFromImageData(imgData);    // ImageData → cv.Mat(CV_8UC4)`,
    ],
    useCases: [`ユーザー自身の写真を読み込んで各フィルタ・検出デモを試す。`, `全デモ共通の入力ステージとして、処理対象の Mat を供給する。`, `ドラッグ&ドロップ対応で複数枚を順に取り込む（パノラマ等の前段）。`, `大きすぎる画像を読み込み時に自動リサイズして処理を軽くする。`],
  },
  "before-after-compare": {
    overview: `入力画像（Before）と処理結果（After）を、左右並列・スライダ・オーバーレイの3方式で並べて見比べる共通UI。原理としては、cv.imshow で2枚のMatをそれぞれCanvasに描画し、その上でReact側が表示方式を切り替えるだけで、画像処理そのものは行わない。結果として、フィルタや検出の効果が直感的に理解でき、特にスライダ比較は境界を動かして差分を体感できるため学習効果が高い。これはOpenCVの機能ではなくアプリのUI部品で、全デモで再利用される。`,
    pipeline: [`処理前の Mat を before 用 Canvas に cv.imshow する。`, `処理後の Mat を after 用 Canvas に cv.imshow する。`, `比較モード（並列/スライダ/オーバーレイ）をUIで選ぶ。`, `選んだ方式で2枚を重ね・並べて表示する。`],
    params: [{ arg: `mode (select)`, desc: `比較表示の方法。side（左右並列で全体を一望）、split（スライダで境界を動かし同位置の前後を比較。学習効果が高い）、overlay（不透明度を変えて重ねる）。default は split。` }, { arg: `imshow の引数`, desc: `cv.imshow(canvasIdまたは要素, mat)。Mat を Canvas に直接描画する。CV_8UC1/8UC3/8UC4 を自動でRGBA表示に変換する。` }],
    code: [
      `cv.imshow('beforeCanvas', src); // 処理前 Mat を before 用 Canvas に描画`,
      `cv.imshow('afterCanvas', dst);  // 処理後 Mat を after 用 Canvas に描画`,
      `// 比較モードに応じて React 側で2つの Canvas を`,
      `//   side: 横並び / split: スライダで境界移動 / overlay: 透明度で重畳`,
      `// として表示を切り替える`,
    ],
    useCases: [`フィルタ（ぼかし・二値化など）の適用前後の差を一目で確認する。`, `スライダ比較で同じ位置の前後を動かしながら効果の強弱を体感する。`, `検出・補正系デモで、結果オーバーレイの不透明度を変えて重なりを確かめる。`, `全デモ共通の出力ステージとして、学習者に処理効果を提示する。`],
  },
  "webcam-capture": {
    overview: `ブラウザの getUserMedia でWebカメラ映像を取得し、cv.VideoCapture（video要素のラッパ）で1フレームずつ cv.Mat として読み出して処理する、ライブ処理系デモの入力基盤。原理は、video要素にカメラストリームを流し、requestAnimationFrame ループの中で cap.read(frame) によりその時点のフレームをMat化し、処理して cv.imshow で描き直すことで動画として見せる。結果はリアルタイムに処理された映像になる。HTTPS（または localhost）とカメラ権限が必須で、端末性能でフレームレートが変動する。本アプリでは利用可能な機能として実装されている。`,
    pipeline: [`getUserMedia でカメラ映像を取得し video 要素に流す。`, `cv.VideoCapture(video) でラッパを作る。`, `ループ内で cap.read(frame) によりフレームを Mat 化する。`, `Mat を処理して cv.imshow で描画し、requestAnimationFrame で繰り返す。`],
    params: [{ arg: `VideoCapture(videoEl)`, desc: `再生中の video 要素を渡してキャプチャラッパを作る。ネイティブのカメラ番号やファイルパスではなく、ブラウザではvideo要素ラッパである点が特徴。` }, { arg: `frame (Mat の確保)`, desc: `read先のMatは video の高さ×幅で CV_8UC4(RGBA) を事前確保する。サイズが映像と一致しないと正しく読めない。ループ外で1回作り使い回すとGC負荷を抑えられる。` }, { arg: `cap.read(frame)`, desc: `現在の video フレームを frame にコピーする。requestAnimationFrame ごとに呼び、処理→imshow→次フレーム、を回す。` }],
    code: [
      `let cap = new cv.VideoCapture(videoEl); // 再生中の video 要素をラップ`,
      `let frame = new cv.Mat(videoEl.height, videoEl.width, cv.CV_8UC4); // フレーム格納用(RGBA)を事前確保`,
      `function loop() {`,
      `  cap.read(frame);          // 現在の映像フレームを Mat にコピー`,
      `  // ここで frame を処理（フィルタ・検出など）`,
      `  cv.imshow('output', frame); // 結果を Canvas に描画`,
      `  requestAnimationFrame(loop); // 次フレームへ`,
      `}`,
      `loop();`,
    ],
    useCases: [`カメラ映像にリアルタイムでフィルタ（グレースケール・エッジ検出）をかける。`, `顔検出・動体検知・物体検出など、ライブ入力を要するデモの映像源にする。`, `手や物の動きに反応するインタラクティブな体験を作る。`, `QRコード・マーカー読み取りや、ジェスチャー入力の前段に使う。`],
  },
  "canvas-drawing-input": {
    overview: `ユーザーがCanvas上にマウス・指で描いた線・点・塗りつぶしを取得し、OpenCVデモの入力（マスクや前景指定、手書き数字）に変換するUI基盤。原理は、Canvasのポインタイベントでストロークを描画し、ctx.getImageData でRGBAピクセルを取り出して cv.matFromImageData でMat化する。塗った領域を二値マスクにすれば inpaint や GrabCut に渡せる。結果は、描画内容に応じたマスク画像やMatが得られる。これはOpenCVのアルゴリズムではなくアプリのUI機能で、連携デモとセットで価値が出る。`,
    pipeline: [`ポインタイベントで Canvas にブラシ描画する（太さは brushSize）。`, `ctx.getImageData で描いた内容を ImageData として取得する。`, `cv.matFromImageData で cv.Mat 化する。`, `必要なら二値マスクに整えて inpaint / GrabCut / kNN などに渡す。`],
    params: [{ arg: `brushSize`, desc: `描画ブラシの太さ(px)。太いと広い領域を一気に塗れて inpaint のマスク作成が速いが細部は潰れ、細いと精密に指定できるが手間がかかる。default 10, 範囲1〜50。` }, { arg: `matFromImageData の引数`, desc: `ctx.getImageData が返す ImageData を渡すと CV_8UC4 の Mat になる。マスク用途ではグレー化＋しきい値処理で CV_8UC1 の二値マスクに変換する。` }],
    code: [
      `// ポインタ操作で ctx.lineWidth = brushSize として線を描く`,
      `ctx.lineWidth = 10; // brushSize: ブラシの太さ(px)`,
      `let imgData = ctx.getImageData(0, 0, w, h); // 描いた内容をRGBAバイト列で取得`,
      `let mask = cv.matFromImageData(imgData);    // ImageData → cv.Mat(CV_8UC4)`,
      `// マスク用途: グレー化して二値化し CV_8UC1 マスクに整える`,
      `cv.cvtColor(mask, mask, cv.COLOR_RGBA2GRAY);`,
      `cv.threshold(mask, mask, 1, 255, cv.THRESH_BINARY); // 描いた所=255の二値マスク`,
    ],
    useCases: [`inpaint で消したい領域をブラシで塗ってマスクとして渡す。`, `GrabCut の前景/背景をなぞって指定する対話的セグメンテーション入力。`, `kNN手書き数字認識で、数字を描いて分類器に渡す入力面にする。`, `領域指定が必要なフィルタ（部分ぼかし・部分補正）のマスク作成に使う。`],
  },
};
